# Incomplete security-sweep setup

Stack detection and dispatch planning completed. The installed skill package has catalogs, scripts and invocation templates, but not the specialist agent system prompts/output schemas referenced by those templates. The runtime exposes general agents, not the named audit agents. No audit findings, verifier results or CSO clearance are claimed.

Fallback for this performance slice: independent spec/authorization-boundary review plus real-store corruption, membership, generation mismatch, audience and snapshot regression tests. This is not a full repository security audit. No dependency or credential changes are in scope.
