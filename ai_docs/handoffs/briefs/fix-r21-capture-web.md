# Brief: capture bounds, archive member kinds, the converting source page, URL echo and CI size (R21-M7..M11)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `r21c` (branch `wp/r21c`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: The five capture/web/CI majors of `ai_docs/reports/2026-09-13-code-capture-review.md` are closed at the source with a RED test each: R21-M7 (a converting source's page drops its triples and entities), R21-M8 (extensionless zip members bypass the zip budget), R21-M9 (zip symlink and non-regular members are captured as text instead of classified), R21-M10 (a credentialed URL that fails `is_git_url` is echoed into the 400 body, the redirect `Location` and the access log), R21-M11 (CI runs the acceptance scenario at the ledger size on LadybugDB). Committed on `wp/r21c`.

CONTEXT, per finding (the review's MAJOR table has the exact lines):
- R21-M7 — `src/hippo/web/routes/sources.py:178-192` branches on the `managed` flag, which is true from staging start (ruling 9), so a converting source's page renders its legacy passages with `triples: []`, `entities: []` and no extraction error; CD2 says the source "keeps its complete legacy passages, code nodes and edges in every query". Fix: branch on membership of the view's legacy lane (`context.legacy_lane()`, ruling 14, `evidence-cc1fix.md`) and read the untagged passages with their triples for a converting source; test the page before publication (the converting-source fixtures in `tests/unit/test_converting_source_serving.py` and `test_managed_web_surfaces.py` show the shape).
- R21-M8 — `src/hippo/ingest/repo_capture.py:508-510` builds the zip budget set from `_wanted_member` (`:540-547`, needs a supported name) while `_member_reason`/`_name_reason` (`:525-533`, `:429-437`, `:550-562`) admit extensionless names, which are then read whole into memory before any count or total check (`:364` counts after the walk). CD3 says "bounded". Fix: build the budget set from the same acceptance rule as `_member_reason`, or check the running count and bytes before each `package.read`; test by lowering the budget.
- R21-M9 — `repo_capture.py:514-533` never classifies archive members by `external_attr`; a symlink member is captured as a text file holding its target path (no escape, nothing followed, but CD3/ruling 7 "symlinks, submodules and non-regular files each carry a distinct recorded reason" is false for archives). Fix: record `symlink` for `S_ISLNK(member.external_attr >> 16)` and `not_regular` for other non-regular types; test with a `ZipInfo` carrying `(S_IFLNK | 0o777) << 16`.
- R21-M10 — `src/hippo/ingest/pipeline.py:179-183` and `src/hippo/ingest/repos.py:75-79` put the URL in the `RepoError` message when `is_git_url` fails; `web/routes/sources.py:427-428` redirects with `f"/?error={exc}"` unquoted; `cli.py:286`'s default uvicorn access log then prints it. A credentialed URL that fails `is_git_url` (a host without a dot, `git+https://`) lands in the 400 body, the `Location` header and the access log. Pre-existing (`e8c68836`), not introduced by the slices, but plan §10 privacy applies. Fix: drop the URL from both messages (closed sentence, keep the git-URL hint), `quote()` the redirect, and test both spellings against the body, the header and `caplog`; do not edit `cli.py` unless the access log needs it (say so).
- R21-M11 — `.github/workflows/ci.yml:25-40` runs `pytest tests/unit -q` with `store: ["ladybug", "fake"]`, and `tests/unit/test_code_capture_acceptance.py:74` defaults to 48 files per language, a size no machine has completed. Fix: make the acceptance scenario's default size under pytest small and explicit (8 files per language, the size CD9's CHECK line pins; the ledger size only when `HIPPO_CODE_ACCEPTANCE_FILES_PER_LANGUAGE` says so) OR set the variable in the CI job; state in the evidence exactly what CI runs on each backend after the change, and keep the `HIPPO_CODE_ACCEPTANCE_BUFFER_POOL_BYTES` default (4 GiB) as it is (`evidence-cc11.md` "Scenario knobs"). The fixture BUILDER `tests/fakes/code_capture_repo.py` is r21w's (it is adding breaking shapes); you change only the acceptance file's default-size line and its docstring.

Standing rules (`code-capture-notes.md`): ruling 7 (exclusion reasons on capture result/coverage), ruling 9/14 (lane decision), m6 redaction precedent (`evidence-cc10.md`: `_explain_git_failure` bounded, no URL/path/stderr in messages, tracebacks, logs or rows), `public_errors.py` closed codes.

REQUIRED BEHAVIOR:
1. RED test per finding, then the fix at the source named above; no widening of a refusal into a skip; every existing refusal message that tests pin stays byte-identical except the two URL-echo messages M10 names (pin the new closed sentences).
2. Coverage and capture results record the new archive reasons (`symlink`, `not_regular`) beside the existing ones, and the archive budget refusal is a closed sentence that names no archive (also closes R21-m21).
3. Backends: Fake for everything; LadybugDB for `tests/unit/test_managed_web_surfaces.py -k converting` and `tests/unit/test_repo_capture.py` (one Ladybug run at a time); Neo4j root-owned (no new store shape expected; say so).
4. Also close, if it is a one-liner in a file you already touch, otherwise leave named in evidence: R21-m15 (`_SCP_LIKE` accepts `user:pass@host:path`; refuse and parametrize the credential test over five spellings), R21-m16 (walk-time binary/size decisions go stale before the copy; `tree.paths` walks twice), R21-m20 (`pipeline.py:504` legacy history warning logs the checkout path and git stderr: bound it like `_explain_git_failure`), R21-m21 (above). Leave named: R21-m18 (`managed_activation.py` refusal sentences, not yours), R21-m36 (CD8 wording, orchestrator), R21-m49 (`test_store_ladybug.py` bare `lb.Database`, unowned).

FILES:
  - own: `src/hippo/ingest/repo_capture.py`, `src/hippo/ingest/repos.py` and `src/hippo/ingest/pipeline.py` (the M10/m20 message and log lines only), `src/hippo/web/routes/sources.py`, `src/hippo/cli.py` (only if M10 needs it), `.github/workflows/ci.yml`, `tests/unit/test_code_capture_acceptance.py` (the default-size line and docstring only), `tests/unit/test_repo_capture.py`, `tests/unit/test_ingest_repos.py`, `tests/unit/test_managed_web_surfaces.py`, `tests/unit/test_managed_web_ingress.py`, `tests/unit/test_ingest_pipeline.py` (M10 test), NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-r21c.md`.
  - do NOT touch: `tests/fakes/code_capture_repo.py`, `src/hippo/knowledge/staged_code.py`, `src/hippo/store/generations.py`, `src/hippo/ingest/code_generation.py`, `tests/unit/test_code_generation.py`, `tests/unit/test_staged_code_writer.py` (r21w), `src/hippo/hipporag/indexer.py`, `src/hippo/store/code.py` (r21i), `src/hippo/knowledge/access.py`, `src/hippo/knowledge/build_authority.py`, `tests/unit/test_build_authority.py`, `tests/unit/test_code_projection.py` (r21a), `src/hippo/ingest/managed_activation.py`, `src/hippo/store/ladybug.py`, `src/hippo/status.py`, `src/hippo/context.py`, `GATES.md`, `docs/`, the plan, other evidence files.

STEPS: worktree + venv (`mcp==2.1.1` pin); baseline the CD3 CHECK line from `GATES.md` plus `test_ingest_repos.py test_managed_web_surfaces.py test_managed_web_ingress.py test_ingest_pipeline.py` on Fake (AnyIO form (b) where a file imports `fastapi.testclient` at module level); RED (`/tmp/hippo-r21c-red-*.log`); fix; GREEN Fake; GREEN LadybugDB per item 3 (`/tmp/hippo-r21c-ladybug-*.log`); Ruff check + format --check on every touched file, the workflow YAML and the evidence; evidence with per-finding before/after, the CI statement and the minors closed/left; one or two commits.

DONE WHEN: every RED test is green on Fake, the CD3 line and the web suites are green, LadybugDB lines green, CI's acceptance size is explicit and stated, evidence written; `horch done` lists the commits, per-finding outcome, counts and logs.

REPORT: `horch note` per finding; `horch tell orchestrator "[<role>] BLOCKED: ..."` if a fix needs a file above (name the exact function) or if M7's lane read needs a store method that does not exist.
