# Brief: CC11 — acceptance evidence and ledger amendments (managed code capture, task 11)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc11` (branch `wp/cc11`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC1–CC10 are merged).

GOAL: Every CD1–CD10 CHECK line of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md` is run by you on its stated backend from a clean tree, the results are recorded in one evidence file, the multi-hundred-file Ladybug fixture of CD9 exists and passes within the plan's section 8.3 ceilings, and the ledger amendments the design review and the slices asked for are prepared as EXACT replacement lines for the orchestrator to apply — you do not edit `GATES.md`, you do not tick a box, and you do not run Neo4j. Plan sections 8.3, 12 (CC11 row) and 13 of `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`; design review m1, m2, question 10 and the CRITERIA amendments it lists (`ai_docs/reports/2026-09-12-code-capture-plan-review.md`); gates CD9 and CD10. Committed on `wp/cc11`.

CONTEXT: the gate checker is the orchestrator's (`UNLAZY_APPROVAL_DIR=/tmp/hippo-rag-gate-approvals node /Users/mascott/.agents/skills/dev-gates/scripts/gate-check.mjs --approve --reverify --timeout N <LEDGER> --root . --cwd .`); it runs each CHECK line verbatim from `/Users/mascott/projects/hippo` and matches EXPECT as a substring, and it rejects duplicate EVIDENCE lines, so you write NO `EVIDENCE:` line anywhere. The Neo4j parity run under CD9 is root-owned (one disposable container, one pytest process at a time, the PC-N4 convention in `ai_docs/gates/rag-it-all/task-5-prose-coordinator/GATES.md:16`): the orchestrator runs it and records the note; you list the files it must cover. The independent SPEC and QUALITY review CD10 requires is a separate reviewer the orchestrator spawns after you finish; your evidence is its input. Every slice's evidence file `evidence-cc1.md` … `evidence-cc10.md` in `ai_docs/gates/rag-it-all/task-5-code-capture/` is your source of truth for what shipped; `ai_docs/handoffs/briefs/code-capture-notes.md` holds the pending ledger amendments already drafted (section "Ledger amendments pending (orchestrator applies)").

PRIOR WORK: CC2's evidence states on which backend the CD1 query bound is proven (M4: Ladybug creates no secondary index); CC4's evidence item 1 corrects plan section 2's claim about `read_plain_provenance`; CC1's evidence finding 4 and CC10's evidence give the CD2 destructive-operation line its proof; CC3/CC8/CC9b give CD7's B4/B5/M2/question-4 clauses their tests.

REQUIRED BEHAVIOR:
1. Run every CHECK line CD1–CD8 and CD10 exactly as written (after the m1 fix below is applied by the orchestrator — if CD5 still names `tests/unit/test_input_binding.py` when you start, run the corrected line and say so), each to `/tmp/hippo-cc11-cd<N>.log` with `echo EXIT $?`, and CD9's Ladybug line to `/tmp/hippo-cc11-cd9.log`. Then the full Fake suite once (`HIPPO_TEST_STORE=fake .venv/bin/pytest tests -q -o addopts='' -W error -W "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning" > /tmp/hippo-cc11-full-fake.log 2>&1; echo EXIT $?`). Any failure is a finding you report with the failing test name and log path; you do not fix production code (stop and ask; the orchestrator routes a fix worker).
2. CD9 fixture: NEW `tests/fixtures/code_capture/` (or the location `tests/fakes/code_fixture.py` already uses; check) providing a deterministic multi-hundred-file repository builder (several hundred files across the walker languages, a README, `go.mod`-style extensionless files, one `node_modules`-like ignored directory, one oversized file, one binary, one symlink, and a handful of commits) plus NEW `tests/unit/test_code_capture_acceptance.py` that on Ladybug converts it through `build_code_source`, closes and reopens the store, asserts pointers, fences, exact membership, native rows and relations, bindings, manifests, raw references, resumable staging state and coverage survive, projects `StructuralCodeEvidence` with exact `DEFINED_IN` support and real line locators through `query_session`, routes through verified dense dispatch, retires the generation under a live snapshot and reconstructs it, and asserts the whole run stays within the section 8.3 ceilings with the CD1 query counter bound holding. State explicitly that the fixture does NOT reach the 50,000-symbol ceiling and that the CD1 linearity proof is CC2's synthetic Fake test (M5). Add the new test file to CD9's CHECK line as a proposed amendment (item 4).
3. Plan amendments: append a section "Post-implementation amendments (CC11, 2026-09-xx)" to `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md` (append only; do not rewrite earlier sections, the orchestrator has been editing them) recording, each as one short paragraph with the section it supersedes: section 2's `read_plain_provenance` correction (CC4 evidence item 1) and the `_source_control` third presence test (review B3); section 5's normalization (CC4 evidence M8: whole path, lowercase host and path, `https` transport) and the walker-version drift note (review m7); section 7 rewritten per ruling 9 (loader-selection set versus serving lane, three-term `source_serves_legacy`); section 8.2's reclaim as the explicit B5 list and the capture instant on `Generation.created_at` (B4); the rebaseline paragraph per M6/M7 as CC8 built it; section 9's `evidence_class` row and the first-parent coverage entry; section 10's redaction recorded as work done by CC10 and the note that `EvidenceSpan.text` stores code text by design (review question 8). Run `ruff format --check` on the plan afterwards.
4. Ledger amendments: verify each pending line in `code-capture-notes.md` against what actually shipped, correct any that no longer match, add CD9's new test file to its CHECK line, and add any CRITERIA line a slice's evidence asks for that is not yet there. Write the final set into your evidence file under "Ledger replacement lines" as literal `CHECK:`/`CRITERIA:`/`EXPECT:` lines, one per gate, ready to paste. CD10's EXPECT becomes `10 files already formatted` (m2; count the files in the line and make the number match — if CC9a added `build_run.py` to the formatter list, it is 11, say so).
5. Evidence, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc11.md`: one table (gate, backend, command, result line, EXIT, log path), the full-suite result, the fixture description with its counts, the Neo4j parity file list for the orchestrator (every file CD9 names plus `test_generation_resume.py`, `test_staged_code_writer.py` and `test_managed_code_activation.py`), the ledger replacement lines, and an "open findings" list for the reviewer with file:line evidence. Ruff-format clean.

FILES:
  - own: NEW `tests/unit/test_code_capture_acceptance.py`, the NEW fixture builder (name it in the evidence), `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md` (append only), NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc11.md`.
  - do NOT touch: `GATES.md` (the orchestrator applies your lines), any `src/hippo/*` file, any existing test, `docs/`, the checkpoint, other slices' evidence files.

STEPS: worktree + venv (with the `mcp==2.1.1` pin); run CD1–CD8 and CD10 on Fake first and record; write the fixture and acceptance test RED then GREEN on Fake; run CD9 on Ladybug (expect a long run; do not pipe to `tail`); the full Fake suite; the plan appendix; the evidence; commit in two commits (fixture and test; plan appendix and evidence). This slice fits one worker because it writes one fixture, one test file and two documents, and otherwise runs lines that already exist.

DONE WHEN: every CHECK line's result is recorded with its EXIT code; CD9 passes on Ladybug with the new file; the full Fake suite result is recorded; the plan appendix and evidence are formatted; `horch done` lists every gate's result line, the fixture counts, the Neo4j file list and every open finding.

REPORT: `horch note` per gate; `horch tell orchestrator "[<role>] BLOCKED: ..."` the moment any CHECK line fails, with the test name and log path, then continue with the remaining gates while you wait.

PRIOR WORK ADDENDUM (orchestrator, 2026-09-13; read `ai_docs/handoffs/briefs/code-capture-notes.md` in full, it is the running record): CC1–CC9b and the two fixes (cc1fix at `d56b618`, lbfix at `9770c00`) are merged; CC10's evidence lands before you start. Items the plan's post-implementation amendments section MUST record, each with its evidence pointer: ruling 5 (repo prose via OpenIE) is DEFERRED by name to a follow-up slice — prose files inside a tree are ordinary passages with coverage `openie="skipped"`, reason `prose_extraction_deferred` (CC9b); ruling 11 (`PROSE_EXTENSIONS` is the sole prose test); rulings 12–14 (no knowledge→ingest import; the `code` generation profile with history_event pairs excluded from identity; the legacy lane serves only untagged rows and `context.legacy_lane()` decides it); plan section 4's `community` label CANNOT be written by this lane (`symbol_write_row` closed dict; CC8) — drop the claim or open a `store/code.py` slice; `ObjectKind` lacks `collection`/`label`/`rel_type` (mapped to `resource`, CC6) and two C# overloads share one codegraph native ID (recorded defect, CC6); a crash between seal and publication is not resumable (store contract, CC3/CC9b); the lease heartbeat can latch before a between-batch rebaseline unless CC10 took the `build_run.py` hook; `validate_generation_profile` does O(members) bounded `_knowledge_get` pairs at query time (CC8); `content_kind` is unset on code passages (CC8); `chunk_overlap_chars` is inert for code yet sits in identity (CC6 finding 11); renames are `not_reported`; `CodeTreeInput.paths` is an exclusion complement. Ledger: CD9's CRITERIA must state the index-backed CD1 bound is proven on Neo4j (parity run 2), not on Ladybug (no secondary-index DDL, CC2), and gain "a refresh over an unchanged file or commit reuses its immutable revision" for the reopen case; the Ladybug engine defect (`IN <list>` on node string columns, `evidence-lbfix.md`) and its tripwire belong in the plan's amendments and in the CD9 evidence. Timing context, not a claim: one managed code build is ~55 s on Ladybug vs ~0.6 s on Fake (CC9b), so size the multi-hundred-file fixture accordingly and never present it as a symbol-ceiling proof (CD9 pending line).
