# Brief: independent SPEC/QUALITY review of activation Task 4c (MCP, local CLI, remote client)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`); the work is merged at HEAD (merge commit "Merge wp/pa4c", branch commits `0d80916`, `db64ade`, `ca8407c`, `96cebf9`, `a147380`, `761dfb2` on top of `ffd2265`). You read and run; you do not edit source or tests.

GOAL: Independent SPEC and QUALITY verdict on Task 4c before publication, with particular attention to authentication boundaries.

CONTEXT:
- Plan `ai_docs/plans/rag-it-all-task-5-production-activation.md`: "Build actors at every boundary" (MCP, remote CLI, direct local `hippo index`), "Production query-session activation" (local CLI code and source listing), "Safe transport failures" (MCP `ToolError`, remote client, CLI stderr). Gates PA1 (MCP/CLI half), PA6. The reviewed transport separation from the earlier MCP subtask (`ai_docs/gates/rag-it-all/task-4-mcp/GATES.md` if present; HTTP MCP resolves only the request credential through `AuthGate`; stdio MCP resolves `HIPPO_TOKEN`; `TransportBoundServer`) must be intact.
- Orchestrator brief: `ai_docs/handoffs/briefs/pa4c-mcp-cli-remote.md`; contracts in `ai_docs/handoffs/briefs/task4-notes.md`. Implementer evidence: `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa4c.md`, including the verbatim CLI principal rule (evidence commands resolve `principal_from_bearer(ctx, HIPPO_TOKEN, require_online=True)`; users exist plus valid token gives that reader; users exist plus missing/unknown token exits 2 before any Source row; store unreachable exits 2; never-gated gives `Principal.open(top_role)` with no build actor; administrative commands ungated and outside the mapping), the `tool_failure` rule (exact-type `ValueError` keeps its text; `AuthorizationChanged` maps to a shared `DENIED` sentence with no code), the `remote.py` probe-timeout fix, and seven declared deviations.
- Diff: `git diff ffd2265..761dfb2 -- src/ tests/`.

FILES:
  - own: `ai_docs/reports/2026-09-11-pa4c-review.md`.
  - do NOT touch: anything else.

STEPS:
1. Run, each to `/tmp/hippo-pa4c-review-<n>.log` with `echo EXIT $?`: (a) `HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_managed_transport_activation.py tests/unit/test_mcp_server.py tests/unit/test_mcp_http.py tests/unit/test_cli.py tests/unit/test_managed_route_activation.py tests/unit/test_web_auth.py -q -o addopts='' -W error -W "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning"`; (b) `HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_cli.py -q -o addopts='' -W error` (must pass bare); (c) `HIPPO_TEST_STORE=ladybug .venv/bin/pytest tests/unit/test_managed_transport_activation.py tests/unit/test_mcp_server.py -q -o addopts='' -W error -W "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning"`.
2. SPEC review; PROVEN (test) / UNTESTED / VIOLATED (file:line): (a) HTTP MCP ingress passes a reader actor only for a real `AuthGate` reader, never reads `HIPPO_TOKEN`, open MCP stays legacy; stdio MCP passes the token-resolved reader; neither manufactures `trusted_local`; (b) remote CLI inherits server authorization and prints only code and message; (c) local `hippo index` gated/open rules exactly as the plan and the evidence state, refusal before any Source row or saved byte; (d) MCP ask/search and CLI ask hold one structural owner dispatched through `retrieval_session`; MCP sources/code tools and CLI code/source listing hold a structural `query_session`; (e) failures carry the same code and message across MCP `ToolError`, CLI stderr and the remote client; redaction across MCP results, stdout, stderr and logs; (f) the `test_cli.py` fix is a genuine usage correction (an injected client owns its transport settings) and a real client still gets a bounded probe.
3. QUALITY review, with attention to: the exact-type `ValueError` pass-through in `tool_failure` (enumerate what still reaches a client verbatim and whether any of it can carry private text: `AmbiguousSymbol`/`UnknownSymbol` candidates are sanctioned, anything else is not); the `require_online=True` fail-closed behavior when the store hiccups on a never-gated deployment (is exit 2 the right answer there, or does it break the documented open mode?); the shared `DENIED` sentence carrying no code (does any client need to distinguish denial from unavailability?); `cmd_index` printing the Source row's stored error verbatim (prove the stored form is always the closed code plus bounded sentence); deviation 2 (gated `hippo index` sets no `owner_id`/`access_role_id` while `hippo_remember` sets both: is the resulting source manageable by its creator?); the transport separation invariants still holding under the new code paths.
4. Report any changed public signature in `remote.py`, `mcp_server.py`, `cli.py`.

DONE WHEN: `ai_docs/reports/2026-09-11-pa4c-review.md` exists with `SPEC: PASS|FAIL`, `QUALITY: PASS|FAIL`, the a–f table, numbered findings with severity, file:line, why, proposed fix, run results with log paths. `horch done` states both verdicts, finding counts by severity, and the report path.

CONSTRAINTS: no edits outside your report; no Neo4j; `HIPPO_TEST_STORE` explicit on every command; MCP HTTP tests need local socket permission (the Claude worker has it).

REPORT: `horch note` after the runs, after the SPEC table, after the report.
