# Brief: independent SPEC and QUALITY review of managed code capture (gate CD10)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`) at the HEAD named in the spawn message (CC1–CC11 are merged; the orchestrator has run the Neo4j parity). You read and run; you do not edit source, tests, the plan or the ledger.

GOAL: State whether CD10 of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md` ("independent SPEC and QUALITY reviews pass with every finding closed") can be signed, by reviewing the merged code-capture slices against the approved design and re-running every CD gate line whose files the slices touched.

CONTEXT: the design `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md` with its "Orchestrator rulings (2026-09-12)" 1–10 (ruling 6 is REVERSED by ruling 9; ruling 9 was amended so a tombstoned converting source serves neither lane) and its appended "Post-implementation amendments" section; the design review `ai_docs/reports/2026-09-12-code-capture-plan-review.md` (B1–B6, M1–M11, m1–m7 and the per-task table: every blocker and major must be closed by a named slice or explicitly deferred with a reason); the per-slice evidence `evidence-cc1.md` … `evidence-cc11.md` and `neo4j-parity.md` beside the ledger; the cross-slice notes `ai_docs/handoffs/briefs/code-capture-notes.md` (open items, grants, pending ledger lines); the parent activation plan `ai_docs/plans/rag-it-all-task-5-production-activation.md` and `docs/rag_it_all.md` Task 5 / G5 for the obligations code capture discharges.

ROUND 2 (2026-09-14, if the spawn message says so): round 1 is `ai_docs/reports/2026-09-13-code-capture-review.md` (architect-reviewer-21 at `cbed8ca`: NOT SIGNABLE, R21-B1..B5, M1..M12, m1..m49). The four fix slices are merged: r21i (`evidence-r21i.md`, B4), r21c (`evidence-r21c.md`, M7–M11 + m15/m20/m21), r21w (`evidence-r21w.md`, B1/B2/B3/B5 + m3/m6/m10/m31, fixture breaking shapes), r21a (`evidence-r21a.md`, M1 authority half + measurement, M2, M4, M6, M12 fixed with `code-binding-v2`, m24/m26/m35; m4 pinned as the single named whole-read exception). The ledger lines they proposed were applied by the orchestrator; the notes file's "CD10 review round 1" table maps every finding to its slice and lists the minors nobody took. Write `ai_docs/reports/2026-09-14-code-capture-review-round2.md`. Re-verify every B and M by reading the fix at HEAD by symbol and by running the reproduction the round-1 report gave (the scratch copies at `/tmp/h21scratch/` and the slices' RED tests), confirm each closed row's test discriminates, re-check the gates' CRITERIA against HEAD, list the minors still open by id, and rule CD10 SIGNABLE or NOT SIGNABLE with the blocking ids. Do not re-run the whole CD9 LadybugDB line if the orchestrator's checker log for it at HEAD is named in the spawn message; cite it.

REQUIRED:
1. SPEC: for each of CD1–CD9, confirm the CRITERIA line is literally true of HEAD (read the code, not the evidence) and that the CHECK line's suites assert each clause; name any clause with no test. For each design-review blocker and major, cite the closing commit or evidence line, or mark it OPEN.
2. Invariants that must hold and that you verify by reading: no staged row of any kind is reachable from a query, status or export surface before publication (CD2, ruling 9); every managed native ID is namespaced and `_validate_managed_native` re-derives it (Task 5 namespace increment); `BuildAuthority.rebaseline` adopts only the authorization epoch and refuses on any suppression, capability or `SourceControl` change (ruling 2, M6); the capture instant is persisted on `Generation.created_at` and never in an identity field (B4); reclaim keeps all four `claim_generation_build` guarantees (B5); derivation rule versions are in the generation configuration and the resume probe asserts absence of unexpected rows (ruling 10, M2); `repos.py` never logs a clone URL with credentials or raw git stderr (m6); the legacy lane's behaviour for non-managed sources is byte-identical (CD1, CD2).
3. QUALITY: layering (`knowledge` never imports `ingest`; `tests/unit/test_import_order.py` and the allowlist), duplication against the prose precedents (`input_binding.py`, `staged_prose.py`, `prose_generation.py`), error surfaces through `public_errors.py` only, determinism (canonical ordering, no wall clock in pure modules), and test honesty (no test that passes vacuously, no widened tolerance).
4. Re-run: every CD CHECK line on Fake in the root tree with `-W error`; the Ladybug lines the evidence files claim (`HIPPO_TEST_STORE=ladybug`), one at a time. Never Neo4j (root-owned; cite `neo4j-parity.md`). Run `.venv/bin/ruff format --check` on your report before finishing.

FILES:
  - own: `ai_docs/reports/<today>-code-capture-review.md`.
  - do NOT touch: anything else.

DONE WHEN: the report exists with a findings table (id, severity BLOCKER/MAJOR/MINOR, file:line, the gate or ruling it violates, the exact fix), a per-gate SPEC verdict, a QUALITY verdict, the command outputs you ran, and a one-line CD10 verdict: SIGNABLE or NOT SIGNABLE with the blocking finding ids. `horch done` with the verdict, the counts of findings per severity, and the report path.

REPORT: `horch note` per gate reviewed; `horch tell orchestrator "[<role>] BLOCKED: ..."` only for a missing input, never to ask whether a finding matters (report it; the orchestrator triages).
