# Independently review proof-reader reuse and prepare paired measurement

Fresh Codex Sol worker, root `/Users/mascott/projects/hippo`, base decoder commit `4c22322`. Use horch only; no Claude/nested workers. Own sanitized `ai_docs/reports/2026-09-16-proof-inventory-review.md` and scratch `/private/tmp/hippo-proof-ab-*`. No source/test edits, commits or models. Other workers still run tests; do not execute the benchmark until the orchestrator says those are terminal.

Part A: review the complete `ai_docs/plans/2026-09-16-proof-inventory-reuse-plan.md` and the current diff in access.py/derivations.py plus new/updated tests. Do not read the implementer's report before writing spec verdict. Confirm raw rather than audience-filtered reads, complete enumeration of nonmember dependencies, exact membership on every lookup, independent reader lifetime per proof, same-store enforcement, standalone behavior and native reads unchanged. The orchestrator requested preserving original constructor validation order before membership reads; inspect the final corrected code. Report each requirement with file:line evidence, then quality verdict and concrete findings. Flag any missed mutation/lifetime pathway; do not generalize away security assertions.

Part B: prepare a deterministic paired proof measurement harness. Baseline is exact source at git commit 4c22322; use a read-only `git archive` of its src tree extracted into a new private scratch directory, not a worktree or checkout mutation. Candidate imports root src. Both subprocesses use the same root .venv and explicit PYTHONPATH to the intended src, verifying imported hippo path in output. Each opens a separate fresh copy of `/private/tmp/hippo-real-eval-YCrJeD/data`, builds the same internal EVERYTHING EvidenceSelection over all manifest active generations (as `/private/tmp/hippo-speed-round1-FfTJFE/profile_proof.py` does), and uses 512 MiB Ladybug buffer.

For each variant, record one cold and three warm `EvidenceAccess.build` runs in the same store. Count SQL operations, time with perf_counter, serialize the complete AuthorizedEvidence dataclass canonically (all sets sorted; datetimes as ISO) into private artifacts, and assert all runs/variants have exactly equal proofs. Record source hashes, git revision, import path, counts and timing. No model calls. Avoid cProfile overhead in the primary timings; existing profile is sufficient. Close stores in finally. Fail loudly if evidence differs or a variant fails; preserve all outputs. Never reuse output/data directories.

Do not execute while tests run. You may finish this bounded task once review and runnable harness are written and syntax-checked, reporting the exact command/output path for the orchestrator. Alternatively, if the orchestrator supplies a specific release before you finish, execute once and add the measured result. Do not infer test completion from a stale file; rely on the orchestrator's process verification.

Keep private corpus/proof contents in /private/tmp; repository report uses only sanitized counts/findings/artifact paths. Ruff report and whitespace check. Report DONE via horch tell, record horch done, close. Sandbox-denied ledger writes are a reporting limitation; never change trust/security settings.
