# Brief: scope the knowledge-table reads on the managed write path (CD1's second half)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `kscope` (branch `wp/kscope`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: A managed code (and prose) build performs no whole-table read of `GenerationEvidenceMember`, `GenerationMember`, `DerivedDependency` or any other knowledge table per record written; every such read is scoped by `generation_id` (or by an exact `where=` key on CC2's per-kind allow-list) so a build's cost is linear in the generation. CC11 measured the defect on Fake (`/tmp/hippo-cc11-scale-fake.log`, probe `tests/unit/test_zz_cc11_scale_scratch.py` in its worktree `.worktrees/cc11`, uncommitted): 10 files 1.1 s / 600 evidence members; 40 files 8.4 s / 2,040; 160 files 109.5 s / 7,800, with ~3 unscoped reads per member (at 160 files: 22,809 `GenerationEvidenceMember`, 12,699 `GenerationMember`, 10,608 `DerivedDependency` rows re-read). Gate CD1 of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md` ("per-batch work is bounded by the batch rather than by corpus size") must become true for the knowledge tables too. Committed on `wp/kscope`.

CONTEXT: CC2 (`evidence-cc2.md`, merged at `c461c9c`) scoped the NATIVE reads and gave every backend `_knowledge_rows(name, *, generation_id=None, where=None)` over a closed per-kind field allow-list (`store/knowledge.py`), plus schema v6 indexes (Neo4j only; Ladybug has no secondary-index DDL, so its bound is the predicate). The Ladybug engine defect (`evidence-lbfix.md`): never use `IN <list>` on a node string column; use the UNWIND primary-key helper. Call sites CC11 found: `src/hippo/store/generations.py:~675` (`_check_knowledge_write`: `frozen = [m for m in _knowledge_rows("GenerationEvidenceMember") if self._generation(m.generation_id).status != "staging"]` on EVERY put), `:~641` (a `GenerationMember` read on each `GenerationEvidenceMember` put), `:~658/:~663` (`GenerationMember` + `GenerationEvidenceMember` on each `NativeBinding` put), all reached from `knowledge/staged_code._write_batch:~467/:~482` (and the prose writer's equivalent); `src/hippo/knowledge/derivations.py:~115-123` (`_Inventory.__init__` reads whole `GenerationEvidenceMember` + `GenerationMember`) reached per rendered passage via `generations.py:~1477 _validate_managed_native -> validate_view`; `DerivedDependency` unscoped via `validate_view` and `validate_generation_derivations` (`generation_checksums:~971`).

REQUIRED BEHAVIOR:
1. Every listed read becomes scoped: by `generation_id` where the semantics are per-generation, or by an exact key (`where={"record_id": ...}`, `where={"generation_id": ..., "record_kind": ...}`, and so on) added to the per-kind allow-list with its index in `store/base.py` (Neo4j schema step, journaled as a v7 step ONLY if a new index is required; keep v6 frozen with its checksum, follow CC2's precedent exactly). The `frozen` check at `:~675` must not enumerate every non-staging generation's members: invert it (read the members of the record being written, or the sealed generations' members for that record id).
2. Semantics unchanged: every existing refusal message and every checksum byte-identical; the prose writer and the code writer produce the same rows as before (the reviewed suites prove it); `_Inventory` keeps its contract for `validate_view`, built once per batch, not per passage, where the caller allows (hoist; do not change `validate_view`'s signature if a batch-level cache can be threaded through `_validate_managed_native`'s existing `selected=` seam).
3. Bound proven: extend CC2's query counter (`tests/unit/test_generation_scoped_reads.py`) so `knowledge_whole_table()` is `[]` across a full `build_code_source` on Fake, and a test that building 10, 40 and 160 synthetic files (reuse CC11's probe shape) is linear in evidence members within a stated constant; CC11's probe is your RED.
4. Backends: Fake, Ladybug (`test_generation_scoped_reads.py test_staged_code_writer.py test_staged_prose_writer.py test_code_generation.py -k "publish or refresh or resume"`), Neo4j root-owned (name the new query shapes).

FILES:
  - own: `src/hippo/store/generations.py`, `src/hippo/store/knowledge.py` (allow-list + query methods), `src/hippo/store/ladybug.py`, `src/hippo/store/memory.py`, `src/hippo/store/base.py`, `src/hippo/store/migrations.py` (only if a journaled step is required), `tests/fakes/fake_store.py` (query methods and counters), `src/hippo/knowledge/derivations.py`, `tests/unit/test_generation_scoped_reads.py`, NEW `tests/unit/test_knowledge_scoped_reads.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-kscope.md`.
  - do NOT touch: `src/hippo/status.py` (opus-22), `src/hippo/knowledge/staged_code.py`, `staged_prose.py`, `code_binding.py`, `code_history.py`, `build_authority.py`, `generation_profiles.py` (ask if a seam is needed), `src/hippo/ingest/*`, `context.py`, `GATES.md`, `docs/`, CC11's files.

STEPS: worktree + venv (`mcp==2.1.1` pin); baseline the CD1 and CD7 CHECK lines from `GATES.md` plus `test_staged_prose_writer.py test_prose_generation.py test_code_generation.py` on Fake; RED = the probe's whole-table counter and the superlinear timing; fix; GREEN Fake on the baseline set + new tests; GREEN Ladybug on the list above; Ruff; evidence with the before/after counts (10/40/160 files) and every changed query shape; two commits (readers/allow-list, then consumers).

DONE WHEN: green on both backends with `-W error`; checksums unchanged; the linearity test passes; evidence written; `horch done` lists the scoped reads, the consumers switched, the before/after numbers, Neo4j shapes and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` if a seam in a frozen file is required (name the exact function).
