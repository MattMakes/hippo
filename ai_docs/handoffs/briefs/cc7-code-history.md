# Brief: CC7 — git history binding (managed code capture, task 7)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc7` (branch `wp/cc7`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC4, CC5 and CC6 are merged).

GOAL: A pure materialisation of a repository's git history for one code generation: one `history_event` artifact and revision per commit with the plan's section 9 temporal fields, commit knowledge objects and observations, the commit-message original spans and the rendered commit passage views, and the `MODIFIES`/`PRECEDES` native rows in the shape the staged writer will write under the generation fence — with every truncation honestly recorded in coverage and no wall clock anywhere. Gate CD6 of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`; plan sections 4, 5, 6 (step 5) and 9 of `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`; rulings 1, 4, 7, 10. The design review (`ai_docs/reports/2026-09-12-code-capture-plan-review.md`) leaves CC7 unchanged but binds m4 and m5 (below). Committed on `wp/cc7`.

CONTEXT: `src/hippo/codegraph/git_history.py` (`read_history(checkout, symbols, source_id, *, depth, timeout_s, total_s, should_stop, node_namespace)` at `~:192`, `History` at `~:124` with `commits`/`modifies`/`precedes`/`skipped`/`truncated`, `LOG_FORMAT` at `~:80` capturing `%aI`, the `--first-parent` walk at `~:319`, `_shallow` at `~:292`); the legacy consumers you replace rather than wrap: `src/hippo/ingest/pipeline.py::_read_history` (`~:454`) and `src/hippo/hipporag/indexer.py::_write_code_graph` (`add_commits`/`add_modifies`/`add_precedes`, untagged); the commit passage shape `src/hippo/ingest/chunker.py::_chunk_commits` (`~:92`); the model (`src/hippo/knowledge/model.py`: `Artifact` kind `history_event`, `ArtifactRevision` fields `provider_revision`/`source_updated_at`/`source_timestamp_original`/`source_timezone`/`source_precision`/`observed_at`, `ObjectObservation` as a `TemporalRecord` with `evidence_class` in its identity, `EvidenceSpan` locator kinds, `NativeBinding` with `native_kind="Commit"`); identities `src/hippo/knowledge/identity.py::repository_identity` and `src/hippo/codegraph/model.py::commit_id(..., node_namespace=...)`; the prose view precedent `src/hippo/knowledge/input_binding.py::_view`; CC6's `src/hippo/knowledge/code_binding.py` and its evidence `evidence-cc6.md` (read it FIRST: it says exactly which commit-related records CC6 did or did not produce and what input hook it left for you); the Task 5A vocabulary (`ValidityKind`, `TemporalBasis`, `TemporalPrecision` literals in `model.py:~46-53`).

PRIOR WORK (CC6, merged at `e02bb08`; read `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc6.md` in full, its twelve findings bind you): `src/hippo/knowledge/code_binding.py` exposes `code_generation(capture, *, workspace_id, source_id, generation_identity_inputs, observed_at) -> Generation` and `materialize_code_evidence(capture, chunks, facts, *, workspace_id, source_id, generation_identity_inputs, observed_at) -> CodeEvidenceBundle`; the bundle passes `commit` chunks (CC5 `CommitSegment`, no original span) through UNBOUND as `commit_chunks`, and you own every commit-side record (ruling in `code-capture-notes.md` item 5b/5d): `history_event` artifacts and revisions, commit `KnowledgeObject`s and `ObjectObservation`s (`evidence_class='declared'` for the commit message field), commit-message `EvidenceSpan`s with a `FieldLocator`, the commit views over them, MODIFIES/PRECEDES rows, and the `history_event` `GenerationMember`s. CC6 imports nothing from `hippo.ingest` (ruling 12: structural validation, pinned expected rule versions); do the same, pin `EXPECTED_CODE_BINDING_RULE_VERSION` against CC6's `CODE_BINDING_RULE_VERSION` and fold your own `CODE_HISTORY_RULE_VERSION` under CC6's reserved configuration key `code_derivation` through the merge, refusing a caller that already set your key. Ruling 13: repository and history_event revisions ARE generation members with spans (CC8 lands the `code` profile that lets the merged bundle seal; your tests are pure and never seal). Evidence classes: `syntax_observed` symbols, `catalog_observed` files, `declared` repository/commit. The prior CC4 facts follow. CC4's `RepositoryDescriptor` (`repo_capture.repository_descriptor`) is the repository key (whole normalized clone path, M8); CC4 records the shallow-clone boundary and `files_skipped` on its capture result. CC5's `PreparedCodeChunk` of kind `commit` carries the commit rendering as a generated segment. CC6's `CodeEvidenceBundle` carries `CODE_BINDING_RULE_VERSION` and CC5's `CODE_CHUNK_RULE_VERSION` and computes `generation_id` from the configuration; you add nothing to that identity except your own rule version (ruling 10).

REQUIRED BEHAVIOR:
1. `src/hippo/knowledge/code_history.py`: `bind_history(history, *, code_bundle, repository, workspace_id, source_id, generation_id, generation_namespace, observed_at, history_depth, shallow_boundary) -> CodeHistoryBundle` (keyword names may follow CC6's spellings; keep them keyword-only), pure: no store, no model, no clock — `observed_at` is the one injected capture instant. It produces, deterministically and in canonical order: one `Artifact(kind="history_event")` per commit (external id from repository plus SHA) with one `ArtifactRevision` whose `provider_revision` is the SHA, `source_updated_at` the author date, `source_timestamp_original` the raw `%aI` string, `source_timezone` its offset, `source_precision="second"`, `observed_at` the capture instant; one `KnowledgeObject(kind="commit")` keyed by repository plus SHA; one `ObjectObservation` per commit with `valid_from` = author date, `valid_to=None`, `validity_kind="explicit_interval"`, `temporal_basis="commit"`, `temporal_precision="second"`, `recorded_from` = the capture instant, `recorded_to=None`, and an explicit `evidence_class` (m5; `declared` is the natural value for a commit's own metadata — name your choice and why in the evidence); one original `EvidenceSpan` per commit message on the `history_event` revision with a real locator from the closed set (a `field` locator naming the message field; never a synthetic whole-file `file_lines` span); one `RetrievalView`+`DerivedRecord`+`DerivedDependency` per commit passage over that span exactly as `input_binding._view` does, unless CC6 already produced it (then consume CC6's and do not duplicate); the native `Commit` rows carrying `generation_id` and the namespaced `commit_id`, each with a `NativeBinding` to its commit object and span; the `MODIFIES` (with hunks) and `PRECEDES` row shapes `read_history` already emits, restricted to symbols of THIS generation (an edge to a symbol outside the bundle is refused, not dropped silently); and the exact `GenerationEvidenceMember` closure for everything you add.
2. Coverage: the bundle carries a `coverage` mapping for `coverage_json` recording `History.skipped`, `History.truncated`, the shallow-clone boundary, renames if `read_history` reports them, the configured depth, and — m4 — the first-parent restriction (`history_walk: "first_parent"`) so a first-parent walk is never presented as the repository's complete history. `history_depth == 0` produces no `history_event` artifact and records `history: "disabled"`.
3. Merge contract: export `merge_code_bundles(code_bundle, history_bundle) -> CodeEvidenceBundle`-shaped result (or whatever CC6's evidence names as the writer's input type) that unions the two inventories and re-runs the CC6-style final validation: one generation, no duplicate record, no orphan binding, every cited byte covered exactly once, every `MODIFIES`/`PRECEDES` endpoint present. CC8 consumes the merged result.
4. Rule version: export `CODE_HISTORY_RULE_VERSION`; the bundle carries it; a test pins the value.
5. Tests, NEW `tests/unit/test_code_history.py`: build `History` values directly (no git needed; `tests/fakes/code_fixture.py::write_commit_history` shows the row shapes) and assert every field in the CD6 criteria; no `datetime.now`/`utc_now` in the module (grep it in the test); a `%aI` with a non-UTC offset keeps its original text and offset; an edge to a foreign symbol refuses; `skipped`/`truncated`/shallow/disabled coverage; two runs with the same inputs produce identical IDs; a different `observed_at` changes observation IDs (that is B4's known property, owned by the coordinator; assert it so nobody is surprised); merge validation refuses a duplicate.

FILES:
  - own: NEW `src/hippo/knowledge/code_history.py`, NEW `tests/unit/test_code_history.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc7.md`.
  - do NOT touch: anything existing (`git_history.py`, `code_binding.py`, `model.py`, `lifecycle.py`; if a field or hook is missing, stop and ask with the exact name).

STEPS: worktree + venv (with the `mcp==2.1.1` pin); baseline `tests/unit/test_git_history.py tests/unit/test_code_binding.py tests/unit/test_managed_input_binding.py tests/unit/test_temporal_conflicts.py` green on Fake (correct names with `ls`); RED; implement; GREEN Fake; Ruff; evidence with the CD6 command result; commit in one or two commits. Pure module: no Ladybug or Neo4j run applies; say so. This slice fits one worker because it is one new pure module mirroring CC6's shapes over an input `read_history` already produces.

DONE WHEN: green with `-W error`; evidence written; `horch done` lists the public signatures, the record kinds and counts for the fixture, the locator kind chosen for commit messages, the `evidence_class` chosen, the rule-version constant, and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for contract questions (especially if CC6's evidence and this brief disagree about who renders commit views: ask, do not duplicate).
