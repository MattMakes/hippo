# Measure exact-content decoding on the captured corpus

Fresh Codex Sol worker; horch communication only, no Claude or nested agents. Root `/Users/mascott/projects/hippo`, branch `rag-it-all-tibs`, base `1ff6a37` plus recovered decoder change. Own only new scratch directories `/private/tmp/hippo-decode-measure-*` and sanitized `ai_docs/reports/2026-09-16-prose-decode-measurements.md`. Do not edit application source, tests, gates, plans or another report. No commits/push, no source ingestion, no Neo4j.

Goal: obtain isolated reproducible runtime and exact quality equivalence evidence for the prose decoder optimization. Done requires a completed Q3 replay, exact comparison with previous saved results, warm/cold proof profile, and a report of remaining bottlenecks. This is measurement, not design or an authorization-cache experiment.

Read `ai_docs/plans/2026-09-16-prose-decode-plan.md`, `ai_docs/reports/2026-09-16-proof-performance-results.md`, and scratch `/private/tmp/hippo-speed-round1-FfTJFE/{replay.py,compare.py,profile_proof.py}`. Previous Q3 baseline 415.103486958s, first optimization 298.610555000s; all304 main proof builds retained. Comparison artifacts are baseline/results.json and record-cache/results.json under that scratch directory.

Steps:
1. Check exact running processes (request sandbox escalation for ps if needed). There may be two G1/G2 checks finishing. Do NOT benchmark while pytest, another corpus replay/profile or another local model task runs. Do not kill anything. Confirm specific live handles, poll at sensible intervals <=30s, and start when idle. The user permits local Ollama requests to the existing service for this task, not reconfiguration/restart. Production services (8011 etc) may run idle; leave them alone.
2. Record UTC time, current HEAD, source diff/sha256, Python/backend/model settings, and a process snapshot in scratch. Create a unique fresh output directory. Run `.venv/bin/python /private/tmp/hippo-speed-round1-FfTJFE/replay.py --output NEW_DIRECTORY --ids Q3`, capture log and exit code. It copies the original corpus and must never be run over an existing output/data. Use require_escalated for local socket access if sandbox prevents it. Do not set a short application timeout. Poll the actual handle until terminal; an observation timeout is not failure and never justifies a duplicate run.
3. Compare the new answer object exactly (answer/thought/raw/context/citation IDs) and stable trace components (passages, seeds, paths, tests, history, evidence fingerprint) against the record-cache Q3 output, using the previous comparison script or an equivalent explicit check. List every difference; don't silently normalize material changes. Timing/ephemeral snapshot IDs are expected to differ. Report main-thread SQL/proof counts, HTTP/model timing and wall time.
4. Profile a single cold evidence build and then a warm build in the SAME store instance using a scratch adaptation of profile_proof.py, saving separate pstats. Keep full fresh storage reads and no code monkeypatch altering behavior. Record top20 cumulative/self costs, SQL count per build if practical, and store decoder cache entries/serialized-key bytes. Full query runs many warm builds; cold-only profiling would mislead.
5. If replay fails, inspect cause and report; do not patch application code or discard failure. If exact equality fails, preserve all artifacts for orchestrator decision. Never call a speedup a quality success just because it is faster.
6. Write sanitized report with exact command/artifact locations, results, confounders and limits. Do not include private question/source/answer text in the repo report; refer to Q3 and private artifacts. Ruff format --check report. Send DONE summary via horch tell orchestrator, record horch done, close normally.

The orchestrator will independently inspect artifacts before committing. Do not run all12 yet; quality fixes remain. Do not create another benchmark process if one is live. Do not modify or delete `/private/tmp/hippo-real-eval-YCrJeD`, production `data/`, original source files or model configuration.
