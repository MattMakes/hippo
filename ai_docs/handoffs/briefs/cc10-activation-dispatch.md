# Brief: CC10 — activation dispatch for code sources (managed code capture, task 10)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc10` (branch `wp/cc10`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC1–CC9b are merged).

GOAL: An authenticated actor's `add_repo`, `add_file` (code name), archive ingress, `reindex`, `reindex_all` and `delete_source` dispatch a `repo`, `archive` or code `file` source to the managed code lane; unsupported kinds and actorless callers stay legacy exactly as today; a managed or converting source never reaches `_read_history`, `_prepare_reindex`, `_clear_passages`, `delete_source`, `shutil.rmtree` on the source directory or any raw unlink; and git clone failures stop logging credentials. Gate CD8 (dispatch clauses) of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`; plan sections 1, 7 (per ruling 9), 10 and the CC10 row of section 12; rulings 8 and 9; design review m6, M10 (`ai_docs/reports/2026-09-12-code-capture-plan-review.md`); CC1's evidence finding 4 (the pipeline-level destructive-operation spies belong here). Committed on `wp/cc10`.

CONTEXT: `src/hippo/ingest/managed_activation.py` (`managed_eligibility` `:~111` — closed, row-only: `text` and plain-prose `file` are `eligible_legacy`, everything else `unsupported`; `plan_dispatch` `:~166`; `build_options` `:~219` returning `PlainBuildOptions`; `run_managed_build` `:~465` calling `build_plain_source` from the saved ingress file; `map_build_failure`/`record_build_failure`/`record_build_receipt`; `ManagedPreflightRefused` → `409 bulk_refused` per `ai_docs/plans/rag-it-all-task-5-production-activation.md`); `src/hippo/ingest/pipeline.py` (`add_repo` `:~158` — no `build_actor` today, stores `{"url": url}` and `repos.repo_name(url)` as the name; `start_indexing`/`run_indexing` `:~223-306` deciding the lane twice; `_run_managed_indexing` `:~337`; `_read_history` `:~454`; `read_source` `:~509` — `rmtree(checkout)` then `clone_repo` for a repo; `delete_source` `:~535` with `_tombstone`; `reindex_all` `:~604` with `_preflight_managed`; `reindex` `:~675`; `_prepare_reindex` `:~700`; `_clear_passages` `:~717`); `src/hippo/ingest/readers.py` (`is_code_name`, `is_plain_prose_name`, `CODE_EXTENSIONS`, `PROSE_EXTENSIONS`, `read_zip`); `src/hippo/ingest/repos.py` (`is_git_url` `:~43` accepting `https://user:token@host/...`, `repo_name` `:~49`, `clone_repo` `:~59`, `_explain_git_failure` `:~88-113` — logs the full URL and git's raw stderr at `:~116` and puts the URL in the `RepoError` message, `walk_repo` `:~115` logging absolute paths at `:~136`); CC9b's `build_code_source`, `CodeTreeInput`, `CodeBuildOptions`; CC4's `repository_descriptor(url)` (refuses an `http(s)` URL carrying userinfo); the existing suites `tests/unit/test_managed_pipeline_activation.py`, `test_ingest_pipeline.py`, `test_ingest_concurrency.py`, `test_ingest_repos.py`, `test_managed_route_activation.py`, `test_managed_web_ingress.py`.

PRIOR WORK (CC9b, merged at `bc7ea22`; read `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc9b.md` in full, its "Findings for CC10/CC11" bind you): the coordinator is `src/hippo/ingest/code_generation.py::build_code_source(ctx, *, source_id, actor, tree: CodeTreeInput, options: CodeBuildOptions, raw_store, embedding_spec, operation_id, should_stop, on_progress=None, embedding_cache=None) -> BuildReceipt` (status `created` | `already_current` | `resumed`; refusals `CodeBuildRefused(ValueError)`); it is HANDED A CHECKOUT via `CodeTreeInput(root, paths=None, kind="repo", repository=None, head_revision=None)`, so cloning into a per-operation directory, the archive extraction seam, and never `rmtree`-ing the existing checkout are yours; `CodeBuildOptions` separates input-affecting fields (chunk size/overlap, synonymy threshold, history depth, exclusions, allow_empty) from operational ones (limits, batch size, workers, git timeouts, lease intervals) and a test proves the latter never enter identity; `already_current` returns without inference. CC9b finding: the lease heartbeat can latch before a between-batch rebaseline; the fix is a hook in `src/hippo/ingest/build_run.py` (CC9a's file, finished) — you are granted ADDITIONS ONLY there if you take it, keeping `test_build_run.py` and `test_prose_generation.py` green; otherwise leave it named for CC11. Ruling 5 (repo prose via OpenIE) is DEFERRED by name: prose files inside a tree are ordinary passages with coverage `openie="skipped"`, reason `prose_extraction_deferred`; do not attempt the widening. Ruling 14: `context.legacy_lane()` decides the serving lane; a converting source serves only its untagged rows until publication. Then: ruling 9 — the managed flag flips at staging start, so `managed_eligibility`'s `managed` branch, `_tombstone`, `_prepare_reindex`'s guard and the untagged-row barrier already treat a converting source as managed; CC1 proved the three store-level refusals (`delete_passages_for_source`, `delete_code_nodes_for_source`, `delete_source`) and left the pipeline-level ones to you. CC4's `walk_tree`/`capture_repository_inputs` is the managed capture path; `walk_repo` stays a legacy reference. CC9b's tests built their own checkout; the clone, the checkout location and the `CodeTreeInput` construction are yours.

REQUIRED BEHAVIOR:
1. `managed_eligibility` returns `eligible_legacy` for `repo`, `archive` and a `file` whose stored name `is_code_name`, keeping `unsupported` for everything else (rich documents, samples); `plan_dispatch` is unchanged in shape, so an actorless caller still gets `legacy` for those kinds and a managed one still raises `ManagedActorRequired`. `run_managed_build` dispatches by kind: plain-prose `text`/`file` to `build_plain_source` as today; `repo`/`archive`/code `file` to `build_code_source` with `CodeBuildOptions` from a new `code_build_options(ctx)` (the same settings the legacy lane reads: chunk size/overlap, `code_history_depth`, `code_git_timeout_s`, `code_history_total_s`, clone depth, plus the operational limits) and a `CodeTreeInput` built from a checkout you own: for a `repo`, clone into a per-operation directory under `source_directory(ctx, source_id)` named by the operation id (never `rmtree` the existing checkout; the legacy `read_source` keeps its own behaviour for legacy sources), read the head SHA, pass `repository_descriptor(meta["url"])`; for an `archive`, extract into a per-operation directory through the existing zip budgets; for a code `file`, the saved ingress file. A managed refresh presents `stage` tokens through `present`/`_present_progress` as prose does.
2. `pipeline.add_repo(ctx, url, *, owner_id, access_role_id, build_actor=None, operation_id=None)`: `check_actor` before the Source row exists (like `add_file`); with an actor, `start_indexing(..., build_actor=...)` takes the managed lane. `add_file` and the archive ingress already thread `build_actor`; verify and extend where a code name now qualifies. `reindex`, `reindex_all` and `delete_source` need no signature change — prove they dispatch code sources by kind through `plan_dispatch`.
3. Destructive-operation spies (CD8, CC1 finding 4, ruling 8): for a managed or converting `repo`/`archive`/code `file` source, `_prepare_reindex`, `_clear_passages`, `delete_passages_for_source`, `delete_code_nodes_for_source`, `remove_orphans`, `store.delete_source`, `shutil.rmtree` on `source_dir(ctx, source_id)` or under the raw root, generation collection and raw unlink are NEVER called by `reindex`, `reindex_all`, `delete_source` or the index job — an actorless delete/reindex refuses (`ManagedActorRequired`), an actorless bulk reindex refuses the managed rows (`ManagedPreflightRefused`), and a delete with an actor tombstones. Assert with `monkeypatch` spies, not by inspecting state alone. A per-operation checkout's own cleanup, if you implement one, must be a named function the spies distinguish from the source-directory `rmtree`; say what you chose.
4. m6, `src/hippo/ingest/repos.py`: `_explain_git_failure` returns one closed reason sentence naming NO URL (the four `reason` clauses are the whole message); the `log.warning` names neither the URL nor git's raw stderr (log the closed reason and, at most, the URL's host with userinfo stripped); `walk_repo`'s "Skipping %s" warning logs the path relative to the root, not the absolute path. Keep `pipeline.add_repo`'s "does not look like a git URL" hint text. `is_git_url` stays as is; a credentialed URL is refused for the managed lane by `repository_descriptor` before any clone (CC4), and the legacy lane's clone of one never logs it. Adapt `tests/unit/test_ingest_repos.py` assertions that pinned the URL in the message or log (name each in the evidence); add a test that a `https://user:token@host/owner/repo` failure never puts `token` in the message, the log or the Source row.
5. Legacy stays legacy: an actorless `add_repo`/`add_file`/archive ingress runs the legacy body unchanged, including `_read_history` and the checkout `rmtree` in `read_source`; `tests/unit/test_ingest_pipeline.py` and `test_ingest_concurrency.py` pass unchanged.
6. Tests, NEW `tests/unit/test_managed_code_activation.py` (the file CD8 names) on Fake and Ladybug: eligibility table for every kind and name; dispatch by kind through `run_managed_build` (spy on `build_code_source` versus `build_plain_source`); `add_repo` with an actor converts through the managed lane with the legacy graph serving until publication (reuse CC1's `code_index` fixture approach); the spies in item 3 for delete, reindex and bulk reindex during conversion and after publication; the clone directory layout; m6's redaction; the closed failure mapping for a `CaptureRefused` and a `RepoError` (no path, URL or stderr on the Source row — `status._public_error` renders from the code).

FILES:
  - own: `src/hippo/ingest/managed_activation.py`, `src/hippo/ingest/pipeline.py`, `src/hippo/ingest/readers.py` (eligibility predicates only, if any is missing), `src/hippo/ingest/repos.py`, `tests/unit/test_ingest_repos.py` (adaptations named in the evidence), NEW `tests/unit/test_managed_code_activation.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc10.md`. Web/MCP routes that call `add_repo` (`src/hippo/web/routes/sources.py`, `mcp_server.py`, `cli.py`) may gain the `build_actor` pass-through ONLY if the plain-prose ingress already does it for `add_file`; mirror that exactly and name the sites, otherwise stop and ask.
  - do NOT touch: `src/hippo/ingest/code_generation.py`, `build_run.py`, `prose_generation.py`, `repo_capture.py`, `code_provenance.py`, `prepared_code_chunks.py`, `src/hippo/knowledge/*`, `src/hippo/store/*`, `src/hippo/codegraph/*`, `context.py`, `status.py`, `docs/`, the checkpoint, `GATES.md`.

STEPS: worktree + venv (with the `mcp==2.1.1` pin); baseline `tests/unit/test_managed_pipeline_activation.py tests/unit/test_ingest_pipeline.py tests/unit/test_ingest_concurrency.py tests/unit/test_ingest_repos.py tests/unit/test_managed_route_activation.py tests/unit/test_code_generation.py` green on Fake (form (b) where a listed file imports `fastapi.testclient` at module level); RED; implement; GREEN Fake on the baseline set plus the new file; GREEN Ladybug on `tests/unit/test_managed_code_activation.py tests/unit/test_managed_pipeline_activation.py -k "repo or archive or code"`; Ruff; evidence with the full CD8 command result; commit in two or three commits (eligibility and dispatch; pipeline and spies; repos redaction). Neo4j is root-owned. This slice fits one worker because the coordinator, capture and eligibility shapes exist; it is dispatch wiring, one clone/extract seam, spies and a redaction.

DONE WHEN: green on both backends with `-W error`; evidence written; `horch done` lists the eligibility table, the dispatch sites (file:line), the checkout layout, every adapted assertion, the redaction proof, counts and logs. The orchestrator then adds the destructive-operation line to CD2's CRITERIA (recorded in `ai_docs/handoffs/briefs/code-capture-notes.md`).

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for contract questions.
