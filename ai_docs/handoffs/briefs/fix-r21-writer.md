# Brief: close the CD10 review's writer blockers (R21-B1, B2, B3, B5)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `r21w` (branch `wp/r21w`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: `build_code_source` seals and publishes ordinary trees (a function longer than `chunk_size_chars`, C# overloads, a SQL table defined in one file and referenced from another), a failure after the seal is retried to publication instead of stranding the source, a resume never drops the second `CODE_EDGE` kind of one endpoint pair or a relation whose payload changed, and `_edges_touching` on LadybugDB and Neo4j returns every kind between a pair exactly as Fake does; the CD9 fixture exercises every one of those shapes. These are R21-B1, R21-B2, R21-B3 and R21-B5 of `ai_docs/reports/2026-09-13-code-capture-review.md` (all reproduced by the reviewer). Committed on `wp/r21w`.

CONTEXT: the review's BLOCKER table gives file:line, the reproduction and the exact fix for each; its appendix "The reproduction module" (copied at `/tmp/h21scratch/tests/unit/test_zz_review21_scratch.py`, read-only; copy the shapes, never import it) holds `MULTI`, the `t5` shape set (`main_guard`, `class_call`, `sql_rw`, `cs_overloads` via `tests.unit.test_code_binding.OVERLOADS`, `big_function` = 400 padded lines) and the `t1`/`t4`/`t6`/`t7` harnesses. Standing rulings in `ai_docs/handoffs/briefs/code-capture-notes.md`: ruling 10 (the resume probe asserts absence as well as presence, canonical payload equality, never `native_write`'s tolerant equality), ruling 12/13 (no knowledge->ingest import; the `code` generation profile), the Ladybug engine defect (`evidence-lbfix.md`: never `IN <list>` on a node string column, use `base.by_ids`), lbconn (recycle only at safe boundaries; you do not touch `store/ladybug.py`). Precedents: `evidence-cc8.md` (writer), `evidence-cc9b.md` (coordinator), `evidence-cc2.md` (`_edges_touching`'s three passes).

- R21-B1 — `knowledge/staged_code.py:353-362` keys `bindings` by `(native_kind, native_id)`, one `NativeBinding` per native row, while the bundle carries one per observing `(object, span)` (`NativeBinding` identity includes `span_id`, `knowledge/model.py:614`); `_inventory` (`:611-620`) then refuses the seal with `Exact NativeBinding inventory differs from prepared coverage`. Reproduced: `big_function` 24 bindings over 13 native rows, `cs_overloads` 18 over 15, `sql_rw` 22 over 21. Fix: key `bindings` by `(native_kind, native_id)` to a TUPLE of every binding, put all of them in the row's dependency group, probe each, and compare the full set at the seal.
- R21-B2 — `ingest/code_generation.py:739` (`_install` always calls `bind_generation_embedding_profile`) and `store/generations.py:493-494` (the `IndexManifest` refusal runs before the `verified_v1` early return at `:496-497`). Any failure after `_seal` (crash, cancellation, heartbeat failure, publication-window epoch refusal) leaves a `failed` generation holding its manifest; `_instant` adopts it, reclaim admits it, and every retry raises `A sealed profile cannot be rebound`; the generation ID is input-derived so the source is stuck until its tree changes. Fix (smallest): in `bind_generation_embedding_profile` move the manifest refusal after the `verified_v1` early return, keeping `validate_generation_profile`, so rebinding an identical sealed profile is a no-op; add the coordinator test that faults between `_seal` and `_publish` and asserts the retry publishes the SAME generation ID with `status == "resumed"`. (Apply the same fix to the prose lane only if it shares the store function; do not edit `prose_generation.py`, say whether it is affected.)
- R21-B3 — `knowledge/staged_code.py:217-226` keys `CODE_EDGE` relations by endpoints only, `:363-364` records `None` as the relation probe, `:531-535` and `:640-646` compare `(rel, a, b)` sets. Two kinds on one pair are ordinary (`CONTAINS`+`INVOKES` for a module-level `main()` call, `READS`+`WRITES` for a function that selects and updates one table); a crash between their two groups makes the probe skip both and the generation seals without `INVOKES` (reproduced, `resumed_from_batches` 92) or `WRITES` (88); a staged relation with a different `omega`, `provenance`, `extra` or hunk is also skipped. Fix: key `CODE_EDGE` by `(a, b, kind)` in `relations`, `_groups`, `_persisted` and `_inventory`; carry each relation's canonical payload in its probe and compare it in the probe and at the seal; add the two-kinds crash test (`t4`/`t7` shape).
- R21-B5 — `store/generations.py:857-863`: the non-Fake `_edges_touching` de-duplicates on `(a, b)` with a `seen` set, so LadybugDB and Neo4j drop the second `CODE_EDGE` kind between one pair from `_native_relationships`, the sealed native checksum, the projection and `status._with_code_edges`; Fake keeps both (`store/code.py:436-447`, `tests/fakes/fake_store.py:607` keep one per `(a, b, kind)`), so no Fake line can see it; reproduced on LadybugDB with a plain bootstrap. Fix: remove the `seen` set; in the `b`-driven pass skip rows whose `a` is already in `ids`; add a two-kinds oracle case to `test_generation_scoped_reads.py` and run it on LadybugDB.

REQUIRED BEHAVIOR:
1. Every reproduction above becomes a RED test first: `t5`'s three seal shapes in `tests/unit/test_staged_code_writer.py` (writer level) and `tests/unit/test_code_generation.py` (through `build_code_source`), `t1` in `test_code_generation.py`, `t4`/`t7` in `test_staged_code_writer.py` and `test_code_generation.py`, `t6` as the oracle case in `tests/unit/test_generation_scoped_reads.py` (RED on LadybugDB, green on Fake before the fix — record both).
2. Each fix is at the source named above; never widen a refusal into a skip or a tolerance. Ruling 10 holds afterwards: the probe compares every native row AND every relation against its canonical payload and asserts absence of unexpected rows; the seal's `_inventory` compares the full binding and relation sets.
3. Prose lane byte-identical: `tests/unit/test_staged_prose_writer.py`, `test_prose_generation.py`, `test_generation_profiles.py` unchanged and green; every existing checksum unchanged (the profile suite pins them).
4. The CD9 fixture `tests/fakes/code_capture_repo.py` GAINS (never loses) the breaking shapes so CD9 exercises them: one function longer than 1500 characters, one C# file with overloads, one SQL table defined in `db/schema.sql` and referenced from a Python file in another directory, one module-level call under `if __name__ == "__main__":`, one function that selects and updates the same table. Keep the fixture deterministic (its determinism test), keep the accepted-file count documented in the builder's docstring, and update `tests/unit/test_code_capture_acceptance.py` ONLY where a pinned count must move (name each in evidence; the acceptance file's default size and knobs are r21c's).
5. Backends: Fake for everything; LadybugDB for `tests/unit/test_generation_scoped_reads.py tests/unit/test_staged_code_writer.py tests/unit/test_code_generation.py -k "publish or resume or overload or two_kinds or big"` and the acceptance scenario at `HIPPO_CODE_ACCEPTANCE_FILES_PER_LANGUAGE=2` (one Ladybug run at a time on the machine). Neo4j is root-owned: name the changed `_edges_touching` query shape and the profile-bind shape for the parity run.
6. Also close, if it is a one-liner in a file you already touch, otherwise leave named in evidence: R21-m2 (SUBJECT/OBJECT second hop and `Missing shared graph endpoint` oracle cases, `generations.py:914-940`), R21-m3 (tripwire regex `(?i)\bIN\s+\$` over whole sources, `test_generation_scoped_reads.py:382-401`), R21-m6 (Fake `_edges_touching` iterates live dicts outside `_lock`, `generations.py:831-837`), R21-m10 (a direct predicate test for the all-principals suppression term, `generations.py:111-116`), R21-m31 (with B2 fixed, a code-lane publication-window epoch-change test asserting the retry publishes), R21-m14 (rewrite the checkout after capture and assert identical passages, test-only in `test_code_generation.py`). Leave named: R21-m1, m5, m19, m32, m33, m34, m38, m40, m41 (coordinator internals outside `_install`).

FILES:
  - own: `src/hippo/knowledge/staged_code.py`, `src/hippo/store/generations.py`, `src/hippo/ingest/code_generation.py` (the `_install` change only), `tests/fakes/fake_store.py` (only if the Fake must mirror a multi-kind edge shape; say so), `tests/fakes/code_capture_repo.py` (additions only, item 4), `tests/unit/test_staged_code_writer.py`, `tests/unit/test_code_generation.py`, `tests/unit/test_generation_scoped_reads.py`, `tests/unit/test_code_capture_acceptance.py` (pinned counts only, item 4), NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-r21w.md`.
  - do NOT touch: `src/hippo/hipporag/indexer.py`, `src/hippo/store/code.py` (r21i), `src/hippo/ingest/repo_capture.py`, `src/hippo/ingest/repos.py`, `src/hippo/ingest/pipeline.py`, `src/hippo/web/*`, `src/hippo/cli.py`, `.github/*` (r21c), `src/hippo/knowledge/access.py`, `src/hippo/knowledge/build_authority.py`, `tests/unit/test_build_authority.py`, `tests/unit/test_code_projection.py`, `tests/unit/test_knowledge_scoped_reads.py`, `tests/unit/test_query_scoped_reads.py` (r21a), `src/hippo/store/ladybug.py`, `src/hippo/status.py`, `src/hippo/context.py`, `src/hippo/ingest/prose_generation.py`, `GATES.md`, `docs/`, the plan, other evidence files.

STEPS: worktree + venv (`mcp==2.1.1` pin); baseline the CD7 and CD8 CHECK lines from `GATES.md` plus `test_generation_scoped_reads.py test_staged_prose_writer.py test_generation_profiles.py` on Fake; RED (item 1, logs `/tmp/hippo-r21w-red-*.log`); fix B1, B3, B5 in the writer/store, then B2; GREEN Fake on the baseline set + the acceptance scenario at N=2; GREEN LadybugDB per item 5 (`/tmp/hippo-r21w-ladybug-*.log`); Ruff check + format --check on every touched file and the evidence; evidence with the per-shape before/after (bindings over native rows, relation kinds stored vs prepared, retry outcome), the Neo4j shapes and the minors closed/left; two commits (writer+store, then coordinator+fixture).

DONE WHEN: every RED test is green on Fake, the B5 oracle is green on LadybugDB, the CD7/CD8 lines and the prose suites are green, the fixture carries the five shapes, evidence written; `horch done` lists the commits, per-finding outcome, counts and logs.

REPORT: `horch note` per finding; `horch tell orchestrator "[<role>] BLOCKED: ..."` if a fix needs a file above (name the exact function) or if the profile-bind change alters a prose checksum.
