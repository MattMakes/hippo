# Brief: fix the CC1 legacy-lane regression (staging-only sources leak)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc1fix` (branch `wp/cc1fix`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: The PA6 CHECK line of `ai_docs/gates/rag-it-all/task-5-production-activation/GATES.md` is green again at HEAD with zero adaptation of its six failing tests, and the CD2 line of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md` stays green, under this ruling: **the legacy lane serves only untagged rows (rows with no `generation_id`), and a `managed` source is presented in the legacy lane only when it has at least one untagged row.** A converting legacy source keeps its complete legacy graph and counts until publication (CD2); a source that was never legacy (bootstrap through managed ingress, staged rows only) stays invisible everywhere until publication, exactly as before CC1 (plan invariant 5, PA2, PA6). Committed on `wp/cc1fix`.

CONTEXT: CC1 (`74ebaa5`, `82e31c1`, `d74e482`; evidence `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc1.md`) added `GenerationQueries.source_serves_legacy(source_row)` (`src/hippo/store/generations.py:~62`) and moved the lane decision to `src/hippo/context.py` (`_build_managed_graph` legacy_ids `:~222`, selected `:~224-226`, proof mapping `:~255-262`, `_build_structural_graph` `:~345/:~352-353`) and `src/hippo/status.py` (`source_view` lanes `:~61-62`, `_legacy_source` `:~78`). Regression, reported by opus-17 at HEAD `c893a95` in a clean tree (`/tmp/hippo-pa8close-base-pa6.log`, 6 failed / 708 passed; isolated repro `/tmp/hippo-pa8close-regress-check.log`): a source whose only rows are STAGED, UNPUBLISHED managed rows now satisfies `source_serves_legacy`, lands in `legacy_ids`, and the legacy loader (`src/hippo/hipporag/graph_index.py::GraphIndex.load` `:~411-432`, which has no generation filter; design review B1) emits its unpublished native rows on the graph surfaces, and its name reaches eval labels. Failing tests: `tests/unit/test_graph_surface_access.py::test_unpublished_managed_native_rows_never_escape_graph_surfaces[/api/entities?q=orion | /api/graph/full | /graph]`, `::test_staged_generation_without_artifacts_is_not_a_source_dropdown_entry`, `::test_unpublished_managed_code_never_appears_in_symbol_lookup`, and `tests/unit/test_eval_access.py::test_generated_names_and_source_labels_come_from_visible_evidence` (label reads "Sample questions: SECRET source container"). Those six tests are the contract; CC1's adapted tests that assert the opposite for a generation-only source are wrong and are yours to re-adapt.

REQUIRED BEHAVIOR:
1. The legacy lane never loads or presents a generation-tagged row (`Passage`, `Symbol`, `DataObject`, `Commit` and their edges with a non-null `generation_id`), on both graph builders in `context.py` and in `status.py`'s legacy counting. Where the loader cannot filter, drop tagged rows after loading and say so; do NOT edit `src/hippo/store/*` or `tests/fakes/fake_store.py` (CC2 is live there: if you need a bounded "untagged rows of source" read, use the existing unscoped read and filter in Python, and name the read you would want in the evidence so CC2/CC3 can add it).
2. A `managed` source is in the legacy lane's presentation (inventory pair, dropdown entry, eval labels, status counts) only when it has at least one untagged row; with none it is absent from every surface until publication. Unmanaged legacy sources: byte-identical behaviour to `13efa40` (before CC1), including any empty-source presentation they have today.
3. `source_serves_legacy` keeps its signature and its three terms (no active pointer, no published `IndexEvent`, no all-principals `Suppression`); do not change it. The presence gate of item 2 lives beside the lane decision in `context.py`/`status.py`.
4. Re-adapt CC1's tests to this ruling: `tests/unit/test_managed_source_inventory.py::test_unpublished_generations_never_produce_a_pair[staging|failed]` (assert no pair AND absent from the legacy inventory when the source has no untagged rows; add the converting-source variant that has legacy rows and IS present with its legacy counts), `tests/unit/test_status_access.py::test_generation_only_source_keeps_the_legacy_lane_until_it_publishes` (rename back to a name that states the contract: hidden until it publishes when it has no legacy rows; add the converting variant), and whatever in `tests/unit/test_converting_source_serving.py` asserted presentation of a rows-less source. Every converting-source assertion (legacy rows present → served, counted once, no staged row visible, publication flips in one transaction, tombstoned mid-conversion → neither lane) must still pass.
5. New tests in `tests/unit/test_converting_source_serving.py`: a converting source with legacy rows AND staged native rows shows exactly its legacy rows on `/api/entities`, `/api/graph/full`, symbol lookup and the source dropdown; a staging-only managed source shows nothing on those same surfaces and no eval label.

FILES:
  - own: `src/hippo/context.py`, `src/hippo/status.py`, `tests/unit/test_converting_source_serving.py`, `tests/unit/test_managed_source_inventory.py`, `tests/unit/test_status_access.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc1fix.md`.
  - do NOT touch: `src/hippo/store/*`, `tests/fakes/fake_store.py` (CC2), `src/hippo/knowledge/*` (CC6), `src/hippo/ingest/*`, `src/hippo/web/*` (opus-17's PA8 closure batch), `tests/unit/test_graph_surface_access.py`, `tests/unit/test_eval_access.py` (the contract; must pass unmodified), `GATES.md`, `docs/`.

STEPS: worktree + venv (`mcp==2.1.1` pin); RED = the PA6 line at HEAD (copy it verbatim from the activation ledger; AnyIO form (b) is needed because its files import `fastapi.testclient` at module level) showing exactly the six failures; fix; GREEN on the PA6 line, the CD2 line (`HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_converting_source_serving.py tests/unit/test_managed_source_inventory.py tests/unit/test_status_access.py tests/unit/test_structural_loading.py -q -o addopts='' -W error`), PA2's line and `tests/unit/test_managed_web_surfaces.py`; Ladybug on `tests/unit/test_converting_source_serving.py` and `tests/unit/test_managed_source_inventory.py`; Ruff; evidence naming the ruling, each re-adapted test and why; one or two commits.

DONE WHEN: PA6 line green with the six contract tests unmodified; CD2 and PA2 lines green; Ladybug green; evidence written; `horch done` lists commits, counts, logs and the store read you would have wanted.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` if the fix cannot be made without a store change (name the exact read).
