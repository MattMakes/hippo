# Brief: independent SPEC/QUALITY review of activation Task 4b-i (web ingress actors, coded permission answers, source-route sessions)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`); the work is merged at HEAD `0bc378c` (branch `wp/pa4b1`; its own commits are `2377f7c`, `d27321f`, `c2c743a`, `ea39383`, `090cd38` plus merges of the integration branch). You read and run; you do not edit source or tests.

GOAL: Independent SPEC and QUALITY verdict on Task 4b-i before publication, with particular attention to the actor boundary (who can build, delete and bulk-reindex managed evidence through the web).

CONTEXT:
- Plan `ai_docs/plans/rag-it-all-task-5-production-activation.md`: "Build actors at every boundary" (web bullets), "Managed delete" HTTP paragraphs, "Reindex and mixed bulk behavior" (including the 2026-09-12 amendment on refusal shape), "Production query-session activation", "Safe transport failures". Gates PA1 (route half), PA6, PA7.
- Orchestrator brief: `ai_docs/handoffs/briefs/pa4b1-web-ingress-actors.md`; contracts and rulings in `ai_docs/handoffs/briefs/task4-notes.md`. Implementer evidence: `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa4b1.md`. Related reviews: `ai_docs/reports/2026-09-11-pa4b2-review.md` (finding 10 about `api.py`/`app.py`), `2026-09-11-pa4c-review.md` (F1, F6), `2026-09-11-pa3b-review.md` (finding 5 on the bulk route).
- Rulings the implementer followed: the four MCP rows and two HTTP rows of `test_query_session.py::test_model_failure_releases_graph_and_revocation_wins` assert the mapped bodies; the 409 handler is `{"error": "Permissions changed; repeat the query", "code": "authorization_changed"}`; one `code` key added to three assertions in 4b-ii's test files and `build_actor=ANY` in one status test; `test_web_base.py::test_unexpected_error_while_asking_is_shown_not_swallowed` adapted to the closed table; delete and bulk reindex pass `build_actor_of(principal)` after Task 3b merged.
- Diff: `git diff 957fc35..090cd38 -- src/hippo/web tests/unit/test_managed_web_ingress.py tests/unit/test_web_base.py tests/unit/test_web_auth.py tests/unit/test_query_session.py` (the branch merged the integration branch twice; restrict to those paths).

FILES:
  - own: `ai_docs/reports/2026-09-11-pa4b1-review.md`.
  - do NOT touch: anything else (workers are live in worktrees; the root tree may carry another worker's uncommitted edits; ignore them).

STEPS:
1. Run, each to `/tmp/hippo-pa4b1-review-<n>.log` with `echo EXIT $?`: (a) `HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_managed_web_ingress.py tests/unit/test_web_base.py tests/unit/test_web_auth.py tests/unit/test_web_security.py tests/unit/test_web_library_evals.py tests/unit/test_web_busy_pages.py tests/unit/test_query_session.py tests/unit/test_status_access.py tests/unit/test_managed_route_activation.py tests/unit/test_managed_pipeline_activation.py -q -o addopts='' -W error -W "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning"`; (b) `HIPPO_TEST_STORE=ladybug .venv/bin/pytest tests/unit/test_managed_web_ingress.py -q -o addopts='' -W error -W "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning"`.
2. SPEC review; PROVEN (test) / UNTESTED / VIOLATED (file:line): (a) pasted text and eligible upload routes call `require(..., "add_sources")`, pass `BuildActor.reader(principal)`, and never `trusted_local`; open and preview identities stay legacy or are refused per the plan table; unsupported inputs stay legacy; (b) delete and reindex use the `manageable_source` principal's reader actor; bulk keeps `edit_graph` and passes its actor; an actorless managed delete or bulk maps to the generic permission response; a tombstoned source's later lookup is the same 404 as any unavailable source; (c) the 409 body and every JSON failure body carry `code`; no `str(exc)` reaches a body except the protected legacy settings validators; the redaction injection test covers JSON, HTML, headers and logs; (d) each owned route holds exactly one structural session (or borrows the request's) through DTO/render/save; `reindex_all` in `routes/sources.py` holds its owner across the pipeline call and its post-operation validate; no preflight-close-reacquire; (e) no Request, token, cookie or ambient principal is captured in a job closure (trace the closure the routes hand to `Jobs`).
3. QUALITY review, with attention to: whether `build_actor_of(principal)` can produce an actor for a preview or open principal under any code path; the bulk route's handling of `ManagedActorRequired` versus the (still) silent `{"accepted": true}` on a refused preflight (the plan amendment now specifies `bulk_refused` 409; record that as an obligation of Task 4e, not a defect here); the `test_web_base` adaptation (does it still prove the error is shown, not swallowed?); duplicated failure-shape helpers between `app.py` and `render.py` (Task 4e dedupes; note the current state); the implementer's open finding (a) that a bare `ValueError` from `canonical_selected_generations` escapes graph-only routes (Task 4e fixes at the source; confirm the exposure is module constants only).
4. Report any changed public route signature or response shape.

DONE WHEN: `ai_docs/reports/2026-09-11-pa4b1-review.md` exists with `SPEC: PASS|FAIL`, `QUALITY: PASS|FAIL`, the a–e table, numbered findings with severity, file:line, why, proposed fix, run results with log paths. `horch done` states both verdicts, finding counts by severity, and the report path.

CONSTRAINTS: no edits outside your report; no Neo4j (the orchestrator holds the container); `HIPPO_TEST_STORE` explicit on every command.

REPORT: `horch note` after the runs, after the SPEC table, after the report.
