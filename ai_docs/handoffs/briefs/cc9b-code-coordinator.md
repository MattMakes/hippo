# Brief: CC9b — the code coordinator (managed code capture, task 9b)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc9b` (branch `wp/cc9b`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC1–CC8 and CC9a are merged).

GOAL: `build_code_source` converts (bootstrap), refreshes, resumes and fails a repository, archive or single-code-file source through detached preparation, durable fenced staging over many transactions, the resume probe, between-batch rebaselining and an atomic publication, returning a `BuildReceipt` that counts skipped batches and rebaselines. Plan sections 3, 5, 6, 7 (as rewritten by ruling 9), 8.2, 8.3 and 10 of `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`; rulings 2, 4, 5, 7, 9 and 10; design review B3, B4, B6 (dissolved), M2, M9, M11 (`ai_docs/reports/2026-09-12-code-capture-plan-review.md`); gates CD7 (the coordinator-visible resume clauses) and CD8 of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`. Committed on `wp/cc9b`.

CONTEXT: the template `src/hippo/ingest/prose_generation.py::build_plain_source` (`:~621-762`: validation, the run, capture, `bind_inputs`, `_generation`, `_prior_receipt` idempotence, `_materialize`, `_install` `:~471` — holder check, `recover_generation_builds`, `begin_managed_source`, `claim_generation_build`, the accepted-pair write under `generation_write`, the profile binding, the fresh-authority comparison — `_publish` `:~584`, the failure branch calling `fail_generation_build`); `src/hippo/ingest/build_run.py` (CC9a: `BuildRun`, `BuildReceipt` with `resumed_from_batches`/`rebaselines`, `credentials`, `authority_fields`); CC4 `src/hippo/ingest/repo_capture.py` (`capture_repository_inputs(root, *, raw_store, source_id, workspace_id, limits, configuration, observed_at, exclusions, provider_revision, repository, ...)`, `repository_descriptor(url)`, the reserved `capture` configuration key — its evidence items 9, 10 and 11 bind you: never pass `capture` yourself, size `CaptureLimits` for accepted PLUS excluded entries, and `walk_tree` is not cancellable), CC4 `code_provenance.read_code_provenance`; `src/hippo/codegraph/extract.py::extract_code(docs, source_id, node_namespace=..., syntax_cache=...)`, `syntax_cache.WALKER_RULES_VERSION`/`SCHEMA_VERSION`/`parser_profile`; `src/hippo/codegraph/git_history.py::read_history(..., node_namespace=...)`; CC5 `prepare_code_chunks` and `CODE_CHUNK_RULE_VERSION`; CC6 `materialize_code_evidence` and `CODE_BINDING_RULE_VERSION`; CC7 `bind_history`, `merge_code_bundles`, `CODE_HISTORY_RULE_VERSION`; CC8 `staged_code.write_staged_code`/`probe_staged_rows`/`_write_batches`/`_write_batch`/`_seal`, `BuildAuthority.rebaseline()`, and `_source_control` now admitting `repo`/`archive`; CC3 `reclaim_generation_build`; `src/hippo/knowledge/lifecycle.py::generation_for_inputs(..., configuration, created_at)`; `src/hippo/knowledge/generation_profiles.py` / `ingest/prose_generation._configuration` for how the embedding profile enters configuration; ruling 5: prose files inside the tree (`readers.PROSE_EXTENSIONS`) go through the shared prose preparation under the plain lane's budgets (reuse `prepare_plain_prose` pieces where they are pure; if a seam must be widened, stop and ask).

PRIOR WORK: CC1 — the managed flag flips at staging start through `record_mutation`; serving lane is `source_serves_legacy`; B6 is DISSOLVED: there is NO publish-time authorization-epoch bump for the code lane, so `_publish`'s "epochs unchanged" assertion is the right shape as-is and you must NOT add `+ int(not source_is_managed)` arithmetic anywhere except the install window exactly as prose does. CC3/CC8 — resume is the store's own rows via `probe_staged_rows`; CC8's evidence names the fail-closed remediation text. Read every evidence file `evidence-cc1.md` … `evidence-cc9a.md` before writing.

ORCHESTRATOR RULINGS 11–13 (2026-09-12, `code-capture-notes.md` items 5b and 5d): the accepted configuration carries the `code` generation profile key CC8 named (read `evidence-cc8.md`), beside the rule versions of CC5/CC6 (folded by `code_generation` under `code_derivation`), CC7's `CODE_HISTORY_RULE_VERSION` which YOU set under CC7's exported `HISTORY_CONFIGURATION_KEY` BEFORE calling `code_generation` (so it is hashed; `merge_code_bundles` refuses a generation whose accepted configuration lacks it), and the walker/grammar versions; from CC7's evidence (`evidence-cc7.md`, binding): pass the FOLDED configuration to `capture_repository_inputs`; subtract CC6 finding 10's unbound-symbol complement from `History.modifies` before `bind_history` (it REFUSES a MODIFIES edge whose symbol has no native row) and record the subtraction in coverage; union your coverage into the merged `coverage_json` rather than overwrite it; obtain the shallow boundary yourself (nothing produces it today: you are granted `src/hippo/ingest/git_history.py` ADDITIONS ONLY to surface `shallow` and `renames` on `History`, keeping `test_git_history.py` green) and pass it as `shallow_boundary`, never `None` for a real shallow clone; `bind_history` requires `observed_at == code_bundle.generation.created_at`; `readers.PROSE_EXTENSIONS` is the sole prose test and an extensionless README is an unparsed `window` file with `openie=skipped`, reason `unparsed`, in coverage; CC5's parity latch stays; the shared chunk/capture dataclasses stay in `ingest` unless you relocate them into `knowledge/inputs.py` following the layering precedent (record either way).

REQUIRED BEHAVIOR:
1. NEW `src/hippo/ingest/code_generation.py` with the plan section 3 signature: `build_code_source(ctx, *, source_id, actor, tree: CodeTreeInput, options: CodeBuildOptions, raw_store, embedding_spec, operation_id, should_stop, on_progress=None, embedding_cache=None) -> BuildReceipt`. `CodeTreeInput` (frozen: absolute checkout root, ordered normalized relative paths or `None` for "walk", source kind `repo`/`archive`/`file`, `RepositoryDescriptor | None`, head SHA or `None`) and `CodeBuildOptions` (frozen; input-affecting group: chunk size/overlap, synonymy threshold, history depth, walker/grammar profile, exclusion and empty policy; operational group: batch size, checkpoint interval, lease duration and renewal interval, worker count, the section 8.3 ceilings, the 64 MiB per-batch payload ceiling). Only the first group plus the four rule versions (CC5, CC6, CC7, CC8's writer version if it exports one) plus `WALKER_RULES_VERSION`, the per-grammar `parser_profile` tuples, `SCHEMA_VERSION` and the embedding profile enter the `configuration` hashed by `generation_for_inputs` (ruling 10, plan section 5); a test proves worker count, clone depth, progress and the capture instant do not.
2. Bootstrap versus refresh is decided from `source_control.active_generation_id is None`, NOT from `source_control.managed` (B2 shape (b) cost: a resumed bootstrap has `managed=True` and no pointer). Do NOT copy `prose_generation.py:~656-658`'s "Managed source needs explicit recovery before initial publication" check. Bootstrap: durable staging over many transactions (not prose's single detached commit): install (holder check, `recover_generation_builds(source_id=...)`, `put_knowledge(gen)` if absent, claim or reclaim, accepted pairs under `generation_write`, profile binding), then batches with `run.check()` between them, seal, then publication in one short transaction under the source lock (`publish_staged_generation(expected_parent_id=None)`, source presentation and counts, the fresh-authority comparison expecting `active_generation_id` to differ and NOTHING else). Refresh: claim before any inference, G1 stays active throughout, `expected_parent_id=G1`; failure or cancellation leaves G1 active, the source `status="ready"` with a `refresh_failed`/`refresh_cancelled` stage exactly as `managed_activation` presents for prose.
3. Capture instant (B4): compute the accepted manifest and the generation identity FIRST (they are instant-independent: CC4 carries `observed_at` without hashing it); then resolve the instant ONCE: if a reclaimable generation with that ID exists (`staging`/`failed`, never published), adopt its stored `Generation.created_at`; otherwise take one `store._now()`. Thread that instant through every revision, observation and view (CC6/CC7 take it as input) and persist it as `created_at` on the generation you install. A test proves a crash-then-resume reproduces every `ObjectObservation` ID byte-identically and reports a nonzero `resumed_from_batches`.
4. Resume (plan 8.2, ruling 10): after reclaim, call CC8's `probe_staged_rows`; skip groups it marks present; fail closed with its remediation on any conflict, partial group or unexpected generation-scoped row; `BuildReceipt.resumed_from_batches` = skipped groups. A retry with different inputs is a different generation and supersedes (claim, not reclaim). `operation_id` idempotence and `already_current` (accepted manifest equal to the active generation's) return without inference or writes, as prose does.
5. Long-build authority (ruling 2): between batches only, when `run.check()` raises `AuthorizationChanged` for an epoch mismatch, call `guard.rebaseline()` once, `run.adopt` the child and increment `BuildReceipt.rebaselines`; any other failure, a suppression change, a `SourceControl` change or a lost capability aborts the build (`fail_generation_build` under the held fence, retaining the staged inventory). Never inside `generation_write`, never after a sticky failure.
6. Ceilings and refusals (plan 8.3, section 10): `CODE_MAX_FILES`, `CODE_MAX_SYMBOLS_PER_SOURCE`, `CODE_MAX_FILE_BYTES` (the parse rail; `parsable=False` units are captured, not excluded — CC4 evidence item 2), `MAX_CHUNKS`, the raw/decoded budgets and the per-batch payload ceiling refuse BEFORE capture or before the batch, never truncate, never install a partial generation as authoritative. Cancellation is cooperative between batches and before each model call and leaves a resumable staging generation. Privacy: no absolute path, URL, source text, diff hunk, commit message, prompt or exception text in logs, Source rows, receipts or errors; failures map through `managed_activation`'s closed mapper and `knowledge/public_errors.py`.
7. History (plan section 9): call `read_history(checkout, code.symbols, source_id, depth=..., node_namespace=generation_namespace(gen), ...)` against the captured checkout in preparation step 5 and feed CC7; the legacy `_read_history`/`_write_code_graph` path is never used for a managed source. `history_depth == 0` records `history: "disabled"` in coverage. Coverage (`coverage_json`) carries CC4's exclusion reasons, CC7's history coverage, `openie: "skipped"` per code/config file and `CodeGraph.truncated`.
8. Tests, NEW `tests/unit/test_code_generation.py` on Fake and Ladybug, with a small real checkout fixture (`git init` in `tmp_path`, two or three commits, a few files across two walker languages plus a README): bootstrap keeps the legacy graph serving until publication (assert through `ctx.graph_for`/`search` as CC1 does) and flips atomically; refresh keeps G1 selected through every batch and after a fault injected mid-batch; crash mid-batch then resume the same generation with the skipped-group count; a changed derivation version is a new generation; identity participation/non-participation; `already_current`; operation-id replay; rebaseline across an unrelated role change and abort on a removed capability; cancellation leaves a resumable generation; every spy in CD8's list (`_clear_passages`, `delete_passages_for_source`, `delete_code_nodes_for_source`, `remove_orphans`, `store.delete_source`, `shutil.rmtree`, generation collection, raw unlink) is never called; no log record or Source row carries a path, URL or text; Ladybug close/reopen mid-staging then resume.

FILES:
  - own: NEW `src/hippo/ingest/code_generation.py`, NEW `tests/unit/test_code_generation.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc9b.md`.
  - do NOT touch: `src/hippo/ingest/prose_generation.py` and `build_run.py` (CC9a; if `BuildRun` needs a code-specific hook, stop and ask with the exact name), `managed_activation.py`, `pipeline.py`, `readers.py`, `repos.py` (CC10's: dispatch, cloning and eligibility are NOT yours — your tests build the checkout and `CodeTreeInput` directly), `src/hippo/knowledge/*`, `src/hippo/store/*`, `src/hippo/codegraph/*`, `docs/`, the checkpoint, `GATES.md`.

STEPS: worktree + venv (with the `mcp==2.1.1` pin); baseline `tests/unit/test_prose_generation.py tests/unit/test_staged_code_writer.py tests/unit/test_generation_resume.py tests/unit/test_build_run.py tests/unit/test_converting_source_serving.py` green on Fake; RED; implement; GREEN Fake on the baseline set plus the new file; GREEN Ladybug on `tests/unit/test_code_generation.py tests/unit/test_converting_source_serving.py`; Ruff; evidence with the CD8 command result restricted to the files that exist (`test_code_generation.py test_ingest_pipeline.py test_ingest_concurrency.py test_managed_pipeline_activation.py test_prose_generation.py`; `test_managed_code_activation.py` is CC10's, say so); commit in two or three commits (options and identity; bootstrap/refresh; resume and rebaseline). Neo4j is root-owned: name the query shapes. This slice fits one worker because every preparation, writing, authority and store primitive is already built and tested; the coordinator is composition plus its test fixture, the analogue of `build_plain_source`'s 140 lines rather than of the whole prose module.

DONE WHEN: green on both backends with `-W error`; evidence written; `horch done` lists the three public types' fields, what enters identity and what does not, the bootstrap/refresh/resume paths in one line each, counts and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for contract questions.
