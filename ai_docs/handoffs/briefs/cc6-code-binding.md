# Brief: CC6 — code evidence and object binding (managed code capture, task 6)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc6` (branch `wp/cc6`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC4 and CC5 are merged).

GOAL: A pure materialisation of the plan's section 4 inventory for one code generation: repository, file and symbol objects with their observations, original spans for every file line range the chunks cite, rendered views for every generated segment, the namespaced native Symbol/DataObject rows and their bindings, and the exact `GenerationEvidenceMember` closure, from CC4's capture and CC5's chunks. No store, no model, no clock (the capture instant is an input). Gate CD5 of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`; plan sections 4, 5, 6 and rulings 1, 4, 7, 10. Committed on `wp/cc6`.

CONTEXT: the prose precedent `src/hippo/knowledge/input_binding.py` (`materialize_chunk_evidence`, `_view` for generated text as `RetrievalView`+`DerivedRecord` over original spans, the extraction input/support bindings, the "one plain unit, consistent generations, linear ID lookups" final validation); `src/hippo/knowledge/lifecycle.py` (`generation_for_inputs`, `generation_passage_id`); `src/hippo/knowledge/model.py` (`KnowledgeObject` kinds `repository`/`file`/`symbol`/`commit`, `ObjectObservation` as a `TemporalRecord`, `NativeBinding`, `Artifact` kinds `repository`/`history_event`); the namespace rules from the Task 5 namespace increment (`src/hippo/codegraph/*`, `store/generations.py::_validate_managed_native` which re-derives namespaced Symbol/DataObject/Commit IDs); CC4's `evidence-cc4.md` and CC5's `evidence-cc5.md` for the exact input shapes; the design review `ai_docs/reports/2026-09-12-code-capture-plan-review.md` (its m5 asks for an `evidence_class` on the section 9 input; question 7 on identity).

PRIOR WORK (CC5, merged at `b516232`; read `evidence-cc5.md` in full, its twelve findings bind you): the real input is `prepare_code_chunks(units: Iterable[CapturedCode], *, facts: CodeGraph | None, settings: CodeChunkSettings, max_chunks: int) -> PreparedCodeChunks(rule_version, chunker_profile, chunks, refusals)`; `PreparedCodeChunk` kinds are `header`, `symbol`, `data_object`, `window`, `prose`, `commit` (there is NO data-object mention passage; a chunk carries `data_object_ids` reached through `defines`); generated segments carry rule ids `code-placeholder-v1` (header placeholders), `code-line-join-v1` (row joins that are not the file's own newline) and `code-commit-v1` (a `CommitSegment` with NO original span, a third segment type beside `OriginalSegment`/`GeneratedSegment`); a commit passage may be empty and carry no segment; `extraction_segments` is absent and `title_dependencies` is empty for every code kind; `requires_view` is almost always true because the complete-line closure keeps the dropped newline; ordinals are part of `chunk_key`, so units arrive in CC4's canonical order; `CODE_CHUNK_RULE_VERSION = 'code-chunks-v1'`.
ORCHESTRATOR RULINGS FOR CC6 (2026-09-12): (a) CC6 materialises spans, views and derived records ONLY for chunks and segments that have original spans (header, symbol, data_object, window, and prose via the prose precedent); `commit` chunks and every `CommitSegment` are passed through UNBOUND on the bundle (a `commit_chunks` tuple in chunk order) because CC7 owns every commit-side record (history_event artifacts/revisions, commit objects/observations, commit views, MODIFIES/PRECEDES) and no `history_event` revision exists yet for a commit view to bind to; do not invent a commit view. (b) "mention" views do not exist; drop that word. (c) Repository, file and symbol objects and observations are yours; commit objects are CC7's.

REQUIRED BEHAVIOR:
1. `src/hippo/knowledge/code_binding.py`: `materialize_code_evidence(capture, chunks, facts, *, workspace_id, source_id, generation_identity_inputs, observed_at) -> CodeEvidenceBundle` producing, deterministically and in canonical order: the repository `Artifact` (identity from the full normalized clone path per ruling 7/M8) and its revision (commit SHA as `provider_revision`), one file `Artifact`/`ArtifactRevision` per accepted file (accepted hash, raw URI), `KnowledgeObject`s for repository, files and symbols with `ObjectObservation`s carrying `evidence_class` (declared/observed/inferred as m5 asks), `EvidenceSpan`s for every original line range the non-commit chunks cite (real complete-line locators from CC4), `RetrievalView`+`DerivedRecord` for every non-commit chunk with a generated segment or `requires_view` (header placeholders, line joins) bound to their original spans, namespaced native `Symbol`/`DataObject` rows plus `NativeBinding`s, the unbound `commit_chunks` pass-through, and the exact member closure over what CC6 produced. No native relation rows (CC7/CC8 write relations from the resolver output) and no model output.
2. Rule versions: the module exports `CODE_BINDING_RULE_VERSION`; the bundle carries it and CC5's `CODE_CHUNK_RULE_VERSION`; both are folded into the configuration passed to `generation_for_inputs` (ruling 10), and the bundle's `generation_id` is computed from that configuration so two captures with a different rule version are different generations.
3. Final validation mirrors the prose materializer: revision coverage and uniqueness, duplicate object/span inventory refused, one generation per bundle, linear ID lookups, and every cited original byte covered by exactly one span; a bundle whose chunks reference a file the capture excluded is refused; a `PreparedCodeChunks` whose `rule_version` differs from the imported `CODE_CHUNK_RULE_VERSION` is refused.

FILES:
  - own: NEW `src/hippo/knowledge/code_binding.py`, NEW `tests/unit/test_code_binding.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc6.md`.
  - do NOT touch: anything existing; if `lifecycle.py` or `model.py` needs a field, stop and ask with the exact name.

STEPS: worktree + venv; baseline `tests/unit/test_managed_input_binding.py tests/unit/test_prepared_code_chunks.py tests/unit/test_repo_capture.py tests/unit/test_knowledge_contracts.py` green; RED; implement; GREEN Fake; Ruff; evidence with the CD5 command result; commit in one or two commits. Pure module: no Ladybug needed; say so.

DONE WHEN: green with `-W error`; evidence written; `horch done` lists the public signature, the bundle's record kinds and counts for the fixture, the rule-version constants, and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for contract questions.
