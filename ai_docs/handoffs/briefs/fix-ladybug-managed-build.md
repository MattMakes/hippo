# Brief: fix the Ladybug-only managed-build regression at the merged base

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `lbfix` (branch `wp/lbfix`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: `HIPPO_TEST_STORE=ladybug .venv/bin/pytest tests/unit/test_managed_source_lifecycle.py tests/unit/test_managed_pipeline_activation.py -q -o addopts='' -W error` is green again (it is 5 failed / 135 passed at `013317f` and later), with the root cause named, bisected to the merge that introduced it, fixed at its source, and pinned by a test that fails on Ladybug before the fix. Fake stays green on the same files. Committed on `wp/lbfix`.

CONTEXT: opus-17 found it while running the PA8 closure batch's Ladybug line, in a clean tree at `013317f` with its own files checked out from the base (`/tmp/hippo-pa8close-ladybug-2.log`, `/tmp/hippo-pa8close-ladybug-basecheck.log`). Failing: `test_the_raw_root_and_embedding_cache_appear_only_for_a_managed_build`, `test_managed_add_refresh_and_conversion_never_destroy_source_wide_evidence`, `test_a_mixed_bulk_refreshes_managed_evidence_and_converts_without_a_legacy_clear`, `test_one_lane_failing_asynchronously_leaves_every_other_source_intact`, `test_a_ladybug_reopen_preserves_pointers_manifests_raw_references_and_the_tombstone`. Every one has the same shape: the managed build does not publish; `managed_activation.py:~440/445` logs "Managed build did not publish ... code=operation_failed exception=ValueError" (five), one `UnicodeDecodeError` and one `OllamaError`, and the row lands on `stage=refresh_failed`. Fake is green on the same files (PA1/PA3/PA5 pass). The activation ledger's PA7 line names these files on Ladybug, so PA7 cannot be recorded until this is fixed.

Suspects, in order (the four merges since the last known-green Ladybug run of these files at `1e2f112`): CC2 `c461c9c` (scoped readers `_native_rows(kind, *, ids=, generation_id=, source_id=, untagged=)`, `_knowledge_rows(name, *, generation_id=, where=)`, `_knowledge_get` by `where={id}`, `_edges_touching`, schema v6 journaled indexes with an EMPTY Ladybug step list, `validate_physical_schema` version >= 6 block — Ladybug's `IS NULL` and parameter handling were probed by CC2 but not on this pipeline path); cc1fix `d56b618` (`context.legacy_lane()` calling `_native_rows(kind, source_id=..., untagged=True)` per kind for an unpublished source with a generation); CC3 `8f32ec4` (`_admit_build`, tightened never-published triple; its own file passed on Ladybug); CC1 `7a0c719`. A `UnicodeDecodeError` from the store layer usually means a bytes column read through a text path (or the reverse) in a new query shape; a `ValueError` inside the publish path may be `validate_generation_profile`/`generation_checksums` seeing rows a scoped read no longer returns on Ladybug. Get the real tracebacks first (`--tb=long`, `-o log_cli=true -o log_cli_level=WARNING`, or catch the exception in a scratch run) before theorising.

REQUIRED BEHAVIOR:
1. Bisect: run the two files on Ladybug at `1e2f112`, `7a0c719`, `c461c9c`, `d56b618`, `8f32ec4` (`git worktree add` throwaway trees or `git checkout` inside your worktree; each run is a few minutes) and name the first red merge and the first failing test in your evidence.
2. Fix at the source. You own `src/hippo/store/*` and `tests/fakes/fake_store.py` EXCEPT the evidence-membership check inside `generation_checksums` (CC8 holds that grant; if the defect is there, stop and tell the orchestrator), plus `src/hippo/context.py` and `src/hippo/status.py`. If the defect is in `src/hippo/ingest/*` or `src/hippo/knowledge/*`, stop and report with the traceback and the exact line; do not edit them.
3. Pin: a test in the owning slice's test file (e.g. `tests/unit/test_generation_scoped_reads.py` or `tests/unit/test_converting_source_serving.py`) that reproduces the failing shape on Ladybug and passes after the fix; the five named tests must pass unmodified.
4. Re-run on Ladybug: the two files above, plus `tests/unit/test_generation_scoped_reads.py tests/unit/test_generation_resume.py tests/unit/test_converting_source_serving.py`; on Fake: the same set plus the CD1 and CD2 lines from `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`.

FILES:
  - own: as in item 2; NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-lbfix.md`.
  - do NOT touch: `src/hippo/ingest/*` (opus-17 holds `managed_activation.py` STAGES; CC9b not spawned), `src/hippo/knowledge/*` (CC8 live on `staged_code.py`, `build_authority.py`, `generation_profiles.py`), `src/hippo/web/*` (opus-17), `GATES.md`, `docs/`.

STEPS: worktree + venv (`mcp==2.1.1` pin); reproduce with tracebacks; bisect; RED test; fix; GREEN both backends; Ruff; evidence naming the merge, the cause, the fix and the logs; one or two commits.

DONE WHEN: the Ladybug line is green with the five tests unmodified; Fake green; evidence written; `horch done` names the introducing merge, the root cause in one sentence, the files changed, counts and logs.

REPORT: `horch note` after the bisect and after the fix; `horch tell orchestrator "[<role>] BLOCKED: ..."` if the defect lives in a file you may not edit.
