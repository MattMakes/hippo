# Brief: CC8 — staged code writer and long-build authority (managed code capture, task 8)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc8` (branch `wp/cc8`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC1, CC2, CC3, CC4, CC5, CC6 and CC7 are merged).

GOAL: The staged code writer writes one code generation in fenced dependency-complete batches, probes the store's own rows to resume a reclaimed generation without duplicating or trusting a stale row, seals an exact inventory into an `IndexManifest`, and `BuildAuthority` gains a bounded `rebaseline()` so a long repository build survives unrelated authorization changes without ever widening its audience. Also lifts `build_authority._source_control`'s `{text, file}` kind allow-list (review B3, first half) since this file is yours. Gate CD7 (and the `rebaseline` clauses of CD8) of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`; plan sections 4, 8.2, 8.3 and 10 of `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`; rulings 2, 9 and 10; design review B3, B4, M2, M6, M7 and question 4 (`ai_docs/reports/2026-09-12-code-capture-plan-review.md`). Committed on `wp/cc8`.

CONTEXT: the writer template `src/hippo/knowledge/staged_prose.py` (`_epochs`, `_local`, `_accepted`, `_write_batches` as complete dependency groups, `_write_batch` callback-free, `_inventory` — which requires the native inventory to be EMPTY, the reason the code lane needs its own writer — `_seal`, `write_staged_prose` refusing an ambient transaction) and its suite `tests/unit/test_staged_prose_writer.py`; `src/hippo/knowledge/build_authority.py` (`SourceControl`, `_source_control` at `~:62-95` with the kind allow-list at `:64` and the Artifact/Generation presence scan at `:69-78`, `BuildAuthority.check_local` at `~:259` which refuses on epoch mismatch FIRST, `bind_inputs` at `~:317` — the child-authority shape `rebaseline` must copy, `capture_build_authority` at `~:340`) and `tests/unit/test_build_authority.py`; the store seams: `generation_write`, `_check_build`, `seal_generation` (tolerates a byte-identical existing manifest), `native_write`'s tolerant equality (`generations.py`, the `boost`/`community`/`entities_json`/`triples_json`/`extraction_error` skip — question 4 says the probe must NOT rely on it), `_canonical_native`, `publish_staged_generation`'s reachable-closure suppression check, CC2's scoped reads (`_native_rows(kind, ids=..., generation_id=...)`, `_native_relationships(...)`, `_knowledge_rows(kind, generation_id=...)`), CC3's `reclaim_generation_build`; the native writers `src/hippo/store/code.py` (`add_symbols`, `add_commits`, `add_code_edges`, `link_definitions`, `add_modifies`, `add_precedes`); `src/hippo/hipporag/preparation.py::prepare_code_rows` for the row/vector shapes and `indexer.module_communities` (the community label is computed before the seal, never `set_symbol_communities` after it).

PRIOR WORK: CC6's `CodeEvidenceBundle` and CC7's merged bundle (read `evidence-cc6.md` and `evidence-cc7.md` for the exact input type and record kinds) are the writer's prepared input; both carry rule versions already folded into the generation configuration (ruling 10). CC3's reclaim returns a job whose credentials `generation_write` accepts; a reclaimed generation still holds its earlier rows. CC1: the managed flag flips at staging start; no publish-time epoch bump exists (B6 is dissolved; do not account for one). CC2: every read you add must be scoped (`ids=` or `generation_id=`), never whole-table.

REQUIRED BEHAVIOR:
1. `src/hippo/knowledge/staged_code.py` mirroring `staged_prose.py`: pure `_write_batches(prepared, *, batch_size)` yielding complete dependency groups in the plan's order (accepted preflight → revision members → repository/file/symbol/commit objects → original spans → derived views → dense passages → native code rows and bindings → native relations → history), a relation group emitted only after both endpoints; callback-free `_write_batch(store, prepared, batch, **authority)` under `generation_write` with the epoch re-validation `staged_prose._local` performs; `_inventory` as the INVERSE of the prose one (exact `Symbol`/`DataObject`/`Commit` rows, `NativeBinding`s, `ObjectObservation`s and native relations for this generation, and NO row of another generation, via scoped reads); `_seal` writing an `IndexManifest` with `("evidence", "dense", "native")` through `seal_generation`; public `write_staged_code(...)` refusing an ambient transaction. A per-batch canonical payload ceiling (64 MiB, plan 8.3) refuses before the batch's transaction opens. No callback, model call or filesystem access inside a transaction.
2. Resume probe (plan 8.2, review B4/M2/question 4, ruling 10): before writing, `probe_staged_rows(store, prepared) -> ResumePlan` reads, with one scoped read per kind, every row this generation already holds and decides per batch group: SKIP when EVERY row of the group is present with a canonical payload equal to what this attempt would write (`_canonical_native` for native rows, record equality for knowledge rows — never a sample, never `native_write`'s tolerant equality); WRITE when none is present; FAIL CLOSED (`ValueError` naming the remediation "explicit failed-generation cleanup") when a group is partially present, when any present row's payload differs, or when the store holds ANY generation-scoped row whose ID this attempt would not produce (the absence assertion). The plan reports `skipped_groups` for `BuildReceipt.resumed_from_batches`. Because the coordinator adopts the persisted capture instant (B4, `Generation.created_at`, owned by CC9b), a resume with the adopted instant must reproduce every `ObjectObservation` ID byte-identically; a test proves that with a fresh instant the revisions' `observed_at` payload difference is detected as a conflict, not written as a duplicate.
3. `BuildAuthority.rebaseline() -> BuildAuthority` (ruling 2 as amended by M6/M7): under `store.transaction()` with the authorization lock and the source lock (copy `bind_inputs`'s shape), refuse if latched, closed or after any sticky failure; refuse if `store.in_ambient_transaction()` is true on entry (never inside `generation_write`); require `store.suppression_epoch() == expected_suppression_epoch` — the suppression epoch is FROZEN at capture and any change refuses (`publish_staged_generation` would refuse at the end anyway, so continuing is waste; this subsumes M7's closure question, say so in the docstring); require `_source_control(store, source_id) == self.source_control` — `source_control` is frozen at capture and never adopted (a changed `access_role_id`/`min_rank`/`owner_id` refuses even though the actor kept every capability); construct a child `BuildAuthority` with the ORIGINAL actor, accepted inputs and `source_control` and the CURRENT authorization epoch; `child.check_local()` must pass in full (actor still holds `core.build` and every accepted revision/span); on success return the child (the coordinator `adopt`s it and counts the rebaseline); on failure latch the failure on self and close the child. `check_local()` itself is unchanged.
4. B3 first half: `_source_control` admits `repo` and `archive` beside `text` and `file`, keeping the identical denial text for everything else. Replace its `managed` computation with the row's `managed` flag or `active_generation_id` alone (drop the `meta["managed"]` term — CC1 recorded that nothing writes it — and the whole-table Artifact/Generation scan, which `check_local` re-ran on every batch); prove with the existing suite that no test depended on the presence scan (if one did, it pinned the classification CC1 retired; adapt it and name it in the evidence).
4b. Code generation profile (ruling 13, `code-capture-notes.md` item 5d; a code-proven gap CC6 found): `knowledge/generation_profiles.py::validate_generation_profile` admits only the plain-prose member set (one `manifest` artifact plus one `file` artifact per accepted input, refusing anything else with "Accepted original revision inventory differs"), and `store/generations.py::generation_checksums` refuses "Evidence outside raw manifest" for a span on any other revision. Both are called at seal/checksum time and from `dense_session.py:~115` at query time, so a code generation cannot seal or be queried today. Add a `code` profile, selected by a closed key in the accepted configuration that CC9b sets (name it; refuse an unknown profile name), whose member set is exactly one `manifest`, exactly one `repository` artifact (head SHA as `provider_revision`, identity from CC4's descriptor), one `file` artifact per accepted input (same identity and raw-identity checks as prose) and zero or more `history_event` artifacts (one per commit CC7 bound); spans, observations and views may sit on the repository and history_event revisions as well as file revisions. The profile's re-derivation of `generation_id` through `generation_for_inputs` (`generation_profiles.py:~181-197`, which today passes ALL member pairs) must EXCLUDE the `history_event` pairs: they are members but not identity inputs, because the generation is settled before `read_history` runs (CC7 finding; the repository member IS an identity input). CC7's `MergedCodeBundle` exports `.accepted_pairs` (the hashed set) and `.history_pairs` (the complement) for exactly this. Never dereference a `history_event` revision's `raw_uri` (`hippo-commit:<sha>` names no stored object). Commit passages arrive as `BoundCommitPassage` beside CC6's `BoundCodePassage`; both need a dense `Passage` row. The plain-prose profile's behaviour and every existing checksum stay byte-identical (the prose suites prove it). For the `generation_checksums` change you hold a NARROW grant on `src/hippo/store/generations.py`: only the evidence-membership check inside `generation_checksums`, nothing else in that file, and only after CC3 has finished (the spawn message will confirm).
5. Tests, NEW `tests/unit/test_staged_code_writer.py` on Fake and Ladybug: dependency completeness (no batch leaves a dangling endpoint or unbound native row); per-batch epoch/lease/fence revalidation; conflicting persisted payload fails closed; extra generation-scoped row fails the resume; partial group fails the resume; full resume after a simulated crash mid-batch skips exactly the written groups and reports the count; seal writes the manifest with matching checksums and `validate_generation_seal` passes; an identical replay is idempotent; a lost fence or expired lease prevents every write and the seal; the payload ceiling refuses; `rebaseline` accepts an unrelated authorization-epoch change, refuses after a capability loss, after a suppression epoch change, after a `SourceControl` change, after a sticky failure and inside an ambient transaction; repo/archive sources capture authority; `test_build_authority.py`'s 21 tests unchanged.

FILES:
  - own: NEW `src/hippo/knowledge/staged_code.py`, NEW `tests/unit/test_staged_code_writer.py`, `src/hippo/knowledge/build_authority.py`, `tests/unit/test_build_authority.py` (additions only), `src/hippo/knowledge/generation_profiles.py` and its unit test (the `code` profile, item 4b), the evidence-membership check inside `src/hippo/store/generations.py::generation_checksums` ONLY (item 4b), NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc8.md`.
  - do NOT touch: the rest of `src/hippo/store/*` and `tests/fakes/fake_store.py` (CC2/CC3 finished; the probe reads through CC2's `_knowledge_rows(kind, *, generation_id=None, where=None)` and `_native_rows(kind, *, ids=None, generation_id=None)` — see `code-capture-notes.md` item 5; if the writer needs another store method, stop and ask with the exact name), `src/hippo/knowledge/staged_prose.py`, `code_binding.py`, `code_history.py`, `model.py`, `src/hippo/ingest/*` (CC9a owns `prose_generation.py`; CC9b owns the coordinator), `docs/`, the checkpoint, `GATES.md`.

STEPS: worktree + venv (with the `mcp==2.1.1` pin); baseline `tests/unit/test_staged_prose_writer.py tests/unit/test_build_authority.py tests/unit/test_generation_resume.py tests/unit/test_generation_failure.py tests/unit/test_code_binding.py tests/unit/test_code_history.py` green on Fake; RED; implement; GREEN Fake on the baseline set plus the new file; GREEN Ladybug on `tests/unit/test_staged_code_writer.py tests/unit/test_build_authority.py`; Ruff; evidence with the CD7 command result (all three files exist once you are done); commit in two commits (writer, then authority). Neo4j is root-owned: name the query shapes you added. This slice fits one worker because the writer template is 242 lines and the authority change is one method plus one allow-list; the probe is the only new algorithm.

DONE WHEN: green on both backends with `-W error`; evidence written; `horch done` lists `write_staged_code`, `probe_staged_rows` and `rebaseline` signatures verbatim, the batch-group order, the probe's three outcomes, counts and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for contract questions.
