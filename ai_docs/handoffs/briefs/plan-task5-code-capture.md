# Brief: design the managed capture of code repositories (the largest remaining Task 5 obligation)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`), read-only except the two files you create. This is a design task: no code, no tests.

GOAL: A reviewed implementation contract, `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`, plus a proposed gate ledger `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`, that lets a fleet of workers move repository and code-file sources from the legacy pipeline to managed generations with the same guarantees the plain-prose lane now has, split into worker-sized tasks.

CONTEXT (read all before writing):
- Master plan `docs/rag_it_all.md`: Task 5 (its steps 1–3 and 7 and the status paragraph, which names "code/rich/history capture and provenance" as what keeps Task 5 open), sections on evidence identities, namespaces and generations, and Task 6 (structured documents, which you must NOT design; rich documents are a separate plan). Extract `docs/rag_it_all_remaining_tasks.md` for Tasks 5A–16.
- What the plain-prose lane built and how: `ai_docs/plans/rag-it-all-task-5-prose-coordinator.md`, `ai_docs/plans/rag-it-all-task-5-production-activation.md` (with amendments), the storage contract `ai_docs/plans/rag-it-all-task-5-storage.md`, derived evidence `rag-it-all-task-5-derived-evidence.md`, dense dispatch `rag-it-all-task-5-dense-dispatch.md`, and the checkpoint `ai_docs/checkpoints/2026-09-11-execution-state.md` (read the whole file; it records every reviewed primitive, including the Task 5 namespace increment, the syntax cache, structural loading with `StructuralCodeEvidence`/`StructuralObjectEvidence`, raw artifact storage, accepted inputs, plain provenance and chunk-to-evidence binding).
- The code side today: `src/hippo/codegraph/` (walkers, `resolve.py`, `syntax_cache.py`, `extract.py`, namespaces), `src/hippo/ingest/{readers,repos,pipeline}.py` (legacy repo and archive ingestion, `add_repo`), `src/hippo/hipporag/indexer.py` (legacy code writes; `write_commit_history`), `src/hippo/store/code.py`, the history extraction under `src/hippo/codegraph` or `hipporag` (`git_history`), `src/hippo/knowledge/{projection,dense,input_binding,build_authority,lifecycle}.py`, `src/hippo/ingest/{managed_activation,prose_generation,accepted_inputs,provenance,prepared_chunks}.py`, `src/hippo/knowledge/inputs.py` (after the layering cleanup). Read `tests/unit/test_structural_loading.py` and `test_managed_pipeline_activation.py` to see what is already proven for code evidence in structural views.
- Constraints that bind the design: every managed guarantee of the prose lane (immutable accepted inputs, detached preparation, atomic legacy bootstrap, fenced refresh with G1 serving, non-collecting failure, exact `GenerationEvidenceMember` closure, original spans with real locators, no source-wide cleanup, tombstone not deletion, one structural owner per query, verified or tag-compatible dense dispatch); the namespace rule (logical source ID constant, node namespace per generation); the syntax cache (rematerialise, never trust cached IDs); code evidence carries `StructuralCodeEvidence` with exact `DEFINED_IN` support and `StructuralRelationEvidence` with `source_generations`; git history becomes artifacts/revisions with the temporal fields Task 5A defined (`recorded_from`, `valid_from`, `observed_at`, `source_updated_at`) and NEVER wall-clock defaults; OpenIE is not applied to code; embeddings for code chunks use the same verified profile binding; Neo4j must keep the contract at scale (batch writes, no per-node transactions); bootstrap ceilings must be real for a 1,000-file repository (the prose ceiling of 1,000 chunks will not do; propose a chunked, resumable staging protocol under one generation with a documented crash/retry story, not "split across commits" in the prose sense).
- User rules: no time-based estimates or prioritisation; LadybugDB is the primary backend; never touch `data/`; production stays legacy until a reviewed activation increment.

DELIVERABLE 1 — the plan (target 250–400 lines), with these sections: outcome and scope (repositories, archives, single code files; history in scope; rich documents, connectors and Task 6 blocks out of scope); existing seams table (file:line, use or boundary); proposed public APIs (a `build_code_source(...)` coordinator or an extension of `build_plain_source` with explicit reasons for the choice, accepted-input capture for a repository tree, per-file provenance and line locators, the generation identity inputs including the commit SHA and walker/grammar versions, history artifacts); deterministic preparation (walk, resolve whole-source, cache rematerialisation, chunking of code with exact span mapping, embedding); staged writing at scale (batches, checkpoints, lease renewal, resume after crash, exact membership closure, how `StructuralCodeEvidence` and relations are written and sealed); bootstrap and refresh protocols (what differs from prose; how a legacy code source converts with legacy serving until publish; how `write_commit_history` legacy native writes are replaced); temporal fields for commits and file revisions; failure, cancellation, idempotence; privacy (paths, code text, model I/O boundaries); acceptance gates (a proposed ledger with CHECK/CRITERIA/EXPECT lines in the repo's ledger format, Fake first, Ladybug primary, Neo4j root-owned); and a task split table: each task with exclusive files, gates, required result, sized so a worker finishes in well under 400k tokens (see `ai_docs/handoffs/fleet-worker-rules.md` budget rule), with dependencies stated.

DELIVERABLE 2 — the ledger file with the proposed gates, unticked, "Status: proposed; no implementation evidence".

Write both, run `ruff format --check` on both (Markdown fenced Python is checked in CI), then `horch done` with the plan path, the number of tasks in the split, the two or three design decisions you are least sure of (so the orchestrator can rule before implementation), and any existing seam you found missing or contradictory.

CONSTRAINTS: read-only otherwise; no Neo4j; no code; do not edit the master plan or any existing plan.

REPORT: `horch note` after reading, after the seams table, after the draft, after the ledger.
