# Brief: activation Task 4b-i — web ingress actors, error handling and source/user route sessions

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `pa4b1` (branch `wp/pa4b1`, base `1acf069`, the current `rag-it-all-tibs` HEAD, which contains Task 4a's structural default and `public_errors.py`).

GOAL: Every web ingress route passes the real reader as a `BuildActor`; managed failures reach clients as the closed public codes; the source, page, user and API routes you own hold one structural session per response with no preflight-close-reacquire; every test in your files is green under the new structural default. Committed on `wp/pa4b1`.

CONTEXT (read all of these before coding):
- Plan `ai_docs/plans/rag-it-all-task-5-production-activation.md`: "Build actors at every boundary" (web bullets), "Managed delete" HTTP paragraphs, "Reindex and mixed bulk" response rules, "Production query-session activation", "Safe transport failures". Gates PA1 (route half), PA6, PA7.
- Task 4a outputs: `ai_docs/gates/rag-it-all/task-5-production-activation/session-audit.md` (70 rows; yours are the rows tagged 4b whose file is `web/app.py`, `web/auth.py`, `web/routes/api.py`, `web/routes/pages.py`, `web/routes/sources.py`, `web/routes/users.py`) and `evidence-pa4a.md` section "Breakage for 4b/4c/4d" (take every row whose test file is one of yours). `src/hippo/knowledge/public_errors.py`: `public_failure(exc) -> PublicFailure | None`, `public_failure_for_code(code)`, `OPERATION_FAILED`; caller rule in its docstring.
- Task 3a merged (`c668688`): `pipeline.add_text/add_upload/start_indexing/run_indexing/reindex` accept `build_actor`; `managed_activation.plan_dispatch` RAISES `ManagedActorRequired` for a managed source without an actor and returns `Dispatch('skip','tombstoned')` for a tombstone (`ai_docs/reports/2026-09-11-pa3a-review.md`, last section). Task 3b (in flight on `wp/pa3b`) adds `delete_source(ctx, source_id, *, build_actor=None, operation_id=None)` and `reindex_all(ctx, *, build_actor=None)`; do that wiring LAST, after the orchestrator tells you "3b merged" and you run `git merge rag-it-all-tibs` (authorized for this brief only).
- Notes that bind you: `ai_docs/handoffs/briefs/task4-notes.md` (all of it), especially the `reindex_all` route validating a view after the pipeline call (must become one held owner spanning the call and the post-operation validate), and the AnyIO rules.

REQUIRED BEHAVIOR:
1. Web page forms and JSON routes for pasted text and uploads call `require(..., "add_sources")`, take the current `Principal`, build `BuildActor.reader(principal)` and pass it to `add_text`/`add_upload`. Delete and reindex use the already authorized `manageable_source` principal's reader actor. Bulk reindex keeps `edit_graph` and passes that principal's reader actor. Never construct `BuildActor.trusted_local()` in web code; never capture a Request, token or cookie in a job.
2. Managed delete keeps `{"deleted": source_id}`; a later lookup returns the same 404 as any unavailable source; `AuthorizationChanged` on a reader's own retry maps to that generic response; `ManagedActorRequired` maps to the generic permission response. `reindex_all` keeps `{"accepted": true}` and exposes no per-source breakdown.
3. `web/app.py` maps exceptions through `public_failure`, preserving existing response shapes and adding `code`; unknown managed-path exceptions use `OPERATION_FAILED`; legacy validation routes keep their current messages. Background managed build failures already write `ManagedFailure.code` to the Source row; the source DTO surfaces it via `public_failure_for_code` (coordinate wording with Task 4b-ii, which owns `status.py`: you consume, they render).
4. Every route you own holds exactly one structural `query_session` (or borrows the request's) through DTO/render/save; no route performs a graph or profile preflight, closes it, then reacquires. `reindex_all` in `routes/sources.py` holds its owner across the pipeline call and its post-operation validate.
5. Tests inject exception strings containing a fake secret, an absolute path, source text and a model body into the managed lane and assert none reaches JSON, HTML, headers or logs captured at public level.

FILES:
  - own: `src/hippo/web/app.py`, `src/hippo/web/auth.py`, `src/hippo/web/routes/api.py`, `src/hippo/web/routes/pages.py`, `src/hippo/web/routes/sources.py`, `src/hippo/web/routes/users.py`, `tests/unit/test_web_base.py`, `tests/unit/test_web_auth.py`, `tests/unit/test_web_sources.py`, `tests/unit/test_web_users.py`, `tests/unit/test_web_pages.py` (use `ls tests/unit | rg web` for the real names and claim only the ones for your routes; list them in the evidence), NEW `tests/unit/test_managed_web_ingress.py`, NEW `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa4b1.md`.
  - do NOT touch: `src/hippo/web/routes/{analyze,code,graph}.py`, `src/hippo/web/render.py`, `src/hippo/status.py` (Task 4b-ii), `src/hippo/mcp_server.py`, `cli.py`, `remote.py` (4c), `src/hippo/analysis/*`, `src/hippo/evals/*`, `eval_access.py`, `changeset_access.py` (4d), `src/hippo/knowledge/*`, `src/hippo/ingest/*`, `src/hippo/store/*`, `context.py`, the shared `GATES.md`, `docs/`, the checkpoint.

STEPS:
1. Worktree + venv per the rulebook (with the `mcp==2.1.1` pin). Baseline: run your test files with the AnyIO form (b) ignore and record which fail on clean `1acf069` (these must match the breakage table rows assigned to your files).
2. RED `test_managed_web_ingress.py`: authenticated pasted text and `.md` upload create a managed source through the coordinator (assert the actor reached `add_text`/`add_upload` via a spy, then a real bootstrap with the MockTransport fixtures from `test_managed_pipeline_activation.py`); open-mode and preview identities stay legacy or are refused per the plan table; authenticated `.pdf`, `.py`, zip, repo, sample stay legacy; delete/reindex/bulk pass the reader actor; the redaction test; a session-ownership test per route pattern (acquire once, revoke between DTO and response, expect the existing 409). Save `/tmp/hippo-pa4b1-red.log`.
3. Implement; fix the breakage-table tests in your files as the table prescribes.
4. GREEN Fake: all your test files plus `test_managed_route_activation.py test_query_session.py test_status_access.py` with form (b); log `/tmp/hippo-pa4b1-fake-green.log`. GREEN Ladybug: `test_managed_web_ingress.py test_web_sources.py` (real names); log `/tmp/hippo-pa4b1-ladybug-green.log`.
5. Ruff; evidence file (commands, results, logs, the audit rows you converted by row id, breakage rows fixed, deviations); commit on `wp/pa4b1` in two or three commits. Then wait for "3b merged", merge, wire delete/reindex_all actors, rerun step 4, commit again.

DONE WHEN: your files green on Fake and Ladybug with `-W error` (form (b) only where a module-level `fastapi.testclient` import exists); evidence written; commits on `wp/pa4b1`; `horch done` lists commits, files, audit rows converted, breakage rows fixed, counts and logs, and anything left for 3b's merge if the orchestrator has not announced it by the time you finish everything else (say so and stop cleanly).

REPORT: `horch note` per step. `horch tell orchestrator "[<role>] BLOCKED: ..."` for ownership or contract questions; wait.
