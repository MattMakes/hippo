# Brief: project a managed code generation's DEFINED_IN and CODE_EDGE arrows

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `codeproj` (branch `wp/codeproj`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: A published managed code generation serves the same structural arrows a legacy code source serves: `DEFINED_IN` from each bound symbol to the passage holding its span, and every native code relation (`CODE_EDGE` kinds such as INVOKES/CONTAINS/IMPORTS, plus `MODIFIES`/`PRECEDES` where the legacy loader serves them) between the selected generation's nodes, with exact evidence; today it projects ZERO arrows. Found by CC11 (probe log `/tmp/hippo-cc11-scratch.log`): on `test_code_generation`'s 7-file world, 14 code nodes and 14 `StructuralCodeEvidence` sidecar rows project with `Counter(arrow kinds) == {}`. Ruling 1 of `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md` keeps code relations NATIVE (`CODE_EDGE` rows, not knowledge relations); the structural projection must read them for the selected generations. Committed on `wp/codeproj`.

CONTEXT: `src/hippo/knowledge/projection.py` (`:~538-545` skips `DEFINED_IN` when a rendered passage's `closure.binding_ids` is empty; there is no read of native `CODE_EDGE` at all; `_assemble` applies `canonical_arrows`, see `812c60e`); `src/hippo/knowledge/code_binding.py:~459-463` refuses `input_binding_ids` on every code `DerivedRecord`, so every symbol chunk (all are rendered views because of the context header) has no binding ids; the sidecar `StructuralCodeEvidence` rows; the native rows and relations CC8 writes (`tests/unit/test_staged_code_writer.py:~505` shows `CODE_EDGE`/`DEFINED_IN`/`MODIFIES`/`PRECEDES` in the native representation); CC2's scoped `_native_relationships(*, generation_id=...)` and `_edges_touching`; the legacy loader's code arrows (`src/hippo/hipporag/graph_index.py`, `code_out`, `DEFINED_IN`) which define parity; `GraphIndex.selected_managed_generations`; `tests/unit/test_derived_projection.py`, `test_structural_loading.py`, `test_code_binding.py`, `test_code_generation.py`. Ruling 14: only the selected (active) generation's rows are served; a staged, failed, retired or tombstoned generation projects nothing. The Ladybug engine defect (`evidence-lbfix.md`): never `IN <list>` on a node string column.

REQUIRED BEHAVIOR:
1. `DEFINED_IN`: each bound symbol (and data object) projects one `DEFINED_IN` arrow to the passage whose original span holds its definition, with the span as exact support; decide whether `code_binding` should emit the binding ids for code derived records (mirroring the prose `input_binding` closure) or whether projection reads the native `DEFINED_IN` rows plus the sidecar — pick the one that keeps evidence exact and identity unchanged (a change to what `code_binding` emits changes bundle contents but must NOT change `generation_id`; prove it), and say why in the evidence.
2. `CODE_EDGE` (and `MODIFIES`/`PRECEDES` if legacy serves them on the graph): projected from the selected generation's native relation rows through one scoped read per generation, endpoints restricted to that generation's nodes (a cross-generation edge never appears), arrow order canonical (`canonical_arrows`), `view_fingerprint` stable across two loads.
3. Parity test: the same repository indexed through the legacy lane and through the managed lane (bootstrap + publish) projects the same arrow multiset by kind and endpoint canonical keys (allow only the documented differences and list them); a second staged generation adds nothing until it publishes; a tombstoned source projects no code arrows.
4. Backends: Fake and Ladybug (`test_derived_projection.py test_code_generation.py -k "publish"`, the new test file); Neo4j root-owned (name the shapes).

FILES:
  - own: `src/hippo/knowledge/projection.py`, `src/hippo/knowledge/code_binding.py` (only if item 1 chooses to emit binding ids; additions only), NEW `tests/unit/test_code_projection.py`, `tests/unit/test_code_binding.py` (only for item 1), NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-codeproj.md`.
  - do NOT touch: `src/hippo/store/*`, `tests/fakes/fake_store.py` (backend-developer-25 is live there on `wp/kscope`), `src/hippo/status.py` (opus-22), `src/hippo/knowledge/staged_code.py`, `code_history.py`, `src/hippo/ingest/*`, `context.py`, `GATES.md`, `docs/`, CC11's files.

STEPS: worktree + venv (`mcp==2.1.1` pin); baseline `tests/unit/test_derived_projection.py tests/unit/test_structural_loading.py tests/unit/test_code_binding.py tests/unit/test_code_generation.py` on Fake; RED = the zero-arrow probe as a test; implement; GREEN Fake and Ladybug; Ruff; evidence with the arrow counts before/after and the identity proof; one or two commits.

DONE WHEN: green both backends; the parity test passes; evidence written; `horch done` lists the arrows projected per kind on the fixture, the identity proof, and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` if a store read is missing (name it; backend-developer-25 may add it).
