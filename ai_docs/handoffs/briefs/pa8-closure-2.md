# Brief: PA8 closure batch 2 (the enumerated residue)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `pa8close2` (branch `wp/pa8close2`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: Every row the re-sign-off `ai_docs/reports/2026-09-13-pa8-resign.md` lists as blocking (PA2-2, PA2-4, PA3a-6, PA3a-8, PA3a-9, PA3a-11, PA3b-6, PA4b2-7, PA4b2-9, W15, W20, T2, D4, D5) is CLOSED or assigned by the plan, following that report's "Routes to a signature" items (1) and (2) exactly, plus its "Corrections the orchestrator owns" that live in evidence files. Committed on `wp/pa8close2`.

CONTEXT: the re-sign-off report (read it whole: it names each fix, the file and the reviewing report that asked for it); the first sign-off `ai_docs/reports/2026-09-12-pa8-signoff.md`; the closure evidence `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa8close.md`; the activation plan `ai_docs/plans/rag-it-all-task-5-production-activation.md` (rollout section `:~268-270` holds the Task 16 assignments D1–D3); the coordinator plan whose implementation notes T2 wants (find it under `ai_docs/plans/` by the coordinator review's reference); `ai_docs/handoffs/briefs/task4-notes.md` (line `:~119` wrongly says minor 7's sentences were written). CC10 (backend-developer-23) is LIVE on `src/hippo/ingest/managed_activation.py`, `pipeline.py`, `readers.py`, `repos.py`, `src/hippo/web/routes/sources.py`, `src/hippo/cli.py` and their tests: if a fix lands in one of those files, leave that row OPEN, say so in the evidence, and the orchestrator routes it to CC10.

REQUIRED BEHAVIOR:
1. Route (1), plan text: owner sentences in the activation plan's rollout section for PA2-2 (Task 16 performance), PA2-4 (the code-capture slice that dispatches the code coordinator: name CC10, `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`, and add the counter `status.source_view` must attribute code edges by generation membership once code generations exist), PA3b-6 (store owner), PA3a-11 and PA4b2-9 (next owners of `ollama.py` and `context.py`); two rollout bullets assigning D4 (`answer_withheld`) and D5 (gold means, accuracy None beside errors N) to Task 16; T2's two sentences in the coordinator plan's implementation notes (what the coordinator review's minor 7 asked for, verbatim from that review).
2. Route (2), the small batch: PA3a-6 (one-line name check), PA3a-9 (a comment explaining the two definitions of "managed" and why both exist, at both sites), W15 (`except Exception` with the bounded log line), W20 (`partials/failure.html`), PA3a-8 (the test plus the 1,000-vs-20,000 decision: pick the value the plan states, say which and why), PA4b2-7 (pin that `/api/simulate` answers 404, not 500; add the `public_errors` row only if the reviewing report asked for one). Each fix is exactly what its reviewing report named; if it needs more, leave it OPEN with the reason.
3. Corrections: `evidence-pa8close.md` "CLOSED: 15" becomes the true count from its own table, and its PA3b-8 test name becomes `test_a_refresh_interrupted_by_a_restart_is_retired_without_losing_g1`; `task4-notes.md:~119` is corrected to say minor 7's sentences are written by this batch (item 1).
4. Do NOT touch `GATES.md`, `docs/`, the checkpoint, `src/hippo/store/*`, `src/hippo/knowledge/*`, or any file in CC10's list above. Do not "fix" W12, 4b-i F2, 4b-i F6 or PA3a-10 (the do-not-fix items).

FILES:
  - own: the activation plan (rollout section and implementation notes only), the coordinator plan's implementation notes, `evidence-pa8close.md` (the two corrections), `task4-notes.md` (one line), the files the six route-(2) findings name outside CC10's list (expected: `src/hippo/web/templates/partials/failure.html`, one web module for W15, `src/hippo/context.py` or `status.py` comments for PA3a-9 — comments ONLY there, the test files the findings name), NEW `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa8close2.md`.
  - do NOT touch: everything else.

STEPS: worktree + venv (`mcp==2.1.1` pin); baseline the PA2, PA4 and PA6 CHECK lines from `GATES.md` verbatim on Fake (PA6 with the ledger's own AnyIO form (b)); RED for each new test; fix; GREEN on the same lines plus every touched test file; Ruff check + format --check on every touched file including Markdown; evidence with one row per id (id, status CLOSED/OPEN/ASSIGNED, commit, test or plan line); two commits (plan + evidence corrections, then code + tests).

DONE WHEN: every one of the 14 ids is CLOSED, ASSIGNED-in-plan, or OPEN-routed-to-CC10 with the reason; PA2/PA4/PA6 lines green on Fake; evidence written; `horch done` lists the commits, the per-id outcomes and the logs.

REPORT: `horch note` per route; `horch tell orchestrator "[<role>] BLOCKED: ..."` for a fix that needs a file you may not touch.
