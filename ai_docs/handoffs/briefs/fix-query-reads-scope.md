# Brief: scope the query-time reads of generation-sized knowledge tables

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `qscope` (branch `wp/qscope`, base = the `rag-it-all-tibs` HEAD named in the spawn message).

GOAL: Opening a structural `query_session`, renewing its lease, projecting the managed graph and dispatching a `retrieval_session`/`dense_session` read the generation-sized knowledge kinds (`IndexManifest`, `IndexEvent`, `GenerationMember`, `GenerationEvidenceMember`, `EvidenceSpan`, `ObjectObservation`, `RetrievalView`, `DerivedRecord`, `DerivedDependency`, `NativeBinding`) ONLY for the selected generations (and the authorized revisions those imply), never whole-table, so a query session's cost is bounded by what it serves rather than by the corpus. Found by CC11 (`evidence-cc11.md`; its acceptance recorder saw 30+ whole-table generation-sized reads during a renewal, and on LadybugDB at ~50 files the projection-plus-arrows phase took 427 s and the dense dispatch more than 7 minutes, with ~300k knowledge reads in one resume bootstrap). Committed on `wp/qscope`.

CONTEXT: the write-path precedent kscope (`evidence-kscope.md`, merged at `df05bac`: `_knowledge_rows(name, *, generation_id=None, where=None)` over the per-kind allow-list in `store/knowledge.py`, schema v7, `derivations.GenerationViews`; kscope finding 7 names query-time `validate_view` building one inventory per view); CC2 (`evidence-cc2.md`, `_native_rows`/`_native_relationships` scoped, `_selected_revisions(generation_id)`); the Ladybug engine defect (`evidence-lbfix.md`: never `IN <list>` on a node string column; use the UNWIND primary-key helper); ruling 14 (`context.legacy_lane()`); `GraphIndex.selected_managed_generations`. Sites CC11 found by grep (verify each, there are more behind variable-kind reads): `src/hippo/context.py:~282` and `:~402` (`IndexManifest`), `src/hippo/knowledge/projection.py:~426` (`GenerationMember`; also `_selected_pairs`, which the activation plan's rollout section assigns to Task 16 as O(selected × authorized revisions), `by_generation` shape), `src/hippo/knowledge/temporal.py:~495` (`GenerationMember`), `src/hippo/store/snapshots.py:~47`, `:~248`, `:~297` (`IndexEvent`, `GenerationMember`, `IndexEvent`), `src/hippo/store/generations.py:~1765` (`IndexManifest`), plus the `EvidenceSpan`/`ObjectObservation` reads on the renewal (`lease_heartbeat` -> proof revalidation) and projection paths. Query-time `validate_view` in `projection.py`/`access.py` builds one inventory per view (kscope finding 7): hoist to one per selected generation per session.

REQUIRED BEHAVIOR:
1. Every listed read (and every one you find on the `query_session` -> renewal -> `retrieval_session`/`dense_session` path with a `_knowledge_rows(kind)` call and no scope) becomes scoped by the selected `generation_id`s (one bounded read per generation, or one `where=` key on the allow-list, adding the index as a journaled v8 step ONLY if a new key is needed — first freeze `V7_INDEXES` and `V7_DESCRIPTOR` as literals, per kscope's gotcha). Reads that are legitimately corpus-wide (the source inventory's list of published events for legacy-lane classification, for example) stay, are named, and are bounded by source not by rows.
2. Results byte-identical: `view_fingerprint`, every projected node/arrow, every proof, every temporal selection and every dense dispatch decision unchanged; the reviewed suites (`test_structural_loading.py`, `test_derived_projection.py`, `test_code_projection.py`, `test_temporal_*.py`, `test_query_session.py`, `test_dense_session.py`, `test_snapshot_store.py`, `test_query_snapshots.py`, `test_status_access.py`, `test_managed_source_inventory.py`) prove it.
3. Bound proven: a query-time counter test on Fake (`knowledge_whole_table(kinds=GENERATION_SIZED) == []` across `query_session` open + one forced lease renewal + `retrieval_session` + `dense_session` over a published code generation AND a published prose generation, with a second, unrelated published generation present so a scoped read cannot pass by accident), and CC11's acceptance recorder assertion (thread-local, in `test_code_capture_acceptance.py`) then passes without the thread filter for the renewal.
4. Ladybug: rerun CC11's `HIPPO_CODE_ACCEPTANCE_TIMINGS` phase timings at N=8 (`.worktrees/cc11`'s test file, once merged; otherwise the coordinator fixture from `test_code_generation.py`) and record the projection and dense-dispatch phases before/after in the evidence (numbers, no time-based claims about production).

FILES:
  - own: `src/hippo/context.py`, `src/hippo/knowledge/projection.py`, `src/hippo/knowledge/temporal.py`, `src/hippo/knowledge/access.py` (query-time reads only), `src/hippo/store/snapshots.py`, `src/hippo/store/generations.py`, `src/hippo/store/knowledge.py` (allow-list), `src/hippo/store/migrations.py` (only for a v8 step), `tests/fakes/fake_store.py` (counters only), NEW `tests/unit/test_query_scoped_reads.py`, `tests/unit/test_knowledge_scoped_reads.py` (counter helpers), NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-qscope.md`.
  - do NOT touch: `src/hippo/store/ladybug.py`, `src/hippo/store/__init__.py`, `tests/conftest.py` (backend-developer-26, `wp/lbpool`), `src/hippo/status.py` (done; ask if a read there needs scoping), `src/hippo/knowledge/staged_*.py`, `code_binding.py`, `code_history.py`, `src/hippo/ingest/*`, CC11's files (`tests/unit/test_code_capture_acceptance.py`, fixtures, the plan, `evidence-cc11.md`), `GATES.md`, `docs/`.

STEPS: worktree + venv (`mcp==2.1.1` pin); baseline the suites in item 2 on Fake; RED = the counter test; fix; GREEN Fake; GREEN Ladybug on `test_query_session.py test_structural_loading.py test_code_projection.py test_temporal_conflicts.py -k "publish or select or reopen"` plus the new file; Ruff; evidence with the site list, the counter proof and the Ladybug phase numbers; two commits (allow-list/readers, then consumers).

DONE WHEN: green both backends; fingerprints/results unchanged; counter proof; evidence written; `horch done` lists every scoped site, the consumers, the before/after phase numbers, Neo4j shapes and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` if a scoped read needs a seam in a frozen file (name the exact function).
