# Brief: independent SPEC/QUALITY review of activation Task 3b (managed delete dispatch, mixed bulk reindex, interrupted-refresh recovery)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`); the work is merged at HEAD (merge commit "Merge wp/pa3b", branch commits `7bf346d`, `990b587`, `4447cbc` (a merge of the integration branch), `3e79f77`). You read and run; you do not edit source or tests.

GOAL: Independent SPEC and QUALITY verdict on Task 3b before publication.

CONTEXT:
- Plan `ai_docs/plans/rag-it-all-task-5-production-activation.md`: "Managed delete: suppression now, physical purge elsewhere" (dispatch and HTTP paragraphs), "Reindex and mixed bulk behavior" (all), invariants 1, 2, 9, the mixed-bulk and monkeypatched-destructive-operation adversarial cases. Gates PA4 (dispatch half), PA5, PA7 (pipeline half; the two Ladybug reopen tests).
- Orchestrator brief: `ai_docs/handoffs/briefs/pa3b-delete-and-bulk-dispatch.md` (including its addendum items from the Task 3a review: driving `reindex` for the conversion test, `caplog.text` redaction, the actorless upload cell, `synonymy_threshold` validation, the restart sweep for interrupted refreshes, the false deviation 7, the profile-mismatch row ahead of `OllamaError`). Implementer evidence with six decisions: `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa3b.md`. Task 1 contract: `ai_docs/reports/2026-09-11-pa1-review.md` (signatures and handoff notes). Task 3a review: `ai_docs/reports/2026-09-11-pa3a-review.md`.
- Implementer decisions to judge: (1) a managed delete has no Busy precondition (legacy delete keeps it); (2) a managed delete never waits for its own job; (3) a failed managed preflight makes `reindex_all` return 0 and the route still answers `{"accepted": true}` with only a local log line revealing it; (4) a bulk with no actor over a managed inventory RAISES `ManagedActorRequired` rather than returning 0; (5) a tombstoned source is dispatched into `tombstone_managed_source` rather than short-circuited; (6) an unbounded `operation_id` is refused even for an unmanaged source. Also the store-level restart sweep (`mark_interrupted_jobs` in `store/memory.py`, `store/ladybug.py`, `tests/fakes/fake_store.py` with shared constants) and the `_fraction` validator.
- Diff: `git diff da51784..3e79f77 -- src/ tests/` (exclude the merge commit's own changes: compare against `4447cbc` for the last commit if needed).

FILES:
  - own: `ai_docs/reports/2026-09-11-pa3b-review.md`.
  - do NOT touch: anything else (several workers are live in worktrees and in this tree).

STEPS:
1. Run, each to `/tmp/hippo-pa3b-review-<n>.log` with `echo EXIT $?`: (a) PA4 and PA5 CHECK lines verbatim from the ledger (Fake; if `test_status_access.py` is in PA5's list and still red on the Group C row, say so, it belongs to 4b-ii's merged fix and should now be green); (b) `HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_managed_pipeline_activation.py tests/unit/test_ingest_pipeline.py tests/unit/test_ingest_concurrency.py tests/unit/test_managed_source_lifecycle.py tests/unit/test_generation_store.py tests/unit/test_import_order.py tests/unit/test_store_knowledge.py -q -o addopts='' -W error`; (c) `HIPPO_TEST_STORE=ladybug .venv/bin/pytest tests/unit/test_managed_pipeline_activation.py -k "reopen or tombstone or bulk or interrupted" -q -o addopts='' -W error` (say how many were selected; the full file takes about 28 minutes and the implementer ran it, cite their log).
2. SPEC review; PROVEN (test) / UNTESTED / VIOLATED (file:line): every delete-dispatch cell (managed + actor → tombstone; managed without actor → raise before any legacy hook, file removal or generation mutation; unmanaged → legacy unchanged; tombstoned → generic not-found, no mutation); the destructive spies (`Store.delete_source`, `_clear_passages`, `delete_passages_for_source`, `delete_code_nodes_for_source`, `remove_orphans`, `rmtree`, generation collect/discard, raw unlink) raising while a managed delete still tombstones and an unrelated source stays byte-identical; the seven bulk protocol steps each mapped to a test, in particular that a failed managed preflight makes ZERO mutations and that a successful preflight clears only legacy lanes, skips tombstones, preserves managed G1, submits each lane with its own operation ID, isolates one lane's asynchronous failure, and fails a raced lane without legacy fallback; the Ladybug reopen after bootstrap, failed refresh and tombstone; the restart sweep turning `refreshing: ...` into `refresh_failed` with `status` still `ready` and counts/pointer untouched on Fake and Ladybug; the addendum items.
3. QUALITY review, with attention to: decision 1 (can a managed delete race an in-flight managed refresh in a way that leaves the refresh's job or generation in an inconsistent state, given `claim_generation_build` now refuses on a tombstone?); decision 3 (is a silent `{"accepted": true}` with nothing done acceptable, or should the route learn a bounded refusal code; note the plan says no hidden per-source breakdown, not no signal at all); decision 4 (raise versus 0: which does the HTTP route currently map, and to what); the restart sweep constants living in `store/memory.py` and imported by the other backends (layering, and whether `base.py`/Neo4j needs the same sweep); the `_fraction` validator versus the coordinator's own `PlainBuildOptions` validation; whether `_preflight_managed` captures `BuildAuthority` under the same lock discipline the coordinator uses; any path where `_prepare_reindex` can run for a managed source.
4. Report any changed public signature.

DONE WHEN: `ai_docs/reports/2026-09-11-pa3b-review.md` exists with `SPEC: PASS|FAIL`, `QUALITY: PASS|FAIL`, the criteria table, numbered findings with severity, file:line, why, proposed fix, run results with log paths. `horch done` states both verdicts, finding counts by severity, and the report path.

CONSTRAINTS: no edits outside your report; no Neo4j; `HIPPO_TEST_STORE` explicit on every command.

REPORT: `horch note` after the runs, after the SPEC table, after the report.
