# Brief: independent SPEC/QUALITY review of Task 5A integration part 2 (correction publication)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`); the work is merged at HEAD (merge commit "Merge wp/t5a2", branch commits `5cd0834`, `880deb7`, `4565ad3`, `7d5984f` on top of `9980961`). You read and run; you do not edit source or tests.

GOAL: Independent SPEC and QUALITY verdict on plan section 5 (append-only correction publication) and the N2 supersession fix before publication.

CONTEXT:
- Contract: `ai_docs/plans/rag-it-all-task-5a-temporal-conflicts.md` sections 1, 3 (per-series supersession), 5 and 6 with every "Amended" note. Orchestrator brief: `ai_docs/handoffs/briefs/t5a-int2-correction-publication.md`. Implementer evidence with eight decisions: `ai_docs/gates/rag-it-all/task-5a-temporal-conflicts/evidence-int2.md`. Prior reviews: `ai_docs/reports/2026-09-11-t5a-int1-review.md`, `...-rereview.md`.
- Signatures reported: `RecordedSegment(record_kind, record_id)`, `TemporalPublicationPlan(published_at, closures, appends=())` with `.fingerprint`; `SameSourceSelection.superseded_by_series`, sorted unique `superseded_version_ids`, `fully_superseded_version_ids(selection)`; `KnowledgeQueries._close_recorded_intervals(segments, *, recorded_to)` (refuses outside an ambient transaction; every row through `update_knowledge`), `_authorized_recorded_closure`; `publish_staged_generation(..., plan=None)`, `publish_generation(..., plan=None)` (compat path rejects a plan); failpoint `closure` plus the existing pointer/version/retirement/event/lease points.
- Implementer decisions to judge: (1) two `_check_knowledge_write` guards are skipped for an authorized close ("Published interpretation is immutable" and the live-build authority branch), gated by a predicate requiring the plan to name that exact row and instant, the row still open, and every other field byte-identical; (3) a version corroborated by a second source cannot be closed by one source's publication; (4) identity preservation enforced as the logical series (kind, workspace, subject, predicate, scope_key) for versions and (kind, object_id) for observations; (5) retry idempotence via `IndexEvent.payload_json.temporal_plan`, present only when a plan exists; (6) `lifecycle.py` unchanged; (7) one EVIDENCE line per gate (part 1 counts cited); (8) a heavier `correction_world` fixture.
- Diff: `git diff 9980961..7d5984f -- src/ tests/`.

FILES:
  - own: `ai_docs/reports/2026-09-11-t5a-int2-review.md`.
  - do NOT touch: anything else.

STEPS:
1. Run, each to `/tmp/hippo-t5a2-review-<gate>.log` with `echo EXIT $?`: T5A1, T5A2, T5A3, T5A5, T5A6 (Fake) and T5A4 (Ladybug) verbatim from the ledger, plus `HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_generation_store.py tests/unit/test_prose_generation.py tests/unit/test_managed_pipeline_activation.py tests/unit/test_snapshot_store.py tests/unit/test_query_snapshots.py tests/unit/test_managed_source_lifecycle.py -q -o addopts='' -W error`.
2. SPEC review; PROVEN (test) / UNTESTED / VIOLATED (file:line): (a) every closure target exists, same workspace and source lineage through complete support, currently open, `recorded_from < published_at`, not already closed differently; (b) every appended segment is an exact member of the staged generation with `recorded_from == published_at` and preserved identity/scope/dependencies; object-observation corrections obey the equivalent closure; (c) one transaction closes, appends, publishes/retires, advances state and writes the event; each of the six failpoints rolls everything back byte-identically; (d) exact retry returns the original receipt and observes applied closures; a different plan, stale parent or fence, later suppression epoch, or conflicting close fails without writes; (e) no model, filesystem, callback or unguarded clock inside the transaction; (f) the compatibility publish path rejects a plan; (g) `select_history` over a corrected series returns the original segment before the correction cutoff and the corrected one after; `recorded_to` visible in serialization; the May ownership example passes; (h) N2: `superseded_version_ids` sorted and unique, keyed per series; `fully_superseded_version_ids` is the only closable set.
3. QUALITY review, with attention to: the two skipped write guards (can the predicate be satisfied by a forged plan from a caller who holds the source lock but not the generation, or by a plan naming a row from another source that shares support?); whether `_close_recorded_intervals`' ambient-transaction check uses `in_ambient_transaction()`; decision 3's conservatism (does it make a legitimate single-source correction impossible when a second source merely corroborated the old claim?); `payload_json.temporal_plan` (does it carry any private text; is the plan fingerprint sufficient for idempotence across a retry with a byte-different but semantically equal plan?); the heavier fixture (does it hide any assertion the lighter one would have caught?); any Ladybug-only path untested on Fake or vice versa.
4. Report the verbatim public signatures for the fixture-loader follow-up and the Neo4j repeat.

DONE WHEN: `ai_docs/reports/2026-09-11-t5a-int2-review.md` exists with `SPEC: PASS|FAIL`, `QUALITY: PASS|FAIL`, the a–h table, numbered findings with severity, file:line, why, proposed fix, run results with log paths, and the signatures. `horch done` states both verdicts, finding counts by severity, and the report path.

CONSTRAINTS: no edits outside your report; no Neo4j; `HIPPO_TEST_STORE` explicit on every command.

REPORT: `horch note` after the runs, after the SPEC table, after the report.
