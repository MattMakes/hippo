# Brief: activation Task 4e — web wrap-up (one failure helper, exact 4xx catches, preview notice, legacy error bounding)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `pa4e` (branch `wp/pa4e`, base = the `rag-it-all-tibs` HEAD named in the spawn message; it contains Tasks 4a–4d, their follow-ups and Task 3b).

GOAL: The web layer has exactly one public-failure helper, every 4xx `detail` comes from an exact closed validator type, a role preview of a managed corpus says so instead of silently showing nothing, and the legacy lane stores a bounded error. Committed on `wp/pa4e`.

CONTEXT (read all before coding): `ai_docs/handoffs/briefs/task4-notes.md` (every section; it is the source of these items), the review reports `ai_docs/reports/2026-09-11-pa4b2-review.md`, `2026-09-11-pa4c-review.md`, `2026-09-11-pa3b-review.md`, and the evidence files `evidence-pa4b1.md`, `evidence-pa4b2.md`, `evidence-pa4cfix.md`, `evidence-pa3b.md` under `ai_docs/gates/rag-it-all/task-5-production-activation/`.

DECISIONS (final):
1. One helper: `web/app.py` uses `render.public_failure_response` / `render.retrieval_failure` (from 4b-ii) and drops its local copy; the 409 handler keeps `{"error": "Permissions changed; repeat the query", "code": "authorization_changed"}`. A grep for `str(exc)` under `src/hippo/web` must find only the legacy settings validators (`api.py` put_settings/checked_settings, `pages.py` settings form), which the plan protects.
2. Exact 4xx catches: the `except ValueError -> HTTPException(400, str(exc))` sites in `web/routes/api.py` (~:50/94/117) and `web/routes/code.py` (~:217) catch only the exact closed validator types (or a dedicated `InvalidRequest` type the validators raise), never `ValueError` by isinstance, so `ReadError`/`TooLarge`/`ProjectionError` subclasses fall through to the mapper. Tests inject each subclass with a path in its message and assert the mapped body instead of the text.
3. Preview notice: `knowledge/access.py:253` gives a preview audience no identity, so `/graph?as_role=` shows a managed corpus as empty. Decision: keep preview audiences on legacy evidence only for this increment, and render an explicit notice in the preview header ("Preview shows evidence available to open readers; managed sources require a signed-in member of the tier") when the workspace has any selected managed generation; no change to `access.py`. Test with one managed and one legacy source.
4. Legacy lane bounding: `ingest/pipeline.py` (~:309) stores `f"{type(err).__name__}: {err}"` for legacy failures; change it to store the exception class name plus a fixed sentence (`ReadError: indexing failed; inspect local logs`) so a path or source text never lands on the Source row; keep the class name so operators can grep logs. Update the legacy tests that assert the old text. (Task 3b is merged; the file is free.)
5. Bulk refusal shape (plan amended 2026-09-12): `pipeline.reindex_all` raises `ManagedPreflightRefused(ManagedDispatchError)` (no source IDs, no count) when the managed preflight refuses, returns 0 only when there is genuinely nothing to do; the route maps it to `409 {"error": "Bulk reindex refused", "code": "bulk_refused"}` and maps `ManagedActorRequired` to the generic permission response. Tests for both, asserting no per-source breakdown and no count.
6. `graph.py:337`'s duplicated settings merge: factor `effective_settings` into `query_access.py` and use it from both sites.
8. Fix the bare `ValueError` at its source: `GraphIndex.canonical_selected_generations` (`hipporag/graph_index.py`) raises `ProjectionError` (import from `knowledge/projection.py` only if that does not create an import cycle; otherwise define the exception in `graph_index.py` and have `projection.ProjectionError` subclass or alias it), so every graph-only owner on every transport maps it without a per-route catch; keep the existing route catches. Test on one 4b-i route, one 4b-ii route and the MCP code tool.
7. Pipeline minors from the 3b review (`ai_docs/reports/2026-09-11-pa3b-review.md` findings 4, 7, 9, 10): wrap each lane's submit in `reindex_all` so a later lane's failure after the legacy clears cannot leave earlier lanes queued without passages (per-lane containment with a bounded log line); the restart sweep must also clear `active_build_id` and mark the orphaned `MaintenanceJob` cancelled so "Reindex to run it again" is not refused with `BuildBusy` for the lease duration (with a test that a reindex right after restart proceeds); move the three sweep constants out of `store/memory.py` (the Neo4j lane) into `store/generations.py` and have `managed_activation._present_progress` build its stage from the shared prefix; validate `operation_id` in `delete_source` even when the row is already gone.

FILES:
  - own: `src/hippo/web/app.py`, `src/hippo/web/routes/api.py`, `src/hippo/web/routes/code.py`, `src/hippo/web/routes/graph.py` (settings merge and preview header only), `src/hippo/web/render.py`, `src/hippo/web/templates/graph.html` (notice only), `src/hippo/knowledge/query_access.py` (`effective_settings` only), `src/hippo/hipporag/graph_index.py` (`canonical_selected_generations`' exception type only), `src/hippo/ingest/pipeline.py` (legacy error storage, `reindex_all` refusal and per-lane containment, `delete_source` operation-id validation), `src/hippo/ingest/managed_activation.py` (`ManagedPreflightRefused`, `_present_progress` prefix), `src/hippo/store/memory.py`, `src/hippo/store/ladybug.py`, `src/hippo/store/generations.py`, `tests/fakes/fake_store.py` (sweep constants and the sweep's job/pointer clearing only), `tests/unit/test_managed_pipeline_activation.py`, `tests/unit/test_ingest_concurrency.py`, tests: `tests/unit/test_managed_web_ingress.py`, `tests/unit/test_managed_web_surfaces.py`, `tests/unit/test_web_base.py`, `tests/unit/test_web_code.py`, `tests/unit/test_ingest_pipeline.py`, `tests/unit/test_graph_surface_access.py`, NEW `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa4e.md`.
  - do NOT touch: `evals/*`, `analysis/*`, `eval_access.py`, `mcp_server.py`, `cli.py`, `remote.py`, `knowledge/access.py`, `store/*`, `docs/`, the checkpoint, `GATES.md`.

STEPS: baseline your files green with form (b) where needed; RED per decision; implement; GREEN Fake (your files plus `test_managed_route_activation.py test_status_access.py`), GREEN Ladybug (`test_managed_web_ingress.py test_managed_web_surfaces.py`); Ruff; evidence; commit in two or three commits.

DONE WHEN: green on both backends; evidence written; commits; `horch done` lists per decision the change and test, counts and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for conflicts.
