# Brief: independent SPEC/QUALITY review of activation Task 3a (managed adapter, eligibility, add/refresh dispatch)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. You work in the ROOT tree (`/Users/mascott/projects/hippo`). The work under review is merged at `c668688` (branch commits `0863b8f`, `efc1075`, `98aac44`, `78e038f` on top of `158ebf2`). You read and run; you do not edit source or tests.

GOAL: Independent SPEC and QUALITY verdict on Task 3a of `ai_docs/plans/rag-it-all-task-5-production-activation.md` before publication.

CONTEXT:
- Plan sections that bind: "Managed eligibility and dispatch" (signatures and table), "Managed build resources and coordinator adapter" (steps 1–7), "Failure and progress presentation", the single-source `reindex` bullets, invariants 1, 3, 5, 8, and the adversarial cases about authenticated pasted text / `.md` / `.pdf` / `.py` / zip / repo / sample, refresh failing at every coordinator boundary, and monkeypatched destructive operations. Gates PA1 (pipeline half), PA3, PA5 (single-source half) in `ai_docs/gates/rag-it-all/task-5-production-activation/GATES.md`.
- Orchestrator brief the implementer followed: `ai_docs/handoffs/briefs/pa3a-managed-adapter-and-refresh.md`. Evidence with eight recorded deviations: `ai_docs/gates/rag-it-all/task-5-production-activation/evidence-pa3a.md` (verify every claim). Orchestrator rulings already made (not deviations): the 1,000-chunk managed ceiling is a refusal with no fallback; the `EmbeddingSpec` prefix helper stays in `managed_activation.py`; `refreshing: publish` is never emitted because publication happens after the last callback; `config.py` untouched because every value existed.
- Diff: `git diff 158ebf2..78e038f -- src/ tests/` (new `src/hippo/ingest/managed_activation.py`, `tests/unit/test_managed_pipeline_activation.py`; modified `pipeline.py`, `readers.py`). Coordinator contract: `ai_docs/reports/2026-09-11-prose-coordinator-review.md` section 4. Task 1 tombstone contract: `ai_docs/reports/2026-09-11-pa1-review.md` penultimate section.

FILES:
  - own: `ai_docs/reports/2026-09-11-pa3a-review.md`.
  - do NOT touch: anything else. Other workers own `src/hippo/ingest/pipeline.py` and `managed_activation.py` in a worktree (Task 3b), `query_access.py`/`ask.py` (Task 4a) and the temporal/snapshot files (5A fixes); nothing is live in the root tree besides you.

STEPS:
1. Run, each to `/tmp/hippo-pa3a-review-<n>.log` with `echo EXIT $?`: (a) the PA3 CHECK line verbatim (Fake); (b) `HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_managed_pipeline_activation.py tests/unit/test_ingest_pipeline.py tests/unit/test_ingest_concurrency.py tests/unit/test_managed_source_lifecycle.py tests/unit/test_build_authority.py tests/unit/test_local_workspace_membership.py tests/unit/test_managed_source_inventory.py -q -o addopts='' -W error`; (c) `HIPPO_TEST_STORE=ladybug .venv/bin/pytest tests/unit/test_managed_pipeline_activation.py -q -o addopts='' -W error` (several minutes).
2. SPEC review; classify PROVEN (test name) / UNTESTED / VIOLATED (file:line) for every cell of the dispatch matrix (new text + actor; new eligible file + actor; either without actor; existing eligible legacy + actor via `reindex`; existing managed + actor; existing managed WITHOUT actor or open/preview actor; unsupported with and without actor; tombstoned) and for: eligibility on saved Source kind and sanitized stored filename, never content-type; exactly one ingress file with every listed refusal (missing, symlink, non-regular, changed during capture, oversized, filename escape); `FileInput` absolute path, `text.md` logical name for pasted text, `text/plain`; raw root and cache root created only by a managed build; options captured from effective config with rejection rather than truncation; progress mapped only to the four Source fields, never `meta_json`; coordinator called outside every ambient transaction with `should_stop` bound to the job key; immutable actor and operation ID captured in the closure, no token/Request/cookie/principal; every failure transition (bootstrap failure vs refresh failure vs cancellation vs tombstone observed) including that G1 pointer and counts are untouched by a failed refresh; redaction across Source rows and logs with injected secret/path/text/model-body strings; the destructive-operation spies; raw retention (the implementer used a byte-for-byte raw-root inventory comparison instead of a monkeypatched unlink; judge whether that proves the invariant).
3. QUALITY review, with attention to: `plan_dispatch` as the single classification path (no second predicate, no way to reach the legacy branch for a managed source); the double-fault redaction fix in `98aac44` (what if recording the failure itself fails?); `_refuse_if_indexing` ordering relative to actor and tombstone checks; the `already_current` refresh still moving `updated_at` (does anything key on it?); `source_dir` delegation with no import cycle; the actor closure surviving process-level job retry; whether any path can call `_clear_passages`, `delete_passages_for_source`, `delete_code_for_source`, `remove_orphans`, `Store.delete_source`, `rmtree`, generation collection or raw deletion for a managed attempt (grep the two modules and trace); whether `managed_activation` imports anything from `knowledge/dense_session.py` (the layering rule says ingest may import knowledge, not the reverse).
4. Report the verbatim signatures of `plan_dispatch`, `Dispatch`, `run_managed_build`, `map_build_failure`, `ManagedFailure`, `record_build_failure`, `record_build_receipt` and the three error classes for Tasks 3b and 4.

DONE WHEN: `ai_docs/reports/2026-09-11-pa3a-review.md` exists with `SPEC: PASS|FAIL`, `QUALITY: PASS|FAIL`, the matrix and criteria table, numbered findings with severity (blocker / major / minor), file:line, why, proposed fix, run results with log paths, and the signatures. `horch done` states both verdicts, finding counts by severity, and the report path.

CONSTRAINTS: no edits outside your report; no Neo4j; `HIPPO_TEST_STORE` explicit on every command.

REPORT: `horch note` after the runs, after the SPEC table, after the report.
