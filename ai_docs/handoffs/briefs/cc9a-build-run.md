# Brief: CC9a — shared build run, extracted from the prose coordinator (managed code capture, task 9a)

Read `ai_docs/handoffs/fleet-worker-rules.md` first. Use the worktree recipe with name `cc9a` (branch `wp/cc9a`, base = the `rag-it-all-tibs` HEAD named in the spawn message; CC1 is merged and nobody else in this wave edits `src/hippo/ingest/prose_generation.py`).

GOAL: The coordinator-side run state the plain-prose coordinator owns today (`_Run`, `BuildReceipt`, `BuildProgress`, `BuildCancelled`, `BuildBusy`, `_credentials`, `_authority_fields`) lives in `src/hippo/ingest/build_run.py` with an unchanged contract, `BuildReceipt` gains the two counters the code coordinator will fill, and the reviewed prose suite passes unchanged. Plan section 3 ("the shared `_Run` failure latch is factored into `src/hippo/ingest/build_run.py` so neither owns it") of `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`; design review M9 and M11 (`ai_docs/reports/2026-09-12-code-capture-plan-review.md`); gate CD8's last clause ("the reviewed prose coordinator suite is unchanged by the `_Run` extraction") of `ai_docs/gates/rag-it-all/task-5-code-capture/GATES.md`. Committed on `wp/cc9a`.

CONTEXT: `src/hippo/ingest/prose_generation.py` (762 lines): `BuildCancelled` `:~53`, `BuildBusy` `:~57`, `BuildProgress` `:~113`, `BuildReceipt` `:~120`, `_credentials` `:~128`, `_authority_fields` `:~132`, `_Run` `:~139-261` (guard capture, failure latch, cancellation, `check`/`_check` with the ambient-transaction reasoning in its comments, `renew`, `start`/`pause` with `LeaseHeartbeat`, `progress`, `adopt`, `close`). Everything from `_pair` `:~263` downwards is prose-specific and stays. Consumers that import these names: `grep -rn "prose_generation import\|from .prose_generation\|BuildReceipt\|BuildProgress\|BuildCancelled\|BuildBusy" src tests` — `src/hippo/ingest/managed_activation.py` (`BuildReceipt`, `BuildProgress`), `src/hippo/ingest/__init__.py` (lazy re-exports; check `tests/unit/test_layering.py` and `test_import_order.py` for the layering allowlist), the web/MCP layers and their tests. The knowledge/ingest layering rule from `ai_docs/handoffs/briefs/layering-knowledge-ingest.md`: `hippo.knowledge` never imports `hippo.ingest`; `build_run.py` lives in `ingest` and may import `knowledge.build_authority` and `knowledge.lease_heartbeat`.

PRIOR WORK: CC1 left `prose_generation.py` unmodified (its `begin_managed_source` call at `_install` `:~491` and the `+ int(not source_is_managed)` arithmetic at `:~484` are the reviewed prose contract; do not touch them). The design review confirmed no prose invariant changes in this wave.

REQUIRED BEHAVIOR:
1. NEW `src/hippo/ingest/build_run.py` holding `BuildCancelled`, `BuildBusy`, `BuildProgress`, `BuildReceipt`, `BuildRun` (the class formerly `_Run`; keep a `_Run = BuildRun` alias in `prose_generation.py` so nothing else changes), `credentials(job)` and `authority_fields(guard)` (public spellings; keep private aliases in `prose_generation.py`). Move the code verbatim including comments; the cancellation message `"Plain source build cancelled"` becomes a constructor argument or class attribute with the prose default so the prose tests see the same text. Behaviour, locking, and the heartbeat interaction are byte-for-byte the same.
2. `BuildReceipt` gains `resumed_from_batches: int = 0` and `rebaselines: int = 0` (plan section 3 and ruling 2). Prose receipts never set them; every existing equality assertion on `BuildReceipt` keeps passing because the defaults are zero. It still names no path, no text and no exception body.
3. `prose_generation.py` imports from `build_run` and re-exports the old names so `from hippo.ingest.prose_generation import BuildReceipt` keeps working; `src/hippo/ingest/__init__.py`'s lazy table gains `build_run` names only if a consumer needs them (say which).
4. NEW `tests/unit/test_build_run.py`: the run's contract in isolation — latch-once failure, cancellation via `should_stop`, `check` refusing under an ambient transaction the way `_check` does today, `renew` turning any error into `AuthorizationChanged("... lease renewal failed")` except `BuildCancelled`, `adopt` closing the previous guard, `close` pausing the heartbeat then closing the guard; the two new receipt fields default to zero and are frozen.
5. Regression: `tests/unit/test_prose_generation.py`, `test_managed_pipeline_activation.py`, `test_managed_route_activation.py`, `test_layering.py`, `test_import_order.py` pass UNCHANGED (no edit to any of them; if one imports a private name you moved, keep the alias rather than editing the test).

FILES:
  - own: NEW `src/hippo/ingest/build_run.py`, `src/hippo/ingest/prose_generation.py` (the extraction only), `src/hippo/ingest/__init__.py` (lazy table only, if needed), NEW `tests/unit/test_build_run.py`, NEW `ai_docs/gates/rag-it-all/task-5-code-capture/evidence-cc9a.md`.
  - do NOT touch: `src/hippo/knowledge/*`, `src/hippo/store/*`, `src/hippo/ingest/managed_activation.py`, `pipeline.py`, `readers.py`, `repos.py` (CC10's), any existing test, `docs/`, the checkpoint, `GATES.md`.

STEPS: worktree + venv (with the `mcp==2.1.1` pin); baseline `tests/unit/test_prose_generation.py tests/unit/test_managed_pipeline_activation.py tests/unit/test_layering.py tests/unit/test_import_order.py` green on Fake (form (b) if a listed file imports `fastapi.testclient` at module level; check); RED (`test_build_run.py` fails on the missing module); implement; GREEN Fake on the baseline set plus the new file; GREEN Ladybug on `tests/unit/test_prose_generation.py -k "reopen or publish"`; Ruff; evidence; one commit. This slice fits one worker because it moves ~150 lines verbatim and adds two defaulted fields.

DONE WHEN: green with `-W error`; every existing test unchanged; evidence written; `horch done` lists the names moved, the aliases kept, the `__init__` change if any, counts and logs.

REPORT: `horch note` per step; `horch tell orchestrator "[<role>] BLOCKED: ..."` for contract questions.
