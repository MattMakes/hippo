# PA8 sign-off review: the production activation ledger

Reviewer `architect-reviewer-18`, 2026-09-12. Root tree `/Users/mascott/projects/hippo`,
branch `rag-it-all-tibs`, HEAD **`1e2f112`** ("Merge wp/cleanup4: final Task 4 cleanup
batch"). Read-only apart from this file: no source, test, plan, checkpoint, gate ledger or
evidence document was edited. `git status` at the start and end of the review shows only
`ai_docs/handoffs/` (untracked) and this report.

**Every judgement and every run below is at `1e2f112`, the HEAD my brief named.** HEAD moved
to `c893a95` while my PA7 run was still going — see "HEAD moved under this review" at the end,
which names exactly what landed, what it does and does not change, and what the orchestrator
must re-run before recording.

Brief: `ai_docs/handoffs/briefs/review-pa8-signoff.md`. The question is narrow — can PA8's
last clause, *"independent SPEC and QUALITY reviews have no unresolved findings"*, be signed
for `ai_docs/gates/rag-it-all/task-5-production-activation/GATES.md` at this HEAD?

## Verdict

**`PA8: NOT SIGNABLE`**

Everything PA8 asks for *except* its last clause is true and re-proven at HEAD: PA1–PA7 pass,
the static line passes, the call-site sweep is attached and every one of its 58 rows is
classified, and Neo4j parity is recorded from an isolated reservation for the store lanes
PA1/PA3/PA4/PA5/PA7 owe. The blocker is the clause itself.

**30 rows are OPEN at HEAD**: 26 numbered findings from the ten activation reviews (PA3b's
findings 3 and 4 are counted once each, because wrap-up findings 17 and 15 restate them),
plus four items that carry no review number — **C1**, **S1**, **T2** and **T3** below.
**5 further items are DEFERRED** to Task 16.

By severity the 30 are **28 LOW/minor and 2 INFO — none MEDIUM or above**. None is a
correctness regression and none is a live disclosure on a client path: the three that were
(wrap-up findings 1, 2 and 3) were closed by `wp/cleanup4`. What is left is coverage gaps the
plan's own adversarial cases name, documentation that drifted behind the code, duplicated
constants, a handful of unguarded `ValueError` catches on administrative paths, and four items
that are unreachable until a later slice exists.

Two further things must be true before the ledger closes, neither of which is a review
finding:

1. **`GATES.md` still records nothing.** Its status line says "proposed; no implementation
   evidence has been recorded" and all eight checkboxes are `[ ]`. Completion-checklist item 1
   ("PA1–PA8 are implemented and **recorded** in the gate ledger") is therefore NOT met. That
   is the orchestrator's recording step, not a finding against the work.
2. **The recorded call-site audit is stale by one row.** `session-audit.md` was produced at
   `043ca51` and never refreshed; `cli.py:567 _sources_locally` is a production `query_session`
   owner at HEAD that has no row in it. The caller itself is correct and is exactly what the
   plan's "Local CLI code and source listing" bullet asked for — the document is what is
   behind, not the code. Recorded below as finding **S1**.

The cheapest route to a signature is the one `evidence-cleanup4.md:186-189` already names,
extended: the two one-line documentation fixes (wrap-up 7 and 16) plus a small batch over the
remaining LOW items, or an explicit orchestrator ruling that accepts the enumerated residue —
in which case PA8's clause should be reworded to "no unresolved findings above LOW", because
as written a LOW with a named fix is still unresolved.

## Run results

Every command was run in the root tree at `1e2f112` with `.venv/bin/python` 3.12.11 /
pytest 9.1.1, `HIPPO_TEST_STORE` explicit, `-o addopts=''`, `-W error`, captured to its own
log and read from the summary line. Nothing was piped to `tail`. macOS has no `timeout`.
**No Neo4j was used and the disposable container was never claimed by this session.** All
eight gate commands were compared against `GATES.md` as strings before use and are verbatim,
including PA6's and PA7's AnyIO filter.

**AnyIO handling.** PA1–PA5 ran the ledger lines **verbatim with a bare `-W error`** — none
of their files imports a transport at module level. PA6 carries the sanctioned command-line
**form (b)**, `-W error -W "ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning"`,
because its line names five module-level `fastapi.testclient` importers that no marker can
reach; that filter is part of the ledger's own PA6 CHECK line. PA7's ledger line also carries
form (b) and I ran it verbatim. The full-Fake suite carries form (b) for the same reason PA6
does. No marker was added, no ini-wide `filterwarnings` exists, and no application warning was
suppressed.

| Gate | Command | Result | Log |
|---|---|---|---|
| PA1 | ledger line, verbatim, Fake | **186 passed, 2 skipped — EXIT 0** | `/tmp/hippo-pa8-pa1.log` |
| PA2 | ledger line, verbatim, Fake | **93 passed, 1 skipped — EXIT 0** | `/tmp/hippo-pa8-pa2.log` |
| PA3 | ledger line, verbatim, Fake | **189 passed, 3 skipped — EXIT 0** | `/tmp/hippo-pa8-pa3.log` |
| PA4 | ledger line, verbatim, Fake | **117 passed — EXIT 0** | `/tmp/hippo-pa8-pa4.log` |
| PA5 | ledger line, verbatim, Fake | **184 passed, 2 skipped — EXIT 0** | `/tmp/hippo-pa8-pa5.log` |
| PA6 | ledger line, verbatim (form (b)), Fake | **714 passed — EXIT 0** in 86.12s | `/tmp/hippo-pa8-pa6.log` |
| PA7 | ledger line, verbatim (form (b)), **Ladybug** | **723 passed — EXIT 0** in 4592.99s (1:16:32) | `/tmp/hippo-pa8-pa7.log` |
| PA8 | ledger `ruff check` + `ruff format --check` line, verbatim | **EXIT 0** — `All checks passed!`, `128 files already formatted` | `/tmp/hippo-pa8-pa8ruff.log` |
| extra | full Fake unit suite, form (b) | **3885 passed, 28 skipped — EXIT 0** in 371.41s | `/tmp/hippo-pa8-full-fake.log` |
| extra | the PA8 call-site sweep | 58 rows | `/tmp/hippo-pa8-callsites.log` |

**PA5, PA6 and PA7 are all larger than the wrap-up review saw** (184 vs 175, 714 vs 321,
**723 vs 484**) because `4a8bbb2` widened three CHECK lines in response to that review's
finding 4. That is the single most valuable change since the wrap-up: every test of PA6's own
"no injected secrets, raw text, paths, tokens, prompts, model bodies, or arbitrary exception
strings" criterion is now inside the gate that claims it, and PA7 now proves the same web,
ingress, eval and transport surfaces on the primary backend instead of only on Fake.

PA7's 1:16:32 is not a regression: it is 239 more tests than the wrap-up's run, on Ladybug,
against an unusually busy machine — the orchestrator's gate-checker was running the identical
PA7 line concurrently (two `HIPPO_TEST_STORE=ladybug` processes on this exact file list, plus
a worker's own Ladybug suite in a worktree). `cleanup4` needed 666s for just two of PA7's four
new files on their own, so the arithmetic is consistent. Exit 0, no flakes, no reruns.

## Every finding of every activation review, re-checked against HEAD

Ten reports are in scope (the brief's CONTEXT list): **97 numbered findings**. To them I add
six rows that carry no review number — **C1** (a residual `evidence-cleanup4.md` raises about
itself), **S1** (the audit document, which I raise), and **T1–T4** (the `task4-notes.md`
bullets that the wrap-up review dispositions without closing, or that route outside the
numbered reviews). 103 rows in all.

Scope note: `2026-09-11-prose-coordinator-review.md`, the three `t5a-*` reports and the two
`temporal-*` reports are **not** activation reviews and are not in the brief's list, so their
findings are not rows here. The `task4-notes.md` bullets that route to coordinator findings
*are* rows (T1, T2), because `task4-notes.md` is in scope and the brief requires every one of
its open bullets to be either closed or explicitly deferred with an owner.

Legend — **CLOSED**: a commit or an explicit reviewer/orchestrator decision closes it, and I
verified the claim by reading the code at HEAD. **DEFERRED**: a later task named in
`docs/rag_it_all.md` owns it. **OPEN**: neither.

Line numbers in every report older than `0d4d2ec` are stale (the layering merge renumbered
`ingest/pipeline.py` and `ingest/accepted_inputs.py` and moved classes between
`hippo.knowledge` and `hippo.ingest`), so every check below was made by symbol, not by
`file:line`.

### `2026-09-11-pa1-review.md` — 8 findings, 0 OPEN

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | major | A committed tombstone does not gate `claim_generation_build`; a tombstoned source can be re-fenced and its failed generation reopened | **CLOSED** | `store/generations.py:196-200` — `self._tombstoned(gen.source_id, source)` raises before the fence advances, with the comment saying why the barrier lives there and not in the dispatcher. `test_a_tombstoned_source_cannot_reclaim_its_failed_generation` and `test_a_refused_reclaim_leaves_the_replayed_fencing_token_pinned` (`test_managed_source_lifecycle.py:303,327`) |
| 2 | minor | `delete_user` overwrote a non-local `mapping_authority` with `"local"`, destroying audit state | **CLOSED** | `store/knowledge.py:320-323` — `authority = LOCAL_MAPPING_AUTHORITY if enabled else existing.mapping_authority`, exactly the review's patch. `test_disabling_a_mapping_keeps_the_authority_that_granted_it` |
| 3 | minor | `_disable_local_workspace_memberships_locked` lacked the `_local_mapping_available()` guard | **CLOSED** | `store/knowledge.py:296-297`. `test_delete_user_on_a_pre_schema_store_has_no_mapping_to_retire` |
| 4 | minor | A user created while `_local_mapping_available()` is false is left unmapped, silently | **CLOSED by documentation** | The optional `log.warning` was *not* taken; instead `_local_mapping_available`'s docstring (`store/knowledge.py:336-343`) now states the consequence in full ("left unmapped and stays unmapped - fail-closed, no managed evidence - until the next `on_first_connection` …"). That is the review's own "no behaviour change" half, recorded rather than remembered |
| 5 | minor | "Many changes, one bump" untested | **CLOSED** | `test_many_repairs_count_every_change_and_invalidate_no_reader` (`test_local_workspace_membership.py:254`) |
| 6 | minor | Nothing asserts cooperative cancellation is requested | **CLOSED** | `test_cancellation_is_requested_before_the_transaction` asserts `calls == [(f"index:{managed.source}", False, 0)]` (`test_managed_source_lifecycle.py:345,357`) |
| 7 | minor | `UnmanagedSource` raised before any authorization check is a managed-vs-unmanaged oracle | **CLOSED** | The handoff was to make the route the boundary, and it is: `web/routes/sources.py:599` calls `manageable_source(request, source_id)` before the `pipeline.delete_source` at `:604`, with a comment saying the lifecycle service rechecks inside its own transaction |
| 8 | minor | The Neo4j lane of the membership change has no test in this tree | **CLOSED** | `neo4j-parity.md` run 1 (tree `812c60e`) runs `test_local_workspace_membership.py` on the disposable container: 313 passed, 4 skipped, exit 0 |

### `2026-09-11-pa2-review.md` — 8 findings, 4 OPEN

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | major | First successful `ping()` bumps the authorization epoch and kills a session already in flight | **CLOSED** | The plan was amended (`plan:67`, "Amended 2026-09-11 after the Task 2 review"): the public wrapper does not bump. `test_a_held_query_session_survives_the_first_ping_bootstrap` (`test_local_workspace_membership.py:271`) |
| 2 | minor | `_selected_pairs` is O(selected generations × authorized revisions) | **OPEN** | `knowledge/projection.py:307-316` still holds the `any(... for revision_id in revisions)` inner loop. The review's cheaper shape (`by_generation` index) was not applied. Latency only, no wrong output |
| 3 | minor | `reindex_all` acquires a structural graph with no owner and no heartbeat | **CLOSED** | `web/routes/sources.py:546-568` — one `query_session` owner spans the inventory, the `pipeline.reindex_all` call and the post-operation `view.validate()`, with a comment naming the old bug |
| 4 | minor | Code-edge attribution for a shared node is by node membership, not by exact pair | **OPEN (latent)** | `status.py:105-110` still counts every `CODE_EDGE_KINDS` arrow whose source vertex is in the contributed set; only the relation counter below it (`:112-116`) is gated on `pair in row.source_generations`. Neither the per-pair provenance fix nor the explanatory comment the review offered as the alternative was applied. Unreachable while a managed prose lane emits no code arrows; becomes real with managed code generations. No later task names it — `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md` addresses a *different* `status.py` predicate (§7, legacy serving), not this counter |
| 5 | minor | A relation supported by two generations is never tested | **OPEN** | Every `source_generations` assertion in the tree is a one-entry tuple (`test_managed_source_inventory.py:422`, `test_structural_loading.py:812,858`, `test_dense_session.py:448`). The multi-contributor branch of `pair in row.source_generations` is still unexecuted, and the plan's adversarial case (`plan:254`) names it |
| 6 | minor | `compose_graphs`' `populated` shortcut version is not asserted | **OPEN** | `test_compose_unions_pairs_and_refuses_two_generations_for_one_source` (`test_managed_source_inventory.py:312-322`) asserts pairs and the duplicate refusal; there is no `.version` assertion |
| 7 | minor | Canonicalisation raises bare `ValueError` where its neighbours raise `ProjectionError` | **CLOSED** | 4e decision 8. `hipporag/graph_index.py:316,319` raise `ProjectionError`; `public_errors._ROWS` already had the row; `test_an_incoherent_selection_reaches_an_mcp_code_tool_as_a_mapped_failure` |
| 8 | minor | Status and MCP identity do structural loading on every call | **CLOSED by decision** | The review's own disposition is "none required for PA2"; the cost note reached the Task 4 brief and every route owner but `reindex_all` passed its session — and `reindex_all` now holds one (finding 3). Carried forward only as a measurement item |

### `2026-09-11-pa3a-review.md` — 13 findings, 6 OPEN

Items 1, 2, 4, 6(deviation), 13 were absorbed by Task 3b and are PROVEN in that review's
"Task 3a addendum items" table; I re-checked each at HEAD.

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | major (cov) | `reindex` never exercised for an eligible legacy conversion | **CLOSED** | 3b addendum 1. `test_an_eligible_legacy_reindex_converts_atomically_and_serves_legacy_until_publish` drives `pipeline.reindex(..., build_actor=...)`; `_prepare_reindex` is monkeypatched to fail |
| 2 | major (cov) | The redaction test cannot see an exception leaked through `exc_info` | **CLOSED** | 3b addendum 2; both redaction tests now read `caplog.text` |
| 3 | minor (cov) | `reindex` never exercised for an unsupported source *with* an actor | **OPEN** | No `pipeline.reindex(ctx, <pdf>, build_actor=...)` case exists (`rg 'pipeline.reindex\('` over `test_managed_pipeline_activation.py`: every actor-bearing call is on an eligible or managed source). The same classification *is* covered through the bulk lane — the mixed-bulk test asserts `w.unsupported: "legacy"` and `prepared == [w.unsupported]` (`:1404-1414`) — so the risk is low, but the single-source entry point the review named is still unguarded |
| 4 | minor | Out-of-range stored `synonymy_threshold` reported as `operation_failed` | **CLOSED** | 3b addendum 4. `managed_activation._fraction` refuses non-numeric, non-finite and out-of-`[0,1]` as `ManagedConfigurationError`, with 6 parametrized cases plus `test_a_build_refuses_a_stored_threshold_generically_and_without_a_model_call` |
| 5 | minor (cov) | "No query, status, startup … creates these directories" proven for query and legacy ingestion only | **OPEN** | `test_the_raw_root_and_embedding_cache_appear_only_for_a_managed_build` (`:589-604`) still covers a legacy add, a legacy reindex and a citations read. Neither `on_first_connection` nor `status.source_view` — both named in plan step 3 — appears before the managed half |
| 6 | minor | `ingress_file` never cross-checks the on-disk name against the name eligibility was decided on | **OPEN** | `managed_activation.ingress_file:276` checks `entry.name != safe_stored_name(entry.name)` — that the name is *safe*, not that it *is* `stored_filename(source)`. The review's one-line fix was not applied. Defence in depth; unreachable through the current writers |
| 7 | minor | Deviation 7 overstates `reindex_all` safety | **CLOSED** | 3b addendum 6 confirms the correction and the preflight makes the statement true; the bulk now clears nothing when any managed preflight fails (`pipeline._preflight_managed`, `test_managed_pipeline_activation.py:1404`) |
| 8 | minor (cov) | The 1,000-chunk managed ceiling is never exercised | **OPEN (partial)** | The coordinator half is covered — `test_bootstrap_bound_excess_leaves_no_managed_state` parametrizes `max_chunks=1` and asserts no managed state survives (`test_prose_generation.py:302-315`). The managed lane's own half is not: nothing asserts the Source row reaches `status="failed"` with a bounded code past the ceiling, and the review's second ask (a Task 5 decision on whether the managed 1,000 should track legacy's `MAX_CHUNKS` 20,000) is nowhere on the record |
| 9 | minor | Two definitions of "managed" coexist, and the narrower one gates destruction | **OPEN (documentation)** | `managed_eligibility` (`managed_activation.py:111-124`) still keys on `source["managed"]` alone; `build_authority._source_control` still uses the broader disjunction. The review asked only for a comment naming which is the authority and why; the docstring says "Closed, and decided on the row alone" and stops there. The code-capture plan touches `context.py`/`status.py`'s Artifact-presence predicate (§7), which is a third site, not this pair |
| 10 | minor (eff) | `present()` opens a locked transaction per extraction callback | **CLOSED by decision** | The review's own disposition: "Not needed for this gate." The lock is what makes the tombstone race safe; measured fine at test scale. Carried as a measurement item only |
| 11 | minor | The embedding-spec mapping is duplicated and reaches into a private name | **OPEN** | `managed_activation.py:37,202` and `dense_session.py:14,227` both import the private `_base_name` from `hippo.ollama` and both spell `EMBED_PREFIXES.get(_base_name(...), ("", ""))`. `ollama.py:240` is a third copy. The review's fix ("whoever next owns `dense_session.py` …") had two owners since (4a, 4f) and neither took it |
| 12 | minor | A refresh interrupted by a restart leaves a permanent `refreshing: …` stage | **CLOSED** | Task 3b's restart sweep plus 4e decision 7. All three lanes read the matching ids and call `GenerationQueries.release_interrupted_build`; `test_managed_pipeline_activation.py:1100-1118` proves the Ladybug reopen reaches `("ready", "refresh_failed")` with `build_interrupted: ` on the row and citations still serving |
| 13 | minor (cov) | No test adds an eligible plain *file* without an actor | **CLOSED** | 3b addendum 3: `test_an_eligible_plain_upload_without_an_actor_stays_legacy`, 3 cases |

### `2026-09-11-pa3b-review.md` — 10 findings, 4 OPEN (2 carried into the wrap-up review)

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | MAJOR | `retrieval_rebuild_required` stored on Source rows but absent from the public code table | **CLOSED** | 4c re-review A1; `knowledge/public_errors.py:198` |
| 2 | MAJOR | `build_interrupted` is an eleventh managed code minted in the store layer, outside the closed vocabulary | **CLOSED** | 4c re-review A2; `public_errors.py:194` maps it to `operation_failed`/500, and the reasoning against 409 is recorded (`task4-notes.md:51`) |
| 3 | MINOR | The actorless refusal distinguishes a live managed source from a tombstoned one, and calls a delete a rebuild | **CLOSED (oracle) / OPEN (wording, = wrap-up 17)** | The oracle half is closed at the boundary: `ManagedActorRequired` is deliberately uncaught and reaches `web/app.py`'s fixed permission body, so no client learns the difference. The wording is unchanged — `managed_activation.py:176` still raises `"A managed source cannot be rebuilt without a build actor"` on the delete path |
| 4 | MINOR | `reindex_all`'s submission loop can raise after the legacy clears have committed | **CLOSED (one cause) / OPEN (the class, = wrap-up 15)** | `pipeline._submit_lane` (`:634`) contains `ManagedDispatchError` at `:646` with a bounded log line, which is 4e decision 7. A `Busy` or a store failure from `start_indexing` still propagates and leaves the lanes ordered after it cleared and jobless |
| 5 | MAJOR | `reindex_all` returns `0` for both "nothing to do" and "refused" | **CLOSED** | 4e decision 5 plus the plan amendment (`plan:133`). `ManagedPreflightRefused` -> `409 {"error": "Bulk reindex refused", "code": "bulk_refused"}` at `sources.py:555-561`; `test_a_bulk_whose_managed_preflight_refuses_answers_a_closed_refusal_code` |
| 6 | MINOR | Ladybug interpolates the sweep constant into Cypher where Neo4j parameterizes it | **OPEN** | `store/ladybug.py:716` still builds `f"n.status = 'ready' AND n.stage STARTS WITH '{REFRESHING_PREFIX}'"` and splices it into three query strings; `store/memory.py` passes `$prefix`. Nothing is injectable today; the two backends express one predicate two ways. The reviewer recorded it without blocking |
| 7 | MINOR | The sweep retires the stage but leaves the orphaned build holder | **CLOSED** | 4e decision 7: `GenerationQueries.release_interrupted_build` (`generations.py:89`) is called by all three lanes after they read the matching ids |
| 8 | MINOR | The restart-sweep tests do not hold the "nothing else moved" half | **OPEN** | `test_managed_pipeline_activation.py:1100-1118` asserts status, stage, error prefix, active pointer and that citations serve. There is still no `before = row_of(...)` / "equal except stage, error, updated_at" comparison, so stale `progress_done`/`progress_total` survive unasserted. `row_of`/`rows_of` helpers exist two functions below and are used by the delete tests |
| 9 | MINOR | Managed-lane domain vocabulary lives in the Neo4j backend module | **CLOSED** | 4e decision 7 moved `REFRESHING_PREFIX`, `INTERRUPTED_REFRESH_STAGE`, `INTERRUPTED_REFRESH_ERROR` (plus a new `INTERRUPTED_REFRESH_CODE`) to `store/generations.py:26-30`; `ladybug.py:85-92` and the Fake import from there. The `managed_activation.STAGES[0]` half of the same drift is still open — wrap-up finding 8 |
| 10 | MINOR/nit | A vanished row skips the identity validation decision 6 promises | **CLOSED** | 4e decision 7: `check_operation_id` at `pipeline.py:553`, *before* `get_source` at `:554` |

### `2026-09-11-pa4b1-review.md` — 8 findings, 0 OPEN

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| F1 | MEDIUM | Six page routes answer a browser with raw JSON on a mapped failure | **CLOSED** | The 4e addendum: `render.wants_html:106`, `templates/failure.html`, `web/app.py:144` `public_failure_page(request, failure, "failure.html")` behind the `wants_html` branch, with the handler's docstring naming `/partials/sources` as the sharpest case |
| F2 | MEDIUM | The ask path's traceback is DEBUG-only and the sentence names an operation ID nothing logs | **CLOSED by decision** | `pages.failure_operation_id` supplies the id; the traceback level is an explicit recorded decision (`task4-notes.md:74`, wrap-up disposition of line 68): class only at INFO/WARNING, traceback at DEBUG under the same id. Both the wrap-up reviewer and 4b-i's redaction test back it. Do not "fix" it with `exc_info=True` |
| F3 | MEDIUM | Seven `sources.py` JSON failure bodies carry no `code` | **CLOSED** | The seven coded bodies exist; `test_every_json_failure_body_the_source_routes_answer_carries_its_code` |
| F4 | LOW | Two different `public_failure_page` functions, same name, adjacent modules | **CLOSED** | `web/app.py`'s is `public_failure_handler`; `test_the_app_handler_and_the_page_renderer_do_not_share_a_name` pins it, and `app.public_failure_page is render.public_failure_page` |
| F5 | LOW | `add_text`/`add_upload` catch `ValueError` broadly enough to report a managed dispatch error as a caller error | **CLOSED** | `sources.py:521-530` — `if not caller_error(exc): raise` / `return public_failure_response(retrieval_failure(exc))` ahead of every `coded_response(str(exc), INVALID_SOURCE, 400)` |
| F6 | LOW | `delete_source` maps every `AuthorizationChanged` to 404, including a genuine mid-request revocation | **CLOSED as an accepted deviation** | `sources.py:607-610`, with the comment stating the non-disclosure reason ("an unavailable source and a suppressed one answer the same way"). Recorded as accepted in `task4-notes.md:33` |
| F7 | INFO | A refused bulk preflight is still a silent `200 {"accepted": true}` | **CLOSED** | 4e decision 5, `bulk_refused` |
| F8 | INFO | The implementer's open finding, with a line-number correction | **CLOSED** | `wp/cleanup4` item 6 corrected `evidence-pa4b1.md` to `graph_index.py:316,319` and recorded why the brief's `:297,300` was itself stale |

### `2026-09-11-pa4b2-review.md` — 10 findings, 2 OPEN

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | MEDIUM | An activation failure at acquisition escapes the graph-browse JSON routes unmapped (SPEC row g VIOLATED) | **CLOSED** | `web/routes/graph.py` maps at `:217-220` (page), `:354-357`, `:391-398`, `:402-404`, `:408-411`, `:557-558` — every route through `retrieval_failure` + `public_failure_response`/`public_failure_page`, plus the global handler at `app.py:144` |
| 2 | LOW | `_answer`'s `except ValueError` shadows the public mapper by type, not by intent | **CLOSED** | `web/routes/code.py:204` catches `(ValueError, OllamaError, httpx.TransportError)` and `:225` applies the exact-type `caller_error` guard (4e decision 2) |
| 3 | LOW | `code.py`'s catch tuple omits `httpx.TransportError` | **CLOSED** | `code.py:35` imports `httpx`; `:204` includes it |
| 4 | LOW | `light_up`'s pre-validation duplicates `query_session`'s settings merge verbatim | **CLOSED** | 4e decision 6: `query_access.effective_settings:104`, used at `query_access.py:135` and `graph.py:388`; `test_effective_settings_is_the_stored_knobs_under_the_callers_own` asserts function identity |
| 5 | LOW | The 400 pre-validation can quote an operator's stored setting and blame the caller | **CLOSED** | `graph.py:391-398` — the `caller_error` guard `wp/cleanup4` added (wrap-up finding 10), with `test_light_up_maps_a_settings_check_that_is_not_the_validators_own_refusal` and `test_light_up_still_names_the_knob_the_caller_sent` |
| 6 | LOW | The HTML analyze form answers HTTP 200 for every mapped failure | **CLOSED** | `web/routes/analyze.py:122` renders with `status_code=failure.http_status` |
| 7 | LOW (latent) | `retrieval_failure`'s total fallback would swallow an authorization denial if `_simulate` called an unguarded `EvalAccess` method | **OPEN (latent)** | The live half in the opposite direction (`evals.py:373`) was the wrap-up's finding 2 and `wp/cleanup4` closed it. The `_simulate` half is unchanged and still unreachable; no test or guard pins it |
| 8 | LOW | A preview audience proves no managed evidence, so tier preview reads as empty | **CLOSED** | 4e decision 3: `graph.py:157-159` `PREVIEW_NOTICE` (a fixed sentence, no interpolation), `graph.html:13` in the actor's page head, `knowledge/access.py` untouched, `test_a_preview_of_a_workspace_with_managed_evidence_says_it_shows_legacy_only` |
| 9 | LOW | The Group D fix reaches for a private `AppContext` method | **OPEN** | `tests/unit/test_query_authorization_boundary.py:225,232` **and now also** `tests/unit/test_query_snapshots.py:202,211` monkeypatch `ctx._build_structural_graph`. The review's "add a public seam when `context.py` is next owned" has not happened, and a second test file has since adopted the private name |
| 10 | INFO | Two out-of-scope sites the 4b-i merge has to reconcile | **CLOSED** | Confirmed closed by 4b-i (`task4-notes.md:32`) |

### `2026-09-11-pa4c-review.md` (SPEC FAIL / QUALITY FAIL) — 9 findings, 0 OPEN

All nine were re-verified CORRECT by `2026-09-11-pa4c-rereview.md`'s findings table, together
with the three addendum items A1–A3. I spot-checked the two with the widest blast radius:
`mcp_server.py:340` `_answering()` is entered *before* `_code_graph` in every code tool
(`with _answering(), _code_graph(...)` at `:458,465,474,486`), so an acquisition failure is
mapped (F2); and the single denial code is spelled once per module with
`test_the_three_copies_of_the_denial_code_are_the_same_string` pinning them equal (F6).

| # | Claim | Status |
|---|---|---|
| F1 | `hippo ask` against a running server printed the model's raw reply body | **CLOSED** (`remote.py:131-154`; ruled deviation (B) keeps a 4xx `detail`) |
| F2 | The four MCP code tools bypassed the failure mapper | **CLOSED** (`mcp_server.py:443-454`, `:458,465,474,486`) |
| F3 | `hippo index` printed the legacy lane's `str(exc)` verbatim | **CLOSED** (`cli._stored_error`, both print sites) |
| F4 | Gated `hippo index` published to everyone and left its creator unable to manage it | **CLOSED** (both the file and git-URL branches) |
| F5 | A bare `ValueError` read one way over MCP and another on the CLI | **CLOSED** (exact-type rule, identical on both) |
| F6 | An authorization denial read three different ways on three surfaces | **CLOSED** (one sentence, one code, four surfaces) |
| F7 | MCP `hippo_sources` did not validate its view | **CLOSED** (`mcp_server.py:561`) |
| F8 | `RemoteAmbiguous` could carry an empty message | **CLOSED** (`remote.py:47,126` + test) |
| F9 | `cli._refusal`'s docstring contradicted its branch order | **CLOSED** (docstring amended) |

### `2026-09-11-pa4c-rereview.md` — 4 new findings (N1–N4), 0 OPEN

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| N1 | Low | `public_errors.py`'s table comment misnames "the last two rows"; the test mirror says eleven against a twelve-entry dict | **CLOSED** | `wp/cleanup4` item 5: the comment now names the two rows (`public_errors.py:144-148`) and the test mirror names the twelfth |
| N2 | Low | `_index_remotely`'s stored-error print has no test | **CLOSED** | `test_remote_index_applies_the_same_stored_error_rule_as_the_local_one` |
| N3 | Low | The git-URL branch of `cmd_index`'s identity rule has no test | **CLOSED** | `test_a_gated_local_index_is_owned_by_its_creator_and_kept_to_their_tier[git_url]` |
| N4 | Low | Nothing pins the `hippo --help` import footprint | **CLOSED** | `test_hippo_help_imports_no_serving_machinery` (five absences, not a count) |

### `2026-09-11-pa4d-review.md` — 7 findings, 1 OPEN

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | minor | `ask._dispatch` is imported privately and its borrow rules differ from the public ones | **CLOSED** | 4f decision 2. `rg '_dispatch\b' src/hippo` finds no definition; `dense_session._session:181-200` owns the rule; `test_no_module_reaches_dense_dispatch_through_a_private_helper` asserts `not hasattr(ask_module, "_dispatch")` |
| 2 | minor | `changeset_access.py:170` loads a full unrestricted graph inside the locked mutation transaction | **OPEN** | `native_ids = set(self.ctx.graph().node_ids)` is unchanged and is the only `ctx.graph()` left in `src/hippo`. The review proved it cannot influence a response or a saved record, so it is minor — but neither proposed fix (a narrow store predicate before `transaction()` opens, or carrying the distinction on the held view) was taken, and `rg 'Managed evidence requires typed changes'` still finds the string only at its `raise`: **no test makes the guard fire** |
| 3 | minor | An evaluation run's retrieval failure has no public surface at all | **CLOSED** | 4f decision 1 plus `a4595ee`'s two status partials. `eval_access.failure_code_of` reads back only a member of `_PUBLIC_CODES`; `errors` is first in `SUMMARY_CARDS`; `test_a_run_that_could_not_route_shows_a_count_and_a_public_reason` |
| 4 | minor | The fact-free question-maker corpus narrows the test to the single-hop path | **CLOSED** | 4f decision 4, re-confirmed unchanged by `wp/cleanup4` item 7 (`shared_entity_pairs` non-empty; a prompt carrying two passages' originals; `{"code","commit"} <= kinds`) |
| 5 | minor | The static RAG-all evaluator's dispatch mode is unobservable by the shared recorder | **CLOSED** | 4f decision 5 at `a4595ee`: `rag_all.py:24` imports the module and calls through it at `:388`; the dispatch-mode test landed with the same commit |
| 6 | nit | `task4-notes.md:29` misstates which modules import `ask._dispatch` | **CLOSED (moot)** | `_dispatch` no longer exists; the sentence describes a symbol the tree does not have, and the wrap-up review's disposition of that line records the promotion |
| 7 | nit | `runner.py:209` logs the question text and the full exception | **CLOSED** | 4f decision 3: `runner.py:230` logs `question=<id> code=<code> exception=<class>` with no `exc_info`; `test_a_failing_question_logs_its_id_and_code_but_neither_its_text_nor_the_exception` |

### `2026-09-12-pa4-wrapup-review.md` — 20 findings, 11 OPEN

`wp/cleanup4` (merged at HEAD as `1e2f112`) closed 1, 2, 3, 10, 12, 14 and 19; `4a8bbb2`
closed 4 by widening the CHECK lines; 5 resolved itself during that review. The eleven below
are what is left, and I verified every one of them at HEAD.

| # | Sev | Claim | Status | Verified at HEAD |
|---|---|---|---|---|
| 1 | MEDIUM | `add_repo` answers a 400 carrying an absolute server path and git's raw stderr | **CLOSED** | `wp/cleanup4` item 1, with a correction to the finding's premise recorded in `evidence-cleanup4.md:53-91`: the route could only ever answer the git-URL hint, but the real leak on the Source row was closed at its source. `ingest/repos._explain_git_failure` drops `git said: {last_line}` (full stderr to `log.warning`); `sources.add_repo` gains the exact-`RepoError` guard. `test_clone_explains_git_failures`, `test_only_the_exact_repo_error_is_printed_at_the_repo_route` |
| 2 | MEDIUM | `/api/evals/.../run` picks its status by substring and prints any `ValueError` subclass | **CLOSED** | `wp/cleanup4`: `except EvalAccessDenied` → 404 by exact type, then `caller_error`-guarded `ValueError`, else the mapper. The substring test is gone. Three named tests |
| 3 | MEDIUM | The question maker logs the model's reply body | **CLOSED** | `wp/cleanup4` item 1b: both per-passage skips log id + closed code + exception class. `test_a_skipped_passage_logs_its_id_and_code_but_never_the_models_reply` |
| 4 | MEDIUM | No PA CHECK line runs the files holding most of Task 4's acceptance tests | **CLOSED** | `4a8bbb2` widened three CHECK lines, one more than the finding asked for. PA5: 4 files -> 5 (`test_ingest_limits.py`). PA6: 12 -> 20 (`test_managed_web_surfaces.py`, `test_managed_web_ingress.py`, `test_graph_surface_access.py`, `test_web_library_evals.py`, `test_managed_eval_activation.py`, `test_evals_question_maker.py`, `test_managed_transport_activation.py`, `test_public_errors.py`). **PA7: 11 -> 15** (`test_managed_web_surfaces.py`, `test_managed_web_ingress.py`, `test_managed_eval_activation.py`, `test_managed_transport_activation.py`), which the finding did not ask for and which is the right call -- those four hold the surfaces PA7's own "no real model credentials or network are required" criterion covers. My runs confirm the widening bites: PA5 is 184 passed where the wrap-up saw 175, PA6 is **714** where it saw 321, and PA7 is materially longer than the wrap-up's 484 in 2369s |
| 5 | (resolved) | Neo4j parity for the rewritten restart sweep | **CLOSED** | `neo4j-parity.md` run 3 (tree `65fbcab`), plus the currency note `4a8bbb2` added, which is the sentence the wrap-up's "what I would do next" item 3 asked for |
| 6 | LOW | `CLOSED_INPUT_VALIDATORS` is a copy and the evidence claims it cannot drift | **OPEN** | `ingest/pipeline.py:316` still hand-lists the five classes; `rg CLOSED_INPUT_VALIDATORS tests/` finds nothing. Adding a sixth closed validator to `public_errors` still silently leaves it unbounded on the Source row |
| 7 | LOW | A stale pointer left by the constant move | **OPEN** | `knowledge/public_errors.py:146` still says `build_interrupted` comes from "(`store/memory.py`, `INTERRUPTED_REFRESH_ERROR`)". The constant is at `store/generations.py:29`. One word. `evidence-cleanup4.md:175-178` explicitly records it as left open and unowned |
| 8 | LOW | `STAGES[0]` duplicates `INTERRUPTED_REFRESH_STAGE` | **OPEN** | `ingest/managed_activation.py:67` is still `STAGES = ("refresh_failed", …)` while `store/generations.py:27` owns the constant — and `managed_activation.py:38` already imports `REFRESHING_PREFIX` from exactly there, so the one-line fix has no layering cost |
| 9 | LOW | The bulk's `Busy` branch skips the revocation check its siblings make | **OPEN** | `web/routes/sources.py:553-554` returns the 409 with no `view.validate()`; the `ManagedPreflightRefused` branch validates at `:560` before its `:561` refusal and the success path validates at `:565` before the `:568` acknowledgement |
| 10 | LOW | `graph.py:386` is the last isinstance 4xx catch in the web layer | **CLOSED** | `wp/cleanup4`: the `caller_error` guard is in place (`graph.py:391-398`) with two tests |
| 11 | LOW | The preview notice's predicate reads the unfiltered inventory | **OPEN** | `graph.managed_evidence_exists:162-174` still iterates `ctx.store.list_sources()`. Its docstring gained an explanation of *why the notice is conditioned on the workspace* and of the tombstone rule, but not the sentence the review asked for — that this is workspace metadata for a manager rather than an audience read. Only a `manage_users`/`manage_roles` holder reaches it and the notice interpolates nothing, so nothing is disclosed |
| 12 | LOW | `authorization_changed` is still spelled in three modules | **CLOSED by decision** | `wp/cleanup4` item 4 took the review's own second option: no consolidation (the `hippo --help` import constraint stands), both halves pinned by `test_the_three_copies_of_the_denial_code_are_the_same_string` and `test_hippo_help_imports_no_serving_machinery` |
| 13 | LOW | The promoted rule refuses `access` beside `session`, but its callers still drop it silently | **OPEN (documented, untested)** | `evals/runner.py:159-167` now carries the invariant as a comment ("a borrowed session already carries the audience it was proved for, and the dispatcher refuses to be handed both"), which is half of the review's second option. The other half — one test pinning that a caller must not hand over a session proved for a different audience — does not exist, and no assert was added at the top of `run_question`. Unreachable in production |
| 14 | LOW | "No readable text" is gone from the Library page for one missing word | **CLOSED** | `wp/cleanup4` item 2: `pipeline` raises `ReadError("no readable text was found in this source")` and `test_a_source_with_no_text_fails_with_a_message` asserts the restored sentence |
| 15 | LOW | Per-lane containment covers one cause, not the class | **OPEN** | `pipeline._submit_lane` (`ingest/pipeline.py:634`) still catches only `managed_activation.ManagedDispatchError` at `:646`. Neither remedy was taken: the catch was not widened to `except Exception`, and while `_submit_lane`'s own docstring is accurate about what the code does, the broader claim in `evidence-pa3b.md` was not narrowed |
| 16 | INFO | `evidence-pa4e.md` residual 6 is stale | **OPEN** | `evidence-pa4e.md:438` still says `evidence-pa4cfix.md` "does not exist anywhere in the tree". It does, 457 lines. `evidence-cleanup4.md:179-182` records it as left open and unowned because that file was not in the cleanup brief's FILES |
| 17 | INFO | `ManagedActorRequired`'s message says "rebuilt" on the delete path | **OPEN** | `ingest/managed_activation.py:176`, unchanged. Reaches a client only through `app.py`'s fixed permission body, so it is a wording bug in the exception and the local log |
| 18 | LOW | Six unguarded isinstance `ValueError` catches remain on the user, role and account paths | **OPEN (deliberately)** | `web/routes/users.py:337,363,403,434,457` and `web/auth.py:382`, plus the two `getattr(exc, "detail", str(exc))` sites at `users.py:228,250`. Deferred by the review and again by the orchestrator's ruling (`evidence-cleanup4.md:12-13,173-174`) to "whoever next owns `users.py`" — which is **not a task**, so this is OPEN, not DEFERRED. Bounded today; a hardening |
| 19 | LOW | `analyze.py` and `graph.py` bind `retrieval_session` at import | **CLOSED** | `wp/cleanup4` item 3: both late-bind through `dense_session` (`analyze.py:312`, `graph.py:406`), and `test_managed_web_surfaces.watch` collapsed onto the single patch point. `test_every_dispatching_surface_reaches_the_rule_through_the_module` |
| 20 | LOW | `/partials/sources` answers a whole page on a mapped failure | **OPEN** | There is no `partials/failure.html`; `templates/partials/` holds eight files and none of them is one, and `app.py:144` renders the full `failure.html` for every HTML-wanting caller. An htmx swap that expected a table fragment receives a full document |

### Two items that are not in any review's numbered list

| # | Sev | Claim | Status | Detail |
|---|---|---|---|---|
| C1 | LOW | `pages.py`'s settings-form `str(exc)` is protected in the plan's sense but unguarded in the code's | **OPEN** | Raised by `evidence-cleanup4.md:153-160` as a new residual. `web/routes/pages.py:226` renders `error=str(exc)` from a bare `except ValueError` around `ctx.store.update_settings(...)`, so a `ValueError` *subclass* raised by the write would be rendered into the page — the exact shape `test_a_settings_write_that_fails_is_mapped_rather_than_quoted_back_as_a_400` guards on the JSON twin. One `caller_error` line closes it; it belongs with finding 18's batch |
| S1 | LOW | The recorded call-site audit is one row behind HEAD | **OPEN** | `session-audit.md` was produced at `043ca51` and is unchanged. `cli.py:567 _sources_locally` holds a production `query_session` at HEAD and has no row in it (at `043ca51` it used a raw `list_sources()` and so matched no `rg` row). Its class is unambiguous — local-CLI source-listing presentation on one structural session, which is precisely what the plan's "Local CLI code and source listing" bullet required — so this is a document-currency gap, not an unclassified caller. See the full re-classification below |

### `task4-notes.md` — four bullets the numbered reviews do not carry

| # | Line | Item | Status | Verified at HEAD |
|---|---|---|---|---|
| T1 | 118 | Per-thread transaction ownership (coordinator review finding 3) | **CLOSED** | Merged; `tests/unit/test_transaction_ownership.py` is in `neo4j-parity.md` run 1 |
| T2 | 119 | `build_authority` full-scans the Artifact and Generation tables 2-3x per progress tick (the 148x Fake-vs-Ladybug gap); `PlainBuildOptions` lacks the plan's pipeline/parser/materializer version fields | **OPEN** | The wrap-up's disposition is "stays open for a later increment"; no increment is named |
| T3 | 91, 96 | Verified dense over HTTP by a reader audience, **and** revocation on the verified lane, are both untested | **OPEN** | The wrap-up judged the `legacy_unknown` fixture blocker "probably stale" and reclassified this as "a small test task, not a fixture-building task". I checked both halves of that judgement. The blocker **is** stale -- `ingest/prose_generation.py:284` stamps `origin="local_curated"` and `knowledge/access.py:383` accepts exactly that origin, so a coordinator-built fixture would reach these routes over HTTP. But the test task was not taken: `test_managed_web_surfaces.py:191-200` still records, in the fixture's own docstring, that "the verified lane reaches these routes as a direct call rather than over HTTP", and neither `wp/cleanup4` nor `42450da`'s 221-line addition to that same file changed it. No owner, so OPEN rather than DEFERRED |
| T4 | 97 | Narrowing a managed row's `AuthorizationChanged` rendering belongs in `_MANAGED_CODES` | **CLOSED by decision** | `knowledge/public_errors.py:196` maps it to `None`, so the generic permission response owns it. The wrap-up's "open by design, no change needed unless the row presentation changes" is the right reading, and nothing since has changed that presentation |

Every other `task4-notes.md` bullet is dispositioned in the wrap-up review's own line-by-line
table (`2026-09-12-pa4-wrapup-review.md:231-281`). I re-checked the five it routes to Task 16
(the DEFERRED set below), the two above that route outside the numbered reviews, and T3/T4,
which the wrap-up dispositioned without closing. I agree with its remaining dispositions.

## DEFERRED items — 5, all to Task 16

`docs/rag_it_all.md:1525` defines **Task 16 — Update/delete recovery, performance and release
evaluation**, with gates G16 and G17, so "defer to Task 16" names a real owner.

| Item | Source | Why it is Task 16's |
|---|---|---|
| A gated `hippo index` sets no `owner_id`/`access_role_id` for CLI-created sources | `task4-notes.md:59` | An open contract question; "likely yes" is not a decision. Nothing merged changes it |
| `add_repo` takes no `build_actor`, so a gated `hippo index <git-url>` stays legacy | `task4-notes.md:60` | Open *by design* — repositories are legacy by plan (`plan:46`). Release-note material |
| A gated `hippo index note.md` can report `model_unavailable` where legacy succeeded | `task4-notes.md:61` | Intended consequence of activating the reader actor. Release-note material |
| `answer_withheld` conflates a withheld answer with a failure that saved a trace | `evidence-pa4f.md`, wrap-up line 76 | A contract question about evaluation semantics, not wrap-up work |
| `summarize`'s gold-means rule lets `accuracy: None` sit beside `errors: N` | `evidence-pa4f.md`, wrap-up line 76 | Pre-existing and correct; release-note material |

**One caveat on the deferrals.** Three of the five (the `hippo index` ownership question,
`add_repo`, and the `model_unavailable` behaviour change) are recorded only in the wrap-up
review and in `task4-notes.md`, which is the orchestrator's scratch file and explicitly labels
itself as such. The wrap-up's own disposition of line 61 says the `model_unavailable` note
belongs in "Task 16 release notes **and the plan's rollout section**" — and the plan's rollout
section (`rag-it-all-task-5-production-activation.md:260-264`) does not mention it. A
three-line addition to that section would move all three from "a reviewer said so" to
"the plan assigns it", which is what the PA8 criterion actually wants of a deferral.

## The call-site audit, re-run and re-classified at HEAD

PA8's CRITERIA requires that `rg -n 'query_session\(|query_access\(|graph_for\(|ctx\.graph\(' src/hippo`
"is attached to review with every production call classified". I re-ran it at `1e2f112`:
**58 rows**, saved at `/tmp/hippo-pa8-callsites.log`. I then resolved each row to its
enclosing symbol and compared the result with `session-audit.md` (produced at `043ca51`,
68 rows).

**Result: 57 of 58 rows are classified by the recorded audit; one is not.**

| Class | Rows | Note |
|---|---|---|
| Not a call — definitions, internal plumbing, one docstring | 11 | `query_access.py:75,85,87,126,136`, `context.py:8,117,129,170,180,201` |
| The dispatcher's own acquisition | 1 | `dense_session.py:201 _session` |
| Borrow-or-acquire | 3 | `status.py:47 source_view`, `status.py:263 _audience_inventory`, `render.py:185 render` |
| Production `query_session` owners, one session per operation | 42 | routes, MCP tools, CLI commands, pages and access services |
| Raw unrestricted `ctx.graph()` on a request path | 1 | `changeset_access.py:170 apply` — see PA4d finding 2 |

Three things changed since the audit was written, and all three are the *intended* direction:

- **`ask.py`, `analysis/simulate.py` and `evals/rag_all.py` no longer appear**, because they
  now dispatch through `dense_session.retrieval_session`. The supplementary sweep
  (`rg 'retrieval_session\(|dense_session\(' src/hippo`) returns the two definitions plus six
  production callers — `ask.py:68`, `evals/runner.py:164`, `analysis/simulate.py:160`,
  `evals/rag_all.py:388`, `web/routes/analyze.py:312`, `web/routes/graph.py:406` — where the
  audit's baseline fact was "no production caller of `retrieval_session` or `dense_session`
  anywhere under `src/hippo`".
- **`sources.py reindex_all` now has a row.** The audit listed it in bold *because* it had
  none ("no session today"); at `sources.py:545` it holds one owner across the inventory,
  the pipeline call and the post-operation `validate()`. That closes PA2 finding 3 and
  `task4-notes.md:135`.
- **`code.py:10` and `graph.py:6`'s module docstrings no longer match the pattern**, so the
  audit's two "stale after 4b converts the route" rows resolved themselves.

The **one unclassified row is `cli.py:567 _sources_locally`** (finding **S1**). Its sibling
`cli.py:451 _code_locally` is in the audit's section 4 (it matched at `043ca51` through
`ctx.graph()`); `_sources_locally` did not match then because it read `list_sources()`
directly, which is exactly the state the plan's "Local CLI code and source listing" bullet
told Task 4c to replace. It has been replaced, correctly — `rg '\bctx\.graph\(\)'` now returns
`changeset_access.py:170` alone. So the *caller* is right and the *audit document* is one row
behind. Completion-checklist item 2 ("no unclassified production caller") is true of the code
at HEAD and false of the recorded artifact; refreshing `session-audit.md` at HEAD, or
appending the one row with its class, closes it.

## The plan's "Completion checklist", item by item

`ai_docs/plans/rag-it-all-task-5-production-activation.md:266-274`.

| # | Item | Verdict | What holds it, or what is missing |
|---|---|---|---|
| 1 | PA1–PA8 are implemented and recorded in the gate ledger | **IN PROGRESS** | Implemented and passing — every gate is green at `1e2f112` in my own runs below. Recording was **not** done at `1e2f112` (status line "proposed; no implementation evidence has been recorded", all eight boxes `[ ]`), and is **under way now**: the orchestrator's gate-checker has an uncommitted `GATES.md` in the working tree with PA1–PA6 ticked and `EVIDENCE:` lines whose counts match mine exactly (189, 117, 184, 714). One gap against the ledger's own "Evidence recording format" (`:55-57`), which asks for exit code, shell, working directory, **source revision**, test count and concise output: those lines carry `path=b6bf9549d64b/37 entries` where the source revision should be. With HEAD now past the tree those runs saw, a revision-less `EVIDENCE:` line cannot be audited afterwards. Not a finding against the implementation, and not mine to edit (`fleet-worker-rules.md:21`) |
| 2 | The low-level graph/session call-site audit has no unclassified production caller | **PROVEN in code, NOT in the artifact** | All 58 rows at HEAD classify (table above). `session-audit.md` is at `043ca51` and lacks the `cli.py:567 _sources_locally` row — finding S1 |
| 3 | Managed destructive-operation spies prove zero source-wide cleanup and zero raw/file removal | **PROVEN** | The ledger's "Mandatory negative evidence" list is met in full. `test_managed_pipeline_activation.py:958-967` and `:1191-1218` monkeypatch `delete_source`, `delete_passages_for_source`, `delete_code_nodes_for_source`, `remove_orphans`, `discard_generation`, `collect_generation`, `pipeline._clear_passages`, `shutil.rmtree`, and `unlink`/`remove`/`rmdir` (through `os`, so `Path.unlink`/`Path.rmdir` are covered) to raise. `test_managed_source_lifecycle.py:139` does the same for the tombstone path, with `:127` asserting retained history, raw references and unrelated sources. PA3/PA4/PA5 all green |
| 4 | Empty-generation visibility and multi-source provenance counts pass on Fake and Ladybug | **PROVEN** | Fake: PA2 (93 passed) — `test_exact_empty_generation_is_visible_with_control_presentation_and_zero_counts`, `test_allowed_reader_sees_the_empty_source_and_a_denied_reader_does_not`, `test_empty_publication_changes_the_audience_view_fingerprint`, `test_empty_selection_resolves_no_profile_and_performs_no_model_request`. Ladybug: PA7 runs the same file, and `test_empty_source_visibility_survives_a_ladybug_close_and_reopen` is the reopen half. Multi-source provenance: the shared-object and relation-support cases in the same file. **One residue**: the *two-contributor* relation case is still untested (PA2 finding 5), and the plan's adversarial case at `:254` names it |
| 5 | Failure redaction passes across Source rows, logs, HTTP, MCP, remote CLI and local CLI | **PROVEN** | This is the clause the widened PA6 line now actually reaches (wrap-up finding 4): 714 passed at HEAD across `test_managed_transport_activation.py`, `test_managed_web_surfaces.py`, `test_managed_web_ingress.py`, `test_graph_surface_access.py`, `test_public_errors.py`, `test_mcp_server.py`, `test_mcp_http.py`, `test_cli.py` and the rest. `test_every_str_exc_in_the_web_layer_is_one_the_review_classified` makes the classification itself a regression test in both directions. Source rows are bounded by `pipeline._legacy_failure` + `CLOSED_INPUT_VALIDATORS`; logs by the class-only rule; HTTP/MCP/CLI by one `PublicFailure` vocabulary |
| 6 | Ladybug close/reopen tests pass; Neo4j parity evidence is recorded from an isolated reservation | **PROVEN** | PA7 at HEAD (below). `neo4j-parity.md` records three runs on the disposable container `hippo-rag-test-b780ab5`, no host bind mounts, one pytest process at a time: run 1 (313 passed, 4 skipped) for store/inventory/membership/tombstone/dispatch, run 2 (160 passed) for temporal/snapshot/generation, run 3 (16 passed, 2 skipped, tree `65fbcab`) for the rewritten restart sweep and managed bulk/tombstone dispatch. I re-checked currency rather than trusting the note: `git diff 65fbcab..HEAD --stat -- src/hippo/store` returns **nothing**, so every store file Neo4j executes is byte-identical between run 3's tree and HEAD -- even though `src/hippo` as a whole moved by 20 files, +691/-396, over the same range (the layering merge, cleanup4, and the 4f completion). The currency note `4a8bbb2` added to the file states the same conclusion |
| 7 | Independent SPEC and QUALITY review have no unresolved findings | **NOT** | 27 OPEN findings, listed above. This is the one that blocks PA8 |

## "Invariants that block the activation" (1–10)

`plan:196-209` — "the implementation is incomplete if any of these is false".

| # | Invariant | Verdict | Proof |
|---|---|---|---|
| 1 | A managed source never enters `_clear_passages`, `Store.delete_source`, legacy source cleanup, orphan sweeping, or source-directory removal | **PROVEN** | The spy set in checklist item 3, plus `legacy_source_cleanup` as the store-level backstop and `plan_dispatch` raising `ManagedActorRequired` before it can ever return `"legacy"` for a managed source. PA3/PA4/PA5 green |
| 2 | Current suppression commits before delete returns and invalidates in-flight current sessions; published/retired history and raw bytes remain untouched | **PROVEN** | `test_tombstone_commits_suppression_fence_and_presentation_together`, `test_held_current_session_fails_and_a_new_session_excludes_the_source`, `test_retained_history_raw_references_and_other_sources_are_untouched`, `test_saved_ingress_and_query_snapshots_survive_the_tombstone`, `test_tombstone_does_not_wait_for_a_blocked_model_callback`, `test_only_the_active_unpublished_build_is_failed_and_cancelled` (`test_managed_source_lifecycle.py`), and the reopen half at `:447` |
| 3 | G1 serves during G2 refresh and after G2 failure; only atomic publish changes the active pointer/counts | **PROVEN** | PA3 (189 passed): `test_managed_pipeline_activation.py` refresh cases, `test_prose_generation.py::test_refresh_model_failure_preserves_last_good_generation`, `test_generation_failure.py`. The conversion case additionally holds legacy evidence readable until publish, which the 3a review verified has no `status` predicate anywhere on the read path |
| 4 | Every production query uses structural generation selection; every dense/model query uses the dense dispatcher from that same held owner | **PROVEN, with one classified exception** | `query_access`/`query_session` default to `structural=True` (`query_access.py:75,126`); `graph_for` keeps `structural=False` as the explicit low-level legacy default exactly as the plan says. All 58 call sites classify. The exception is `changeset_access.apply`'s raw unrestricted `ctx.graph()` — read strictly, invariant 4 does not admit it; the plan's own audit section does, as an explicitly classified non-evidence operation, and the 4d review proved it can only refuse, carries no identifier into its sentence, and never reaches a response body or a saved record. It is the single reason this row is not an unqualified PROVEN, and it is PA4d finding 2 |
| 5 | Hidden, suppressed, staging, failed or policy-denied evidence cannot affect routing, model resolution, source visibility, counts, scores, traces or errors | **PROVEN** | PA2 and PA6. The ledger's "Mandatory negative evidence" fixture requirements for PA2/PA6 (an authorized empty generation, a policy-denied empty generation, a current-tombstoned generation, hidden wrong-profile evidence, relation-only support, code-only support, shared multi-source objects, a profile change during use, an authorization change during output construction) are recorded as met by the SPEC tables of the `pa2` and `pa4*` reviews. I did not re-derive the nine cases one by one; I take those SPEC tables at their word here |
| 6 | Empty authorized active generations appear in source inventory with zero counts and no Ollama call | **PROVEN** | `test_exact_empty_generation_is_visible_with_control_presentation_and_zero_counts` and `test_empty_selection_resolves_no_profile_and_performs_no_model_request`. The "no model call" assertion belongs on `retrieval_session` (`dense_session` alone makes six), which is what the test uses |
| 7 | Real readers are backed by current local `WorkspaceMembership` and Source authority; open/preview identities cannot create, refresh or delete managed evidence | **PROVEN** | PA1 (186 passed): `test_a_mapped_reader_can_capture_build_authority_without_manual_setup`, `test_removing_the_reviewed_local_authority_invalidates_a_live_build`, `test_identity_and_source_changes_invalidate_a_captured_authority`, plus `BuildActor.reader`/`trusted_local` and the actorless/open cells of the dispatch matrix |
| 8 | Raw roots are configured below the absolute data directory, created only for managed ingest, and retained by delete/reindex | **PROVEN, with a coverage residue** | `managed_activation` builds them lazily at `Path(config.data_dir).resolve() / "knowledge" / "raw-v1"` and the sibling `cache/embeddings-v1`; `test_the_raw_root_and_embedding_cache_appear_only_for_a_managed_build` proves a legacy add, a legacy reindex and a query create nothing, and the delete spies prove retention. The residue is PA3a finding 5: `on_first_connection` and `status.source_view`, both named in plan step 3, are not in that test |
| 9 | Bulk mode preflights all managed authority before any legacy clear and never resurrects tombstones | **PROVEN** | PA5 (184 passed): the mixed-bulk test drives a managed refresh, an eligible conversion, an unsupported legacy lane and a tombstone; `prepared == [w.unsupported]` ("only the legacy lane may be cleared"), the tombstone is skipped, and a failed preflight makes zero mutations. `_preflight_managed` raises `ManagedPreflightRefused` before the first clear |
| 10 | Ladybug reopen preserves source pointers, tombstones, fences, memberships, manifests, counts and query behaviour; Neo4j implements the same transitions without weakening lock/epoch checks | **PROVEN** | PA7 at HEAD (below) on the primary backend, with the ledger's PA7 fixture requirements met (temporary absolute `HIPPO_DATA_DIR`, clean close, reopen of the same database and raw tree, re-run current/history/source-view assertions; nothing writes to the repository's `data/`). Neo4j: `neo4j-parity.md` runs 1–3, currency re-checked above. The one recorded softness is PA3b finding 6 — the two backends express the sweep predicate two different ways — which is a drift risk, not a weakened check |

## What would make PA8 signable

In the order I would take it. Nothing here is large; the total is one small batch plus two
words.

1. **Two one-word documentation fixes, both already identified as unowned**
   (`evidence-cleanup4.md:175-182`): wrap-up finding 7 (`public_errors.py:146` points
   `build_interrupted` at `store/memory.py`; it is `store/generations.py:29`) and finding 16
   (`evidence-pa4e.md:438` says `evidence-pa4cfix.md` does not exist; it does). These are the
   cheapest two left and they are the only findings with no cost at all.

2. **One small code batch over the LOW items that have a named one-line fix**: findings 8
   (`STAGES = (INTERRUPTED_REFRESH_STAGE, …)`), 9 (`view.validate()` before the bulk's `Busy`
   return), 11 (one docstring sentence), 17 (one string, "rebuilt" → "deleted" on that path),
   6 (one test pinning `CLOSED_INPUT_VALIDATORS`), 13 (one test pinning the borrow pair), and
   PA4d finding 2's missing test (a changeset naming a managed object id, which makes the one
   guard that unrestricted read exists for actually fire). Findings 18 and C1 are the same
   `caller_error` line applied seven times in `users.py`/`auth.py`/`pages.py`; taking them
   together closes the last isinstance catches in the web layer and lets the `str(exc)`
   allow-list shrink rather than grow.

3. **Three cheap coverage gaps the plan's own adversarial cases name**: PA2 finding 5 (a
   relation supported by two generations — `plan:254` names it), PA3a finding 5 (add
   `on_first_connection` and `status.source_view` to the raw-root test — `plan` step 3 names
   both), and PA3b finding 8 (`before = row_of(...)`, then assert the row is equal except
   `stage`, `error`, `updated_at`). PA3a finding 3 and PA2 finding 6 are one case each if the
   batch is open anyway. **T3** is the one worth doing beyond them: the fixture blocker that
   made verified-dense-over-HTTP untestable is gone, so the two cases the 4b-ii review called
   fixture-blocked are now just two missing tests.

4. **Refresh `session-audit.md` at HEAD** (finding S1) — or append the one
   `cli.py:567 _sources_locally` row with its class. Completion-checklist item 2 asks for an
   artifact, and the artifact is one row behind.

5. **Three lines in the plan's rollout section** recording the `model_unavailable` behaviour
   change, the CLI ownership question and `add_repo`'s legacy status as Task 16's, so the five
   DEFERRED items are assigned by the plan rather than by a scratch file.

6. **Record the gates.** Replace the eight `[ ]` with `[x]` and an `EVIDENCE:` line each in the
   ledger's own format, and update the "Status:" line. Completion-checklist item 1 cannot be
   true until that happens.

Items that should **not** be "fixed", and why, so a later reader does not reopen them: wrap-up
finding 12 (the three `authorization_changed` spellings — consolidating drags knowledge imports
into `hippo --help`; both halves are now pinned by test); 4b-i finding F2 (the DEBUG traceback
level — adding `exc_info=True` at WARNING would break 4b-i's redaction test, which is the point
of it); 4b-i finding F6 (delete's uniform 404 — a deliberate non-disclosure deviation); PA3a
finding 10 (`present()`'s per-callback locked transaction — the lock is what makes the tombstone
race safe).

The residue that genuinely cannot be closed inside this increment, and that a reviewer should
be willing to sign over once it is written down somewhere durable: PA2 finding 4 (code-edge
attribution by node membership) is unreachable until managed code generations exist, and
`ai_docs/plans/rag-it-all-task-5-managed-code-capture.md` is the natural owner but does not
name it; PA4b2 finding 7's `_simulate` half is unreachable; PA4b2 finding 9 and T2 both wait on
a file nobody owns yet. Four rows, each one sentence in the right plan away from being a clean
DEFERRED instead of an OPEN. That sentence is worth writing: under PA8's clause as it stands,
an unreachable gap with no owner reads exactly like an unfixed defect.

## HEAD moved under this review

`git rev-parse HEAD` was `1e2f112` when I started and `c893a95` when my PA7 run finished
77 minutes later. Twenty-three commits landed in between: the **managed code-capture slice**
(`wp/cc1`, `wp/cc4`, `wp/cc5`, `wp/cc9a`) plus its plan amendments, rulings and design review.
That is a different ledger — `ai_docs/plans/rag-it-all-task-5-managed-code-capture.md`, gates
`CD*` — but it is not disjoint from this one.

**What it touched that a PA gate exercises**

| File | Change | Gates that name a test over it |
|---|---|---|
| `src/hippo/status.py` | `source_view`'s lane predicate becomes `store.source_serves_legacy`; new `_legacy_source` counts legacy passages from the held graph | PA2, PA5, PA6, PA7 |
| `src/hippo/context.py` | the matching legacy/managed classification | PA2, PA6, PA7 |
| `src/hippo/store/generations.py` | `source_serves_legacy` added beside `source_is_managed` | PA1, PA4, PA7 |
| `src/hippo/ingest/prose_generation.py` | coordinator run state factored out into the new `ingest/build_run.py` | PA3, PA7 |
| `tests/unit/test_status_access.py`, `test_managed_source_inventory.py`, `test_prose_generation.py`, `test_import_order.py` | assertions adapted | PA2, PA3, PA5, PA6, PA7 |

**What this does and does not change.**

- **It does not change the verdict.** I re-checked every OPEN finding that lives in a file the
  slice touched, at `c893a95`: PA2 finding 4 is unchanged (`status.py:109-117` still attributes
  inherited code edges by node membership), and so are wrap-up findings 6, 7, 8, 9, 17 and 20,
  `evidence-pa4e.md:438`, and the `users.py` set. Nothing in the slice closes a finding and
  nothing in it opens one against this ledger.
- **It does change which tree the gate results describe.** My eight results — and the
  `EVIDENCE:` lines the orchestrator is writing from its own concurrent run, which match mine
  test-for-test — are at `1e2f112`. Five gates name at least one file the slice rewrote, so
  **PA2, PA3, PA5, PA6 and PA7 need one more pass at `c893a95` before they are recorded as
  true of HEAD.** PA1, PA4 and the PA8 static line name nothing the slice touched at the test
  level, but PA1 and PA4 exercise `store/generations.py`, so the conservative set is
  "everything except the static line". This is the same trap the wrap-up review hit and
  described (its passes A–D); its rule — "a result nobody could cite is worse than no result" —
  is the right one here too.
- **The `EVIDENCE:` format matters more than usual now.** Recording a revision in those lines
  is what makes the difference between "PA6 passed at `1e2f112`, before the code-capture slice"
  and "PA6 passed", which after twenty-three commits is no longer the same claim.

I did not re-run at `c893a95`: my brief pins `1e2f112` and says to run each line once, PA7
alone is 77 minutes, and three other sessions were holding the machine. Flagging it is the
useful move; re-running it on my own initiative against a tree my brief did not name would
produce a second set of numbers nobody asked for while the tree kept moving.
