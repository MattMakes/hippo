# PA8 re-sign-off review: the production activation ledger after the closure batch

Reviewer `architect-reviewer-19`, 2026-09-13. Root tree `/Users/mascott/projects/hippo`, branch
`rag-it-all-tibs`, HEAD **`a7e6a06`** ("Record PA1-PA7 MET at 9770c00, widen PA2, amend CD7/CD8
and record Neo4j parity run 5"), confirmed at the start and again before this file was written.
Brief: `ai_docs/handoffs/briefs/review-pa8-resign.md`. Read-only apart from this file: no source,
test, plan, ledger or evidence document was edited. **No Neo4j was used.**

Every row below was checked by symbol at `a7e6a06`, not by the line numbers the older documents
carry. IDs follow the previous sign-off (`ai_docs/reports/2026-09-12-pa8-signoff.md`): `PA2-5` is
the PA2 review's finding 5, `W15` is the Task 4 wrap-up review's finding 15, `C1`/`S1`/`T2`/`T3`
are that report's unnumbered rows, and `D1`–`D5` are its five DEFERRED items in table order.

## Verdict

**`PA8: NOT SIGNABLE (PA2-2, PA2-4, PA3a-6, PA3a-8, PA3a-9, PA3a-11, PA3b-6, PA4b2-7, PA4b2-9, W15, W20, T2, D4, D5)`**

Of the previous sign-off's **30 OPEN rows**, **18 are now CLOSED** (17 by the closure batch, and
PA2-5 closed as unreachable with its invariant pinned) and **12 are still OPEN** with no plan owner.
Of its **5 DEFERRED rows**, **3 are now DEFERRED by the plan** and **2 (D4, D5) are deferred only by
a review and an evidence file**, which the brief's clause does not accept.

Every blocking row is LOW, minor or INFO. None is a live disclosure or a wrong result on a client
path. The verdict follows from the clause as written, not from severity.

**Why 12 rows were never touched.** The closure batch did everything its brief asked. The gap is in
the work list it was given: the previous sign-off's "What would make PA8 signable" items 1–5 name
18 of the 30 rows. Eight OPEN rows appear in no item: PA2-2, PA3a-6, PA3a-8, PA3a-9, PA3a-11,
PA3b-6, W15 and W20. Four more (PA2-4, PA4b2-7, PA4b2-9, T2) were routed to "one sentence in the
right plan", and that sentence was never written. `pa8-closure.md` then scoped the batch to items
1–5 exactly.

Everything else PA8 asks for holds at HEAD:
- the static line passes (run below);
- the call-site sweep has the same 58 rows `session-audit.md` classifies;
- Neo4j parity is current for every store file (no `src/hippo/store` change since run 5 at `9770c00`).

## Runs

Root tree at `a7e6a06`, `.venv/bin/python` 3.12.11, pytest 9.1.1, Ruff 0.16.6. Commands were copied
verbatim from `GATES.md`, output captured to a log, and results read from the summary line.

| Line | Result | Log |
|---|---|---|
| PA2, verbatim (Fake, bare `-W error`; the line carries no AnyIO filter and none of its four files imports a transport at module level) | **105 passed, 1 skipped in 2.03s — EXIT 0**. Identical to the checker's 105/1 | `/tmp/hippo-pa8resign-pa2.log` |
| PA8 static, verbatim | **EXIT 0**. `ruff check`: `All checks passed!` (on the console; the redirect captured only the second command). `ruff format --check`: `136 files already formatted` | `/tmp/hippo-pa8resign-pa8ruff.log` |
| PA8 call-site sweep `rg -n 'query_session\(\|query_access\(\|graph_for\(\|ctx\.graph\(' src/hippo` | **58 rows**. With line numbers stripped, the set is byte-identical to the same sweep at `c893a95`, the revision `session-audit.md` was refreshed at | `/tmp/hippo-pa8resign-callsites.log` |
| PA7 | **Not re-run**, per the brief. Cited from the checker: `742 passed in 4111.53s (1:08:31)`, provenance in section 2 | `/tmp/hippo-orch-pa-gates-3.log` |

The static line reports 136 formatted files where the checker saw 135. The difference is
`src/hippo/ingest/code_generation.py`, which is new since `9770c00` (see section 2).

## 1. Every previously OPEN or DEFERRED row, at HEAD

### CLOSED — 18

"Gate" names the PA CHECK lines that run the closing test.

| # | Sev | Commit | Test or artifact (gate) | Verified at HEAD |
|---|---|---|---|---|
| PA2-5 | minor | `6adc2af`, merged `be1cfc0` | Closed as **unreachable by design**. `tests/unit/test_multi_generation_support.py` has 8 tests (PA2): each of the five store refusals (`Sealed assertion proof group cannot gain support`, `Incomplete assertion proof group` ×2, `Incomplete exact interpretation closure`, both borrow doors, `Generation member belongs to another source`) plus `test_every_projected_relation_carries_exactly_one_contributing_pair` | Ruling at `task4-notes.md:149-158`; decision at `:159-166` keeps the seal clause. The PA2 line gained the file in `a7e6a06` and my run includes it (97 + 8 = 105). The discrimination proof (relax the seal clause and 2 pins fail) is in `evidence-pa2f5.md`. Fake and Ladybug are green per that evidence, and Neo4j parity run 4 includes the file. Residual, not a row: the multi-pair branch in `status.source_view` is dead code, and its simplification is assigned to "a later cleanup slice" that has no name |
| PA2-6 | minor | `4be9920` | `test_a_lane_holding_only_an_empty_generation_still_owns_the_composed_version` (PA2, PA7) | Asserts `compose_graphs(...).version` in both argument orders, plus the two-contributor and no-contributor cases |
| PA3a-3 | minor | `4be9920` | `test_a_single_reindex_of_an_unsupported_source_with_an_actor_stays_legacy` (PA1, PA3, PA5, PA7) | Calls `pipeline.reindex(ctx, <.py upload>, build_actor=actor)` and asserts the legacy lane, that `_prepare_reindex` ran, and `managed` false |
| PA3a-5 | minor | `4be9920` | `test_the_raw_root_and_embedding_cache_appear_only_for_a_managed_build`, extended (PA1, PA3, PA5, PA7) | `on_first_connection` runs through `getattr(store, "on_first_connection", store.ensure_schema)` (the real bootstrap on Ladybug in PA7), then `status.source_view` under `query_session`, both before the managed half, with `knowledge_root` asserted absent |
| PA3b-8 | minor | `4be9920` | `test_a_refresh_interrupted_by_a_restart_is_retired_without_losing_g1` (PA1, PA3, PA5, PA7) | Compares every field of `before = row_of(...)` with the recovered row except `stage`, `error` and `updated_at`, and asserts `progress_done`/`progress_total` explicitly. Note: `evidence-pa8close.md` names this test `test_an_interrupted_refresh_is_retired_and_the_published_generation_keeps_serving`, and no test by that name exists |
| PA4d-2 | minor | `ccb6635` | `test_a_changeset_naming_managed_evidence_is_refused_by_the_unrestricted_read` (PA6, PA7) | The guard now fires. The test asserts the fixed sentence, no id interpolated, an unchanged `graph_version`, and that the changeset is still a draft. The review's optional refactor (a narrow predicate before the transaction) was "not urgent" and was not taken. `changeset_access.apply`'s `ctx.graph()` remains the one classified exception in `session-audit.md` and invariant 4, which is exactly the route the previous sign-off named (the missing test) |
| W6 | LOW | `ccb6635` | `test_the_pipelines_closed_validator_tuple_is_the_public_tables_own_family` (PA5) | Compares `pipeline.CLOSED_INPUT_VALIDATORS` with the `invalid_source` family of `public_errors._ROWS`, so neither side can grow alone |
| W7 | LOW | `4e235bb` | Documentation | The table comment in `knowledge/public_errors.py` now names `store/generations.py` for `INTERRUPTED_REFRESH_ERROR` |
| W8 | LOW | `ccb6635` | No test. An identity test cannot fail, because both spellings are one interned literal | `managed_activation.STAGES = (INTERRUPTED_REFRESH_STAGE, …)`, imported from `store.generations` on the line that already brought `REFRESHING_PREFIX` |
| W9 | LOW | `ccb6635` | `test_a_bulk_refused_as_busy_still_makes_the_revocation_check_its_siblings_make`, `…_with_nothing_revoked_still_says_busy` (PA6, PA7) | `sources.reindex_all`'s `except Busy` calls `view.validate()` before its 409, matching both siblings. The evidence correctly says these tests were green on arrival, because `query_session` already validates on exit |
| W11 | LOW | `ccb6635` | Documentation | `graph.managed_evidence_exists` docstring: "workspace metadata answering a manager's question … not evidence answering a reader's question" |
| W13 | LOW | `ccb6635` | `test_run_question_hands_the_dispatcher_a_session_or_an_audience_but_never_both` (PA6) | A borrow hands `access=None` and the session. An owner hands the audience and no session. The pair raises `DenseSessionUnavailable` with reason `invalid_borrow` |
| W16 | INFO | `4e235bb` | Documentation | `evidence-pa4e.md` residual 6 now carries the dated correction (the file exists, 457 lines, `603dadd`) |
| W17 (= PA3b-3 wording) | INFO | `ccb6635` | `test_the_actorless_refusal_names_the_delete_path_it_also_guards` (PA1, PA3, PA5, PA7) | `plan_dispatch` raises "A managed source cannot be rebuilt or deleted without a build actor", and the test drives both `reindex` and `delete_source`. Naming both operations, rather than the review's literal verb swap, is correct, because both entry points share that one classification |
| W18 | LOW | `ccb6635` | `test_a_user_or_role_write_that_fails_is_mapped_rather_than_quoted_back_as_a_400` (parametrized), `test_the_role_form_still_shows_the_validators_sentence_but_not_a_subclasses`, `test_a_password_change_that_fails_is_not_redirected_back_with_its_own_words` (PA6, PA7) | `users.py`'s five `HTTPException(400, str(exc))` sites and `auth.py`'s password redirect re-raise unless `caller_error(exc)`. The two role-form `_back(getattr(exc, "detail", str(exc)))` sites also keep pydantic `ValidationError`, which is that form's own field message. `STR_EXC_SITES` records all of them as guarded |
| C1 | LOW | `ccb6635` | `test_a_settings_form_write_that_fails_is_mapped_rather_than_rendered_into_the_page` (PA6, PA7) | `pages.settings_submit` re-raises unless `caller_error(exc)`. The test asserts 500 `operation_failed` and no leaked path |
| S1 | LOW | `4e235bb` | `session-audit.md`, refreshed at `c893a95` with the `cli.py _sources_locally` row | Still current at HEAD: the sweep's symbol set is unchanged (Runs table). Line numbers in the document may drift; classification does not |
| T3 | LOW | `4be9920` | `test_a_reader_reaches_verified_dense_over_http`, `test_revoking_during_a_verified_dispatch_is_the_generic_permission_answer` (PA6, PA7) | A reader's bearer token over `TestClient` reaches `/api/graph/light-up` with `modes == ["verified"]`. The revocation test asserts `dense_capability.mode == "verified"`, bumps the epoch while the owner is held, and gets 409 plus the permission body |

### OPEN — 12, no owner in any plan

The previous sign-off's findings table already verified each row's claim. This table records its
state at HEAD and the smallest change that would move it to CLOSED, DEFERRED-by-plan, or
unreachable-and-pinned. A `grep` across `docs/rag_it_all.md`, `ai_docs/plans/`, `task4-notes.md`,
`whats-next.md` and the checkpoint found no assignment for any of them.

| # | Sev | State at `a7e6a06` | What would close it |
|---|---|---|---|
| PA2-2 | minor (latency) | `knowledge/projection._selected_pairs` still runs `any(... for revision_id in revisions)` once per selected generation | The review's `by_generation` index, or one sentence assigning it to Task 16's performance work |
| PA2-4 | minor (latent) | `status.source_view` still counts every `CODE_EDGE_KINDS` arrow whose source vertex is in `node_ids`, which is attribution by node membership. Only relation-predicate counts check `pair in row.source_generations`. **The precondition is closer than at the last sign-off:** `ingest/code_generation.py` (CC9b, `bc7ea22`) now builds managed code generations with native `CODE_EDGE` rows. Nothing under `pipeline.py`, `managed_activation.py`, `web/`, `cli.py` or `mcp_server.py` imports it, so this is still unreachable at HEAD. `rag-it-all-task-5-managed-code-capture.md` does not name this counter | A sentence in the code-capture plan assigning per-pair code-edge attribution to the slice that dispatches the code coordinator, before managed code is served. Or the fix now |
| PA3a-6 | minor (defence in depth) | `managed_activation.ingress_file` still checks `entry.name != safe_stored_name(entry.name)`, not that the name equals `stored_filename(source)` | The review's one-line check |
| PA3a-8 | minor (coverage, decision) | `PlainBuildOptions.max_chunks = 1000` against legacy `pipeline.MAX_CHUNKS = 20_000`. The coordinator half is covered (`test_bootstrap_bound_excess_leaves_no_managed_state`, `max_chunks=1`). No managed-lane test shows the Source row reaching `failed` with a bounded code past the ceiling, and no 1,000-vs-20,000 decision is on record | One test plus one plan sentence recording the decision |
| PA3a-9 | minor (documentation) | Two definitions remain: `managed_eligibility` reads `source["managed"]`; `build_authority._source_control` reads `managed or active_generation_id`. `db7aa77` removed the Artifact/Generation scan and `meta["managed"]` from the disjunction, so the gap is narrower. `managed_eligibility`'s docstring ("decided on the row alone") still does not name `_source_control` as the deliberately broader guard, while `graph.managed_evidence_exists` asserts "`managed_eligibility` is the one definition" | The review's comment at `managed_eligibility` |
| PA3a-11 | minor (design) | `managed_activation.py` and `knowledge/dense_session.py` both import the private `ollama._base_name` and repeat `EMBED_PREFIXES.get(_base_name(...), ("", ""))`, with a third copy inside `ollama.py` | A public helper in `ollama.py`, or a named owner in a plan |
| PA3b-6 | minor (backend drift) | `store/ladybug.py`'s sweep still builds ``STARTS WITH '{REFRESHING_PREFIX}'`` into the Cypher text. `store/memory.py` passes `prefix=REFRESHING_PREFIX`. The Ladybug fix converted only `IN $list` predicates | Parameterise it (a `store/*` owner is needed), or a plan sentence |
| PA4b2-7 | LOW (latent) | Web `analyze._simulate` now calls `EvalAccess(...).get_result` and `_saved_result_check` inside the route's `except (ValueError, OllamaError, httpx.TransportError)`, which feeds `retrieval_failure`. `get_result` is `@_guarded_item`, and every read on its path (`_refs`, `_authorized`, `get_question`) answers `None`; none raises `EvalAccessDenied`. `EvalAccessDenied` has no row in `public_errors`, so if one ever reached this catch it would become `operation_failed`. Still unreachable, still unpinned | A test pinning that a denied `result_id` through `/api/simulate` gets 404, not 500, which makes it unreachable-and-pinned. Or an exact-type `EvalAccessDenied` catch ahead of the mapper |
| PA4b2-9 | LOW | `test_query_authorization_boundary.py` and `test_query_snapshots.py` still monkeypatch the private `ctx._build_structural_graph` | A public seam in `context.py`, or a plan sentence naming its owner |
| W15 (= PA3b-4 class) | LOW | `pipeline._submit_lane` still catches only `managed_activation.ManagedDispatchError`. A `Busy` or a store failure from `start_indexing` still escapes the loop, leaving the lanes after it cleared and without a job | The review's `except Exception` with the same bounded log line |
| W20 | LOW | `templates/partials/` has no `failure.html`. `web/app.py`'s handler renders the full `failure.html` for every HTML caller, including `/partials/sources` | The review's partial template, chosen by `render.wants_html` |
| T2 | LOW | **Half closed.** The scan half is CLOSED by `db7aa77` (CC8, merged `d1910c8`): `_source_control` reads the Source row alone, pinned by `test_build_authority.py:456-467` ("no record table is scanned"). What remains per check is two `_knowledge_rows("AccessPolicy")` reads, which T2 did not name. **The version half is OPEN.** Coordinator review minor 7 asked for two sentences in the coordinator plan's "Coordinator implementation notes": the pipeline/parser/materializer versions are fixed constants (`prose_generation._configuration`'s `input_pipeline`, and `_generation`'s `parser_version`/`linker_version`), and `workers`/`embedding_cache` were added. `task4-notes.md:119` says this was "recorded in the plan notes by the fixer". It was not: that 11-line section has no such sentence, and plan section 3 (`:55`) still lists the versions as `PlainBuildOptions` fields | The two sentences |

### DEFERRED — 5

| # | Item | Status at HEAD |
|---|---|---|
| D1 | A gated `hippo index` sets no `owner_id`/`access_role_id` | **DEFERRED by the plan**: `rag-it-all-task-5-production-activation.md:269`, Task 16 |
| D2 | `add_repo` takes no `build_actor` | **DEFERRED by the plan**: `:270`, Task 16 |
| D3 | A gated `hippo index note.md` can report `model_unavailable` | **DEFERRED by the plan**: `:268`, Task 16 |
| D4 | `answer_withheld` conflates a withheld answer with a failure that saved a trace | **Not in the plan.** Deferred only by `evidence-pa4f.md:370-373` and the wrap-up review (`:197-199`, `:265`). The closure brief said "three lines … assigning the five DEFERRED items" but named three, and `evidence-pa8close.md`'s row marks "the five DEFERRED items" CLOSED on those three lines |
| D5 | `summarize`'s gold-means rule lets `accuracy: None` sit beside `errors: N` | **Not in the plan.** Same sources (`evidence-pa4f.md:383-385`; wrap-up `:197-199`, `:265`) |

Task 16 exists (`docs/rag_it_all.md:1525`, gates G16/G17). Two more bullets under the plan's
"Rollout and rollback boundary" close D4 and D5.

## 2. Does the ledger record what it claims?

**Every `[x]` has an EVIDENCE line from the reverify pass.** All seven EVIDENCE outputs match the
PASS lines of `/tmp/hippo-orch-pa-gates-3.log` character for character:

| Gate | EVIDENCE output = log-3 PASS output |
|---|---|
| PA1 | `188 passed, 2 skipped in 29.49s` |
| PA2 | `105 passed, 1 skipped in 2.35s` (with `test_multi_generation_support.py`) |
| PA3 | `191 passed, 3 skipped in 26.91s` |
| PA4 | `117 passed in 6.84s` |
| PA5 | `188 passed, 2 skipped in 30.26s` |
| PA6 | `728 passed in 95.34s (0:01:35)` |
| PA7 | `742 passed in 4111.53s (1:08:31)` |

PA8 is `[ ]` with no EVIDENCE line. That is correct: the checker ran its static command (exit 0,
135 files) and reported `EXPECT=not matched`, because PA8's EXPECT is a prose clause only a sign-off
review can satisfy.

**The Status line names the revision**: `9770c00`, with the log path, and says outright that the
EVIDENCE lines carry no git revision.

**The PA7 evidence comes from a process that started at `9770c00`.**
- The six approval files log-3 cites for PA1–PA6 (`c8be210e…`, `8d77ac9f…`, `356b1975…`,
  `eeb7c01c…`, `6193413e…`, `600226b8…`) all have mtime **2026-09-12 06:33:01 -0700**. `9770c00` was
  committed at **06:32:17**.
- The log's last write is **07:44:48**. 06:33:01 plus the seven reported durations (191.19 s for
  PA1–PA6, then 4111.53 s) is 07:44:44, and the PA8 ruff pass fits the remaining seconds. The gates
  ran one after another, so PA7 started at about **06:36:12**.
- On `rag-it-all-tibs`'s first-parent line, the commit after `9770c00` is `bc7ea22`, at
  **2026-09-13 10:57:24**. HEAD was `9770c00` for the entire run.
- The excluded run is log-2 (`/tmp/hippo-orch-pa-gates-2.log`). Its PA8 approval `fa435c79…` is
  dated 04:21:51, between `c893a95` (04:17:34) and `e02bb08` (04:32:44). Its PA7 reports
  `723 passed in 4517.50s`, not the ledger's 742.
- Caveat: the checker runs in the root working tree, so an uncommitted edit during that window
  cannot be ruled out after the fact. `git status` now shows only untracked documentation.

**`9770c00` evidence still stands for `a7e6a06`.** `git diff 9770c00..a7e6a06 -- src tests` touches:
- `codegraph/git_history.py`: adds `shallow_boundary` only; `read_history` is unchanged.
- `knowledge/code_binding.py` and `knowledge/code_history.py`: add `stored=None` parameters. `_reuse(revision, None)` returns the revision unchanged, so existing callers behave identically.
- The new `ingest/code_generation.py`, which no production module imports.
- `test_code_binding.py`, `test_code_generation.py` and `test_code_history.py`.

None of those test files is on a PA line. Only three PA-line files mention these names:
- `test_ingest_pipeline.py` monkeypatches `git_history.read_history`, which is unchanged;
- `test_managed_transport_activation.py` calls a remote method named `code_history`;
- `test_managed_eval_activation.py` mentions one in a docstring.

No `src/hippo/store` file changed, so Neo4j parity run 5 (at `9770c00`) remains current. My PA2 and
static re-runs at `a7e6a06` agree.

**The closure batch's open Ladybug blocker is closed by recorded evidence.** `evidence-pa8close.md`
recorded its Ladybug lane as "BLOCKED BY A BASE REGRESSION": five managed builds did not publish.
PA7 at `9770c00` runs `test_managed_pipeline_activation.py` on Ladybug, including those five tests
and the extended raw-root test, and reports 742 passed with no skips. `evidence-lbfix.md` pins the
cause with three tests in `test_generation_scoped_reads.py`.

**Recording deviations (not blocking):**
- The EVIDENCE lines carry `path=b6bf9549d64b/37 entries` where the ledger's own format asks for the source revision. The Status line compensates, explicitly.
- The Status line says the pass ran "on 2026-09-13". It ran on 2026-09-12, 06:33–07:44 -0700; 2026-09-13 is when `a7e6a06` recorded it.
- `a7e6a06`'s commit message (and the spawn message) give PA7 as "77 minutes". The recorded run took 1:08:31. The 77-minute figure matches the earlier `c893a95` run (1:15:17) and the previous reviewer's own run (1:16:32).

## 3. The do-not-fix items were left alone

The previous sign-off names **four** items, not two, and all four are unchanged since `1e2f112`:

| Item | Evidence at HEAD |
|---|---|
| W12, three `authorization_changed` spellings | `cli.DENIED_CODE`, `mcp_server.DENIED_CODE`, `web/app.AUTHORIZATION_CHANGED` are all present. `git log -S'authorization_changed' 1e2f112..HEAD -- src/hippo` is empty. Both pins exist: `test_the_three_copies_of_the_denial_code_are_the_same_string`, `test_hippo_help_imports_no_serving_machinery` |
| 4b-i F2, traceback at DEBUG only | `pages.py` still logs `log.debug("ask failure detail [%s]", operation, exc_info=True)`. `git log -S'exc_info' 1e2f112..HEAD -- src/hippo/web/routes/pages.py` is empty. That file's diff since `1e2f112` touches only an import and `settings_submit` (C1) |
| 4b-i F6, delete's uniform 404 | `sources.delete_source` still maps `AuthorizationChanged` to `404 "no such source"`, with the non-disclosure comment. The file's only hunk since `1e2f112` is in `reindex_all` (W9) |
| PA3a-10, `present()`'s locked transaction per call | `managed_activation.present` still opens `store.transaction()` and takes `_lock_source`. The module's hunks since `1e2f112` are the import, `STAGES` (W8) and the `plan_dispatch` sentence (W17) |

## 4. Ruling on the clause

The brief's rule: SIGNABLE only if every remaining row is CLOSED, DEFERRED by the plan, or closed as
unreachable with a pinned invariant.

- **CLOSED or unreachable-and-pinned:** 18 of the 30 previously OPEN rows.
- **DEFERRED by the plan:** 3 of the 5 previously DEFERRED rows.
- **None of the three:** PA2-2, PA2-4, PA3a-6, PA3a-8, PA3a-9, PA3a-11, PA3b-6, PA4b2-7, PA4b2-9,
  W15, W20, T2, D4, D5.

**`PA8: NOT SIGNABLE (PA2-2, PA2-4, PA3a-6, PA3a-8, PA3a-9, PA3a-11, PA3b-6, PA4b2-7, PA4b2-9, W15, W20, T2, D4, D5)`**

Three routes to a signature, cheapest first:

1. **Write the missing plan sentences.** Plan text alone moves PA2-2, PA2-4, PA3a-11, PA3b-6 and
   PA4b2-9 to DEFERRED-by-plan once each names an owner:
   - PA2-2: Task 16 performance.
   - PA2-4: the code-capture slice that dispatches the code coordinator.
   - PA3b-6: a `store/*` owner.
   - PA3a-11 and PA4b2-9: whoever next owns `ollama.py` and `context.py`.

   Plan text alone also closes T2 (two notes sentences) and D4/D5 (two rollout bullets).
2. **One small batch** for the rows whose fix is a line or a test: PA3a-6, PA3a-9's comment, W15,
   W20, PA3a-8's test and decision, and PA4b2-7's pin. Every one is named in its review.
3. **Or reword the clause.** The previous sign-off proposed "no unresolved findings above LOW". The
   orchestrator can record that ruling and accept the enumerated residue above. Under that wording
   PA8 would be signable at `a7e6a06` today: nothing above LOW remains, and the static line,
   call-site audit and Neo4j parity all hold.

## Nits noticed along the way (not rows)

- `evidence-pa8close.md`'s header says "CLOSED: 15", but its table marks 17 finding rows plus the deferral row CLOSED.
- `evidence-pa8close.md` misnames the PA3b-8 test (see that row).
- `task4-notes.md:119` says coordinator minor 7 "is recorded in the plan notes by the fixer". It is not (T2).
- `pipeline.delete_source`'s own sentence "A managed source cannot be deleted without a build actor" cannot be reached, because `plan_dispatch` refuses first. `evidence-pa8close.md` deviation 1 already records this.
