# Plain-prose coordinator gates

Scope: explicit API only. No production route, bulk dispatch or source deletion activation.

Status: independent review complete and its blockers resolved. PC1 (65 passed / 2 skipped), PC3 (136 passed) and PC4 (Ruff clean) pass at HEAD 7c0cecb; PC2 (66 passed / 1 skipped, 0 failed on real Ladybug) and PC-N4 (65 passed / 2 skipped, 0 failed on disposable Neo4j) ran at 82bd317, immediately before the txown store increment merged, and the orchestrator accepted them because the two coordinator files are byte-identical across that merge. The two coordinator files are still uncommitted in the root tree. Review findings 1, 2, 4 and 5 and minors 6, 8, 9(a) and 10 are resolved here; minors 7, 9(b) and 11 are deferred to files outside this slice's ownership. Finding 3's store half landed separately as txown (7e6328c, evidence-txown.md beside this file), so only the coordinator's own probe at prose_generation.py:651 still reads process-global transaction state; per that evidence the orchestrator switches it after this slice commits and un-skips test_concurrent_build_is_not_rejected_by_another_threads_transaction, which is that one line's RED. Neo4j parity is recorded under PC2 as PC-N4. Nothing in this slice activates a production route. Runnable checkboxes await root gate checking.

- [ ] PC1: Fake end-to-end input/authority/bootstrap/refresh/recovery/receipt/cancellation and privacy checks cover plan PC1–PC6.
  CHECK: HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_prose_generation.py -o addopts='' -q -W error
  EXPECT: passed
  EVIDENCE: 65 passed, 2 skipped on Fake with -W error at HEAD 7c0cecb (re-run after the txown merge; identical result at 82bd317). Log: /tmp/hippo-prose-fix-pc1.log. The two skips are the real Ladybug close/reopen contract (runs green under PC2) and test_concurrent_build_is_not_rejected_by_another_threads_transaction, which stays skipped until prose_generation.py:651 switches to store.in_ambient_transaction() (review finding 3; see the plan's "Known limitation (deferred 2026-09-11)" and its same-day update). Baseline before the fixes was 59 passed / 1 skipped (/tmp/hippo-prose-fix-baseline.log). RED evidence: /tmp/hippo-prose-fix-red.log — part 1 is test_pause_during_renewal_heartbeat_read_keeps_a_healthy_build_healthy failing against unmutated code with the bogus AuthorizationChanged of finding 4; part 2 is both test_authority_change_after_publication_write_rolls_back_and_keeps_g1 cases failing only under a temporary mutation that deletes _publish's after-publication epoch recheck (finding 5), proving the new tests pin that check rather than the later captured-authority comparison. The source was restored byte-identical after the mutation.

- [ ] PC2: Disposable Ladybug end-to-end rollback, reopening, fencing and raw retention cover plan PC7.
  CHECK: HIPPO_TEST_STORE=ladybug .venv/bin/pytest tests/unit/test_prose_generation.py -o addopts='' -q -W error
  EXPECT: passed
  EVIDENCE: 66 passed, 1 skipped, 0 failed in 1117.98s (0:18:37) on real Ladybug with -W error, run at HEAD 82bd317 — i.e. before the txown store increment (7e6328c) merged. The orchestrator schedules the final Ladybug pass after prose_generation.py:651 switches to store.in_ambient_transaction(); that later run, not this one, is the published state. Log: /tmp/hippo-prose-fix-pc2.log. The single skip is the deferred finding-3 case; the Ladybug close/reopen contract runs green here. This replaces the review's RED result (2 failed / 58 passed in 992.16s, /tmp/hippo-prose-review-pc2.log). Both failures were test-harness wall-clock budgets, fixed as review findings 1 and 2 with no production change: test_two_detached_bootstrap_workers_admit_exactly_one_atomic_winner now derives its barrier/join budgets from the backend (5s/10s on Fake, 120s/300s elsewhere, sized against the measured 18.59s single Ladybug bootstrap in /tmp/hippo-prose-review-timing.log), and test_inflight_model_return_waits_for_renewal_transaction_without_false_ambient_error's unblock helper now waits on the rendezvous events with no wall clock, with teardown setting held/checking/release so neither wait can outlive the build.
  PC-N4 EVIDENCE (disposable Neo4j parity for the same file; not a separate runnable gate — do not re-run it as part of gate checking, the container admits one pytest process at a time): 65 passed, 2 skipped, 0 failed in 1100.64s (0:18:20), EXIT 0, with -W error, using HIPPO_TEST_STORE=neo4j NEO4J_URI=bolt://127.0.0.1:32774 NEO4J_USER=neo4j NEO4J_PASSWORD=hippo-disposable-test against the same file and flags as PC2. Log: /tmp/hippo-prose-fix-neo4j.log. One exclusive run, held for its whole duration and released on completion; no second Neo4j process ran. The two skips are the Ladybug-only close/reopen contract and the deferred finding-3 case. Run at HEAD 82bd317 (pre-txown), accepted as-is by the orchestrator because the coordinator files are byte-identical at 7c0cecb; the final Ladybug plus Neo4j pass is scheduled after the prose_generation.py:651 probe switch.

- [ ] PC3: Reviewed authority, preparation/writer, profile and existing ingestion compatibility remain green.
  CHECK: HIPPO_TEST_STORE=fake .venv/bin/pytest tests/unit/test_build_authority.py tests/unit/test_managed_prose_preparation.py tests/unit/test_staged_prose_writer.py tests/unit/test_generation_profiles.py tests/unit/test_ingest_concurrency.py -o addopts='' -q -W error
  EXPECT: passed
  EVIDENCE: 136 passed on Fake with -W error at HEAD 7c0cecb (re-run after the txown merge; identical result at 82bd317). Log: /tmp/hippo-prose-fix-pc3.log.

- [ ] PC4: Ruff, formatting and independent review (plan PC8).
  CHECK: .venv/bin/ruff check src/hippo/ingest/prose_generation.py tests/unit/test_prose_generation.py && .venv/bin/ruff format --check src/hippo/ingest/prose_generation.py tests/unit/test_prose_generation.py
  EXPECT: 2 files already formatted
  EVIDENCE: All checks passed; 2 files already formatted. Log: /tmp/hippo-prose-fix-pc4.log. Independent review is recorded in ai_docs/reports/2026-09-11-prose-coordinator-review.md; its findings 1, 2, 4, 5 and minors 6, 8, 9(a), 10 are resolved in this slice. Finding 3 is deferred to the store layer, minor 7 (plan §3 drift) and minor 9(b) (_source_control table scans) are deferred to files outside this slice's ownership, and minor 11 is documentation for the next brief.

RED/GREEN history: 23 initial missing-module failures; unauthorized own-setup epoch rebase; immutable retired-operation receipt and operation-key retargeting; shared-depth heartbeat false rejection; sticky cancellation/progress failure; changed-input expired-attempt recovery; original publication-credential mismatch. Each observed failure was followed by the focused fix and green run. Exact logs: /tmp/hippo-prose-coordinator-{red,delta-red,lifecycle-red,latch-red,recovery-red}.log. Additional admission, source-policy, partial-start, concurrent-bootstrap, held-renewal and persistence cases strengthen those boundaries; their passing runs do not claim any production dispatch activation.
