# CDK Neo4j parity (root-owned)

Run by the orchestrator only, against the disposable container the orchestrator grants, never by a
worker and never against port 7687. One pytest process at a time. Each line is recorded here with
exit code, revision and test count once it has run.

- CK1: `HIPPO_TEST_STORE=neo4j .venv/bin/pytest tests/unit/test_store_migrations.py tests/unit/test_generation_store.py tests/unit/test_policy_migration.py tests/unit/test_knowledge_scoped_reads.py -q -o addopts='' -W error` — RUN 2026-09-15 by the orchestrator at `9e5b93c` (S1b merged) against `hippo-rag-test-b780ab5` (bolt 127.0.0.1:32774), one pytest process: exit 0, 114 passed, 4 skipped, log `/tmp/hippo-orch-ck1-neo4j.log`. Proves the four-step v8 journal, the `knowledge_unit_id` constraint, the three `Unit` indexes and the classification backfill on Neo4j.
- CK3: `HIPPO_TEST_STORE=neo4j .venv/bin/pytest tests/unit/test_connector_sync.py tests/unit/test_staged_records.py tests/unit/test_generation_profiles.py -q -o addopts='' -W error` — RUN 2026-09-16 by the orchestrator at `94c891f` (S3c merged) against `hippo-rag-test-b780ab5` (bolt 127.0.0.1:32774), one pytest process: exit 0, 108 passed, 2 skipped, log `/tmp/hippo-orch-ck3-neo4j.log`. The failure matrix M1–M22 and M8b, the staged writer and the connector profile hold on Neo4j.
- CK5: `HIPPO_TEST_STORE=neo4j .venv/bin/pytest tests/unit/test_connector_local.py tests/unit/test_connector_git.py -q -o addopts='' -W error` — RUN 2026-09-16 by the orchestrator at `01a121e` (S5b merged) against `hippo-rag-test-b780ab5` (bolt 127.0.0.1:32774), one pytest process: exit 0, 54 passed, log `/tmp/hippo-orch-ck5-neo4j.log`. Both parity suites (prose and code worlds) hold on Neo4j.
- CK6: `HIPPO_TEST_STORE=neo4j .venv/bin/pytest tests/unit/test_connector_exemplar.py tests/unit/test_connector_surfaces.py -q -o addopts='' -W error` — RUN 2026-09-16 by the orchestrator at `0d50887` (S6 merged at `972f269`) against `hippo-rag-test-b780ab5` (bolt 127.0.0.1:32774), one pytest process: exit 0, 42 passed, log `/tmp/hippo-orch-ck6-neo4j.log`. The exemplar syncs, is retrievable and its surfaces answer on Neo4j.

- CK3 (rerun after `r7-fix`): the same command — RUN 2026-09-16 by the orchestrator at `ce653d3` (the review's fix slice `r7-fix` merged at `7c8f132`; it changed `sync.py`, `guard.py`, `emit.py` and `predicates.py` after the run above) against `hippo-rag-test-b780ab5` (bolt 127.0.0.1:32774), one pytest process: exit 0, 112 passed, 2 skipped, log `/tmp/hippo-orch-ck3-neo4j-2.log`. The four tests `r7-fix` added hold on Neo4j too; this run is CK3's Neo4j parity of record.
